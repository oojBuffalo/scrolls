# Autonomous roadmap & automation buffer

How the hourly autonomous worker (`work/scrolls-dev`) spends its runs. This is
the **scheduling layer** under `docs/product/prd.md` (direction) and
`docs/product/mvp.md` (scope). The cron worker and launcher live outside the
repo (`/Users/claw/.hermes/scripts/scrolls_hourly_progress_worker.sh` and
`…_launcher.sh`); this doc is the queue they should read.

**How to read the hour slots.** The launcher fires hourly but holds a lock, so a
run that overruns an hour causes later ticks to **skip, not overlap**. The
"hour-by-hour" buffer below is therefore an *ordered queue of intended coherent
slices*, not a promise that slot N starts exactly at hour N. A run picks the next
unstarted slice whose preconditions are met, finishes it to a real stopping
point (committed, tested, clean), and stops — even if that consumes several
nominal slots.

**Authority.** Slices are justified against `docs/custody-vision.md` and the
MVP. New adapters are out unless they introduce a new custody shape
(custody-vision §2.7). Anything not on the queue still loses to "finish the
half-done slice from the previous run first."

---

## 24h buffer (concrete) — 2026-06-16 → 2026-06-18

Ordered queue. `→ Mn` marks the MVP slice; `cap N` marks the PRD capability.

| Slot | Intended slice | Maps to |
| --- | --- | --- |
| H1 (2026-06-16) | **✓ shipped.** Sentinel boundary defined and implemented for compiled `library/` pages: pure helper `src/scrolls/generated.py` (`@generated`…`@end` fence, preserve user regions, tombstone stale-annotated pages) wired through `src/scrolls/kb.py`. Tests in `tests/test_generated.py`, `tests/test_kb.py`. See ADR 0102. | → M1, cap 6 |
| H2 | **✓ shipped (with H1).** KB compiler writes every generated region inside the fence; a re-compile replaces only the fenced content and keeps annotations outside it. `docs/library-format.md` regenerated (pinned examples now assert the generated body; new sentinel-fence example added). MCP `get_tag_page` reads the heading from the fenced body. | → M1, cap 6 |
| H3 | **✓ shipped.** M1 extended to generated `agents/` files: `install_agent_docs` now writes through `generated.write_generated`, with skill frontmatter as a regenerated *header* pinned at byte 0 (new `header=` arg on `splice`/`write_generated`) and the shared body as the fenced region. A note after `@end` survives a reinstall; `SKILL.md` keeps a suffix annotation, header-less `AGENTS.md` either side. Tests in `tests/test_agents.py`, `tests/test_generated.py`; `_TARGETS` now maps relpath→header. | → M1, cap 6 |
| H4 | **✓ shipped (with H3).** `docs/architecture.md` Agent-install entry now describes the refresh-safe boundary; `docs/library-format.md` agents section documents the contract and points at `test_agent_install_preserves_annotation_outside_the_fence`. ADR 0102 consequences confirmed: both `kb.py` and `agents.py` now carry the generated-vs-user boundary it predicted. Full `uv run pytest` green (2297). **M1 complete.** | → M1, cap 6 |
| H5 | **✓ shipped.** M2 contract written into `docs/cli.md` as a new top-level "The completeness contract" section, split into **G1** (honest absence / honest failure — empty ≠ error, nothing fabricated) and **G2** (honest scope / truncation, the H6–H8 enforcement target). G1 is locked *now* by a cross-surface invariant module `tests/test_completeness.py` (search/list/related/works/context/doctor); cross-referenced from `docs/library-format.md` "What consumers may rely on". Full suite green (2318). | → M2, cap 7 |
| H6 | **✓ shipped.** M2 G2 enforced on `search` + `list` via an opt-in `--stats` envelope. New pure builder `src/scrolls/scope.py` (`scope_envelope` → `{scope, stats, results}`, consistent with the `works`/`graph` stats companion); `search.py` gains `count_matches` (the honest denominator past the cap, so `truncated` iff `matched > returned`); `list` gains `--limit`. The bare array stays the default, so G1's locked `[]` empty form and the CLI/MCP list contract never regress (the documented additive resolution of the G1/G2 tension). Tests: `tests/test_scope.py`, `tests/test_search.py` (count_matches), `tests/test_cli.py` (scope echo, truncation, opt-in default, empty-scope honesty). `docs/cli.md` G2 marker flipped for these two surfaces; `README.md` updated. Full suite green (2347). | → M2, cap 7 |
| H7 | **✓ shipped. M2 enforce on `context` + `related` + `works`.** `related`: `--stats` echoes anchor + limit + truncation, `count_related` the past-the-cap denominator. `works`: an always-on `scope` companion in `to_payload` naming the `--min` floor or the resolved `ref` anchor (no `--stats` flag — `works` already emits an object, no bare array to protect; uncapped, so the floor *is* its truncation story). `context`: a `Coverage:` line over `count_matches` — `all N` vs `top N of M matching scrolls` — with the empty bundle's G1 form untouched. CLI + MCP twins share the builders, so both surfaces carry the honesty. Tests in `tests/test_works.py`, `tests/test_context.py`, `tests/test_mcp.py`; `docs/cli.md` G2 markers flipped; README updated. | → M2, cap 7 |
| H8 | **✓ shipped. M2 enforce on `doctor` — M2 complete.** The custody `drift` block now states what it verified: `basis` (`last_verify` — verdicts read from the ledger, not re-checked live this run), `as_of` (the freshest verdict timestamp the picture rests on, `null` when none), and `unverified` (held items the ledger has no verdict for — never checked, so unknown, **not** clean). A reader holding only the report can tell "confirmed unchanged at the last verify" from "never checked". Tests in `tests/test_doctor.py` (drift unverified/basis/as_of, no-ledger-table honesty); `docs/cli.md` G2 marker flipped — **G2 now enforced across every read surface.** Full suite green (2365). | → M2, cap 1/7 |
| H9 | **✓ shipped (with H10).** M3 design folded into the implementation: the budget tiers are specified in `docs/cli.md` (the `--budget` paragraph) and the `src/scrolls/context.py` module docstring — `index`/`connected`/`full`, strictly nested, identity/index first → deep bodies on demand (the obsidian L0–L3 adaptation). No ADR: the tier set is a small, reversible CLI surface documented in `cli.md`, the same way the `--stats` envelope and Coverage line were (no ADR for H6–H8). | → M3, cap 10 |
| H10 | **✓ shipped. M3 complete.** `scrolls context --budget {index,connected,full}` bounds bundle *depth* through nested tiers (`BUDGET_TIERS` in `context.py`): `index` is the catalog (Best Matches + Links, no graph build), `connected` adds `## Connected scrolls`, `full` (default) adds `## Excerpts` — the current bundle, unchanged. A tier below `full` carries a `_Budget:_` note disclosing what it held back (the depth-axis counterpart to the Coverage line's scope honesty; the two hold independently). Same-work collapse (ADR 0101) is index-level, so it holds at every tier. CLI + MCP twins share `build_context`. Tests in `tests/test_context.py` (10 new: tiering, default=full unchanged, budget note, coverage×budget independence, collapse×budget, empty-bundle/invalid-budget honesty) + `tests/test_mcp.py` (twin). `docs/cli.md`/`README.md` updated. Full suite green (2375). | → M3, cap 10 |
| H11 | **✓ executed in the 2026-06-16 run that shipped H9/H10.** Buffer refresh checkpoint (maintenance rule): M1, M2, and M3 are all shipped ahead of schedule (M3 was the Day-2 target). M4 broken into the concrete vertical slices H12–H15 below; M5 into H16–H17. 3-day/week plans re-derived. | maintenance |
| H12–H15 | **✓ shipped as one slice (ADR 0103). M4 complete.** `scrolls export bundle <query>` / `import bundle <path>`: one self-contained Markdown file that is both a readable topic *briefing* (per scroll: id, source, custody fidelity tier, capture timestamp, link, content-hash, capped excerpt) and a lossless re-import unit — the same `item_to_dict` JSONL `export items` writes, inside a ` ```jsonl ` fence wrapped in the ADR 0102 `@generated` sentinel (so the briefing body stays hand-annotatable). Scoped by the query + `context`/`search` facets and *complete about that scope* (every match, `count_matches` is the limit — no top-N), so it is the shareable, take-it-with-you complement to `export items`. New `src/scrolls/bundle.py` is an *envelope* reusing `search_items`/`count_matches`/`get_fidelity`/`dump_items_export`/`item_from_dict`/`generated.fence` — no new storage. `import bundle` reuses the `import items` path (`INSERT OR IGNORE`, custody-safe), so losslessness is the ADR 0082/0099 property already tested; the export→import→fresh-library round-trip is verified end to end. Tests: `tests/test_bundle.py` (11). Docs: `docs/cli.md` (two headings), README, `docs/architecture.md`, ADR 0103 + index. Done in one slice rather than the H12–H15 split because the round-trip core already existed — the bundle is an envelope, and the ADR lands with working code (repo convention). | → M4, cap 9 |
| H16–H17 | **✓ shipped as one slice. M5 complete → MVP M1–M5 done.** The one agent-runnable end-to-end dogfood flow — *hold a topic → prove custody (`doctor` score) → detect loss (`verify` drift recheck) → take it with me (`export bundle` → `import bundle`)* — runs offline against fixtures in `tests/test_dogfood.py` (4 tests: each leg + the full ordered flow). Done as one slice (the H12–H15 precedent) because every underlying surface already shipped, so M5 is a *proof* tying M1–M4 together, not new feature code. The flow's two live edges (capture, recheck) are stood in for offline: **hold** seeds rendered full-fidelity items via the production `write_scroll` path; **detect** injects a scripted re-capture at the one `cli.live_recapture` seam (as `test_verify_cli.py` does). Captured before/after narrated in new `docs/dogfood.md`; referenced from `docs/custody-vision.md` §8. The sharp result the proof makes visible: detecting source drift moves the *drift posture* (`unverified` → `drifted`, recorded) **without** lowering the integrity `score` (held at 100) — because raw is sacred and drift is a recorded event, never an overwrite (custody §2.4). | → M5, cap 11 |
| H18 | **✓ executed (with H16–H17).** Buffer refresh checkpoint (maintenance rule): MVP M1–M5 marked complete; 3-day/week plans re-derived below; post-MVP cap-8 enrichment + scheduled-custody-maintenance week broken into concrete slices H19–H24. | maintenance |
| H19 | **✓ shipped. Enrichment provenance audit (cap 8 — baseline).** Audited what the three engines already record: rules stamps `classified_by="rules-v1"`, the LLM classifier adds `classified_model`, kb_llm summaries store a `members_hash` + `engine` and skip unchanged concepts. The gap was not *recording* but a *tested cross-engine contract*: new `tests/test_enrichment_provenance.py` (9 tests) pins method-is-recorded, deterministic re-derivation (re-classify is a no-op in result; the marker is idempotent, not accreting), capture-chain preservation, and anti-fabrication — the cap-8 baseline, modeled on `test_completeness.py`. Baseline + the one known gap (rules records version, not precedence tier; deferred to H20) documented in `docs/architecture.md`. Network-free. | → cap 8 |
| H20 | **✓ shipped. Re-derivable classification records inputs + method (cap 8).** The rules engine now stamps, alongside `classified_by="rules-v1"`, which precedence tier fired (`classified_basis` — `curated-source`/`title-pattern`/`documentation-url`/`weak-source`, closing the H19 gap) and a fingerprint of the rule tables it ran under (`classified_ruleset` = `RULESET_FINGERPRINT`, a digest over the category maps + title patterns); the two together pin the ruleset so a reader can tell whether a re-classify today would still reproduce the stored category. `_category` now returns `(category, basis)`; both new fields are deterministic and idempotent (re-classify is a no-op in result — the H19 contract holds). Surfaced as a derived `classification` view (`items.classification_provenance` → `by`/`basis`/`ruleset`, `+model` for the LLM engine) carried identically by `scrolls list`, `show`, and the MCP `list_scrolls`/`get_scroll` twins; the key is omitted for unclassified/user-set items (honest absence, row shape stable). Tests: `tests/test_classify.py` (per-tier basis, fingerprint, deterministic re-derivation), `tests/test_enrichment_provenance.py` (cross-engine baseline extended), `tests/test_cli.py` + `tests/test_mcp.py` (surface echo, omission). Docs: `docs/cli.md` (classify/show/list), `docs/architecture.md` (enrichment bullet + downstream-engine section, H19 gap note closed). Full suite green (2412). | → cap 8 |
| H21 | **✓ shipped. Confidence / recency marker on the classification view (obsidian "confidence levels").** Every present `classification` view now carries a derived `confidence` marker (`items.classification_confidence`): `level` — `deterministic` for a rules match (reproducible from signals) vs `inferred` for an LLM category (a probabilistic judgment to weigh more cautiously), the "rule-matched vs llm-inferred" trust axis; and `freshness` — `current`/`stale`/`unknown` against the *live* ruleset for the rules engine, omitted for the LLM (no ruleset to compare, and a wall-clock timestamp would break the H19/H20 idempotence contract — so no freshness is fabricated, honest absence). "Freshness vs capture" is expressed as the *ruleset fingerprint* recency, the only idempotent recency signal we have. Because the view has one home (`items.classification_view`), the marker rides `list`/`show`/`search` + every MCP twin identically *for free* — the H26 parity. The freshness leg is a new single primitive `classify.classification_freshness` (`current`/`stale`/`unknown`/`None`); `is_stale_classification` and `doctor`'s `custody.enrichment` both refactored to delegate to it, so the per-item marker an agent reads can never disagree with the count doctor reports (`test_per_item_confidence_marker_converges_with_the_doctor_aggregate`). Facets parity needs no new dimension: the level axis aggregate *is* `facets method` (H28) and the freshness axis aggregate *is* `doctor custody.enrichment` (H25), both derived from the same view — a new facet would be redundant churn. Tests: `tests/test_classify.py` (freshness buckets, per-engine confidence, end-to-end), `tests/test_enrichment_provenance.py` (§6 cross-engine confidence contract + convergence), `tests/test_doctor.py` (marker↔aggregate convergence), `tests/test_cli.py`/`test_mcp.py`/`test_search.py` (the `confidence` key on every surface, stale recency travels to browse). Docs: `docs/cli.md` (classify/show/list), `docs/architecture.md` (enrichment bullet). Full suite green (2471). The summary-axis counterpart is H29/H31. | → cap 8 |
| H22–H23 | **✓ shipped as part of H34 (one slice).** The scheduled-maintenance *flow* (H22 draft) and the *custody-delta artifact* (H23) landed together inside the real `scrolls maintain` command rather than as a scripted-test precursor — the H12–H15 / H16–H17 precedent: every surface the flow composes (`verify`, `compile_kb`, `run_doctor`) already shipped, so the flow *is* the command and the delta artifact (`.maintenance/last-run.json`) *is* the durable record. The offline flow + delta computation are pinned in `tests/test_maintain.py`. | → cap 1, maintenance framing |
| H24 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; keep ≥6 un-started slots. Consider the deferred bi-temporal drift framing *only if* an agent workflow has shown the event record insufficient (else keep deferred per MVP out-of-scope). | maintenance |
| H25 | **✓ shipped. Stale-ruleset signal in `doctor` (makes H20's fingerprint actionable).** `run_doctor` now emits a `custody.enrichment` block (`src/scrolls/doctor.py` `_check_enrichment_provenance`): of the held rules-classified items, how many were classified under the live ruleset (`current`), under a *superseded* one (`stale`, with offending ids + fingerprints in `items` so a re-classify can be targeted), or before H20 with no fingerprint (`unfingerprinted` — unknown, not silently current, the drift block's `unverified` honesty). Report-only like drift: a stale fingerprint means the ruleset changed, not that the category is wrong, so it never feeds `issues`/the exit code, and doctor never auto-reclassifies (custody §2.4). LLM classifications are out of scope (a different re-derivability axis). Tests: `tests/test_doctor.py` (current/stale/unfingerprinted/llm-ignored, no-op-on-issues+exit, exact custody-block shape). Docs: `docs/cli.md` (doctor section), `docs/architecture.md` (enrichment bullet). Full suite green (2417). Ties the cap-8 fingerprint into the cap-1 health report and the H22 maintenance flow. | → cap 8, cap 1 |
| H26 | **✓ shipped. Classification view on `search` hits (search ≡ list ≡ show ≡ MCP parity).** H20's derived `classification` view now travels with ranked hits: `_QUERY` carries `items.provenance`, `_hit` derives the view per-hit (`items.classification_view`, the new raw-dict core `classification_provenance` now delegates to), and a shared `search.hit_payload` serializer renders the hit and drops `classification` when no engine stamped the category — the honest-absence shape `list`/`show` keep. Both `scrolls search` (CLI, incl. the `--stats` envelope's `results`) and MCP `search_scrolls` route through `hit_payload`, so the echo + omission read identically on every surface. Derived from each row's *own* provenance column, so it costs no extra query and leaves the G2 count/truncation untouched (scope-honest). Tests: `tests/test_search.py` (view carried, llm `model`, omission via `hit_payload`), `tests/test_cli.py` (echo, `--stats` echo, omission, **search ≡ list ≡ show** parity), `tests/test_mcp.py` (echo + omission). Docs: `docs/cli.md` (search hit keys + H20 note), `docs/architecture.md` (enrichment bullet). Full suite green (2427). The parity H21's confidence marker assumes is now in place. | → cap 7, cap 8 |
| H27 | **✓ shipped. `scrolls classify --stale` — act on the H25 signal (regenerate on request).** Re-runs the rules engine over exactly the items doctor's `custody.enrichment.stale` flags — categories produced under a superseded ruleset — refreshing both `category` and `classified_ruleset` to the live fingerprint. Selection is one shared predicate `classify.is_stale_classification` backing *both* doctor's stale report and the refresh, so the count doctor shows equals the count `--stale` acts on and the signal converges (`test_classify_stale_clears_the_doctor_stale_signal`). The explicit, user-invoked counterpart to H25's read-only report: regeneration *on request*, never doctor's silent overwrite (custody §2.4). Rules-only — rejects `--engine llm`/`--batch`/a single id; an item the live ruleset no longer matches falls to `unmatched` and is left untouched (surfaced, never wiped). **Precondition fix:** `overrides.apply_overrides` now drops the engine's category-derivation stamps when a user sets `category`, making `classification_view`'s documented "a user override carries no engine stamp" *true* — so a hand-set category never counts stale and is never refreshed (user overrides always win). Closes the loop H20 (record) → H25 (report) → H27 (refresh). Tests: `tests/test_classify.py` (predicate buckets), `tests/test_overrides.py` (stamp-clearing), `tests/test_cli.py` (refresh, current/user-set untouched, convergence, all three rejections, empty no-op). Docs: `docs/cli.md` (classify + doctor cross-ref), `docs/architecture.md` (enrichment + overrides bullets). Full suite green (2451). | → cap 8, maintenance framing |
| H28 | **✓ shipped. `scrolls facets method` — the facets leg of the classification-method parity.** A new `method` dimension in `compute_facets` (`facets.py` `_method_counts`/`_method_bucket`, modeled on `_fidelity_counts`): counts of *held* items by how their category was produced — `rules-v1` / `llm-v1` for an engine-classified item, `user-set` for a hand-set category with no `classified_by` stamp, `unclassified` for none. Built from the same `items.classification_view` the per-item view uses (the H26 refactor), so the aggregate and the per-item view never disagree; scope-honest (counts over the same `where`/`params` as every other facet) and reads only `category`+`provenance`, never bodies. Added to `FIELDS`, so the CLI `facets` choices and MCP `list_facets` passthrough pick it up for free. Completes the "search ≡ list ≡ MCP ≡ facets" parity (H21 names it) on the *aggregate* axis. Tests: `tests/test_facets.py` (buckets, scoping, empty shape), `tests/test_cli.py` + `tests/test_mcp.py` (echo + updated empty-shape). Docs: `docs/cli.md` (facets derived-dimensions paragraph), `docs/architecture.md` (enrichment bullet). Full suite green (2432). | → cap 8, cap 7 |
| H29 | **✓ shipped. LLM concept-summary provenance view + doctor stale-summary signal (the summary-axis counterpart of H20/H25).** `kb_llm` summaries already store `engine` + a `members_hash` fingerprint of the scrolls synthesized (the re-derivation key). Now surfaced as a derived `summary_provenance` view (`kb_llm.summary_provenance` over a stored `ConceptSummary` + the concept's live members → `by`/`members_hash`/`freshness`, `None` when never synthesized — the `classification_view`-returns-None honest-absence posture) and a `doctor` `custody.summaries` block (`doctor._check_summary_provenance`): of the summary-eligible concepts (≥ `MIN_MEMBERS` rendered members, the one shared `kb_llm.eligible_concepts` denominator both generators now delegate to), how many have a `current` summary (stored `members_hash` == live), a `stale` one (members changed since synthesis, or a superseded engine — slug + stored + `live_hash` listed in `items` so a re-synthesis can be targeted), or `never` (eligible but unsummarized — unknown, not silently current, the drift block's `unverified` honesty). Report-only like H25: a stale summary means the membership moved, not that the synthesis is wrong, so it never feeds `issues`/the exit code and doctor never auto-regenerates (custody §2.4). One shared `kb_llm.summary_freshness` primitive (`current`/`stale`/`never`) backs the view, the doctor aggregate, and (H31) the refresh, so the per-concept marker, doctor's counts, and the refresh pool can never disagree (`test_summaries_aggregate_converges_with_the_per_concept_view`). `kb_llm.is_stale_summary` is the H31 selector hook, mirroring `is_stale_classification`. Tests: `tests/test_kb_llm.py` (freshness/provenance buckets, eligibility-denominator parity with the generator), `tests/test_doctor.py` (current/stale/never/superseded-engine, no-eligible empty shape, the `eligible == summarized + never` invariant, no-op-on-issues+exit, convergence). Docs: `docs/cli.md` (doctor `summaries` paragraph), `docs/architecture.md` (enrichment bullet — summary axis). Closes the summary-axis *record/report*; H31 adds the refresh. | → cap 8, cap 1 |
| H30 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; mark shipped, prune overtaken slices, keep ≥6 un-started slots. Re-confirm the week plan still maps to `docs/product/mvp.md` / cap 8. | maintenance |
| H31 | **✓ shipped. `scrolls kb --stale` — refresh summaries whose members changed (the summary-axis counterpart of H27).** The explicit on-request regeneration that acts on H29's stale-summary signal: re-synthesizes exactly the concepts doctor flags in `custody.summaries.stale` (stored `members_hash` ≠ live members), refreshing the summary + `members_hash` to the live members, then compiles. A `stale_only` mode on both generators (`kb_llm.generate_concept_summaries[_batch]`) narrows the target set to `_summary_targets` filtered by the one shared `kb_llm.is_stale_summary` predicate doctor uses — so the count `--stale` regenerates equals doctor's stale count and refreshing clears the signal (`test_kb_stale_clears_the_doctor_stale_signal`). Unlike a full `--engine llm` run it leaves never-summarized eligible concepts (generation, not refresh) and skips orphan pruning; a current library is a network-free no-op (no targets → no model call). Implies the llm engine (rejects `--engine deterministic`, the H27 explicit-vs-default posture — `kb`'s `--engine` default flipped to `None`) and composes with `--batch` (both llm). Regeneration *on request*, never doctor's silent overwrite (custody §2.4) — closing the loop H20→H25→H27 mirror on the summary axis: H29 (record/report) → H31 (refresh). Tests: `tests/test_kb_llm.py` (stale_only targets only stale, never/current/orphan untouched, no-op, count==stale-set), `tests/test_kb.py` (CLI refresh, only-stale, never-left, no-op, deterministic rejection, doctor-signal-clears, `--batch` compose). Docs: `docs/cli.md` (kb `--stale`, doctor cross-ref), `docs/architecture.md` (enrichment bullet). | → cap 8, cap 1 |
| H32 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; mark shipped, prune overtaken slices, keep ≥6 un-started slots. Re-confirm the week plan still maps to `docs/product/mvp.md` / cap 8. | maintenance |
| H33 | **✓ shipped. Summary axis pinned into the cap-8 cross-engine invariant.** `tests/test_enrichment_provenance.py` stated the provenance-complete/re-derivable contract across the *classification* engines (sections 1–6) but only *referenced* the kb_llm summary axis. A new section 7 pins the summary engine into the same cross-engine contract: method recorded (`engine` + `members_hash`), deterministic-in-the-fingerprint re-derivation (recency is the members fingerprint, so unchanged members read `current`/no-op and changed ones `stale` — never a timestamp), honest absence (`summary_provenance(None, …)` is `None`, the `classification_view`-returns-None posture), and a freshness view that equals the one `kb_llm.summary_freshness` primitive doctor's `custody.summaries` and `kb --stale`/`is_stale_summary` read (convergence by construction). Network-free (stored `ConceptSummary` + primitives, no model call), so cap 8 now holds — tested — on *both* enrichment axes. The cap-8 enrichment theme (classification + summary: record → report → refresh → confidence/view → cross-engine invariant) is complete. | → cap 8 |
| H34 | **✓ shipped. `scrolls maintain` — the scheduled custody-maintenance pass as a durable command (obsidian "scheduled agents", custody-shaped).** One command runs *recheck* (`verify --all`, bounded by `--limit`, or `--no-recheck` for a fully offline pass — the one live edge, behind the `cli.live_recapture` seam so it scripts offline) → *regenerate views* (`compile_kb`, deterministic, never an LLM re-synthesis) → read-only *audit* (`run_doctor`, never `--fix`) → a **custody delta** (score / fidelity tiers / drift posture / `custody.enrichment`+`custody.summaries` stale counts) vs the snapshot the last run recorded at `<root>/.maintenance/last-run.json`. The new logic is the snapshot/delta layer (`src/scrolls/maintain.py`: `custody_snapshot` distils a doctor report; `compute_delta` diffs vs the baseline — `first_run` when none, missing-axis-reads-zero forward-compat, ADR 0082); everything it composes already ships. Report-only and idempotent (custody §2.4): records drift events and regenerates views, never repairs rows / reclassifies / re-summarizes — `doctor --fix` / `classify --stale` / `kb --stale` stay the explicit on-request mutations; the dot-prefixed snapshot is never a compiled page nor created by `init`, so a lost/corrupt one degrades to "first run". Exit mirrors `doctor` (nonzero only on structural `issues`; drift/stale are reported, never a failure). The delta makes the dogfood custody point *recurring*: source drift moves the drift posture without lowering the integrity score. Tests: `tests/test_maintain.py` (14 — pure delta/snapshot + the offline flow). Docs: `docs/cli.md` (`### scrolls maintain`), `README.md`, `docs/architecture.md` (Maintain subsystem + surfaces bullet), `docs/dogfood.md` (the recurring sibling). Full suite green (2516). | → cap 1, maintenance framing |
| H35 | **✓ shipped. Enrichment provenance travels in the shareable bundle's *briefing* (cap 9 + cap 8).** `export bundle` already carried full `provenance` losslessly in its embedded JSONL, but the readable briefing per scroll listed only id/source/fidelity/timestamp/hash/excerpt — *how the category was derived* was in the data, not the reading. Now each briefing entry carries the same derived `classification` view `list`/`show`/`search` carry (`bundle._classification_line` → `classified \`<cat>\` by \`<by>\` (<basis|model …>) · confidence <level>[, <freshness>]`), omitted on honest absence (unclassified / user-set, no engine stamp), and a `--concept`-scoped bundle carries that concept's synthesized LLM summary + its `summary_provenance` (`bundle._concept_summary_block` → engine + members-fingerprint freshness, computed against the concept's *whole* live membership, not the query-filtered subset). Both are *derived read views* outside the `@generated` JSONL fence, so the round-trip/lossless invariant is untouched — pinned by `test_classification_provenance_still_round_trips_in_the_block`. Tests: `tests/test_bundle.py` (7 new — rules/llm view, honest absence, round-trip preserved, concept summary current/stale/absent). Docs: `docs/cli.md` (export bundle), `docs/architecture.md` (export-bundle bullet), `bundle.py` module docstring. Full suite green (2525). "Provenance travels with every result" (custody-vision) now holds on the readable side too. | → cap 9, cap 8 |
| H36 | **✓ shipped. `scrolls maintain` run log — the custody *trend*, not just the last delta (cap 1, maintenance framing).** Each `maintain` pass now **appends** its `{recorded_at, snapshot, delta}` to an append-only `<root>/.maintenance/log.jsonl` alongside refreshing the `last-run.json` baseline (the custody-ledger posture: append, never rewrite), and `scrolls maintain --history [N]` prints the last N runs (default 10) as a JSON array, oldest first — the score/drift *trajectory* an unattended worker reads over time, not a single diff. Pure log read/append in `maintain.py` (`append_log_entry`/`read_log`, a JSONL sibling of `last-run.json` — no new storage shape); the history read is custody-safe like `load_snapshot` (missing log → empty history, one corrupt line skipped). `--history` is read-only and mutually exclusive with `--limit`/`--no-recheck` (those run a pass; `--history` reads one back). Folded into the M2 completeness invariant (`tests/test_completeness.py`: never-maintained and before-init both → honest `[]`). Tests: `tests/test_maintain.py` (8 new — append-only growth, corrupt-line skip, bounded N, oldest-first history, read-only-never-runs-a-pass, honest-empty, mutual exclusion). Docs: `docs/cli.md` (heading + history paragraph + example), `docs/architecture.md` (maintain bullets), `docs/dogfood.md`, README. Full suite green (2544). Precondition: H34 (shipped). | → cap 1, maintenance framing |
| H37 | **✓ shipped. Anti-fabrication / completeness invariant covers `maintain` (cap 7).** `maintain --no-recheck` (the audit-shaped, network-free invocation) is now pinned into the M2 cross-surface contract (`tests/test_completeness.py`): added to both `CHECKED_EMPTY_CASES` (a healthy library → zero-`issues` report, first-run delta) and `BEFORE_INIT_CASES` (no library → `score: null`, never a fabricated `100`, never an error — exit 0, empty stderr). The G1 surface list is now seven (the docstring + `docs/cli.md` "The completeness contract" updated to name `maintain`). Pins `maintain` into the same honest-absence invariant `search`/`context`/`related`/`works`/`doctor` satisfy — the first-run/missing-axis honesty proved in `test_maintain.py` is now also *the contract*. Full suite green. | → cap 7 |
| H38 | **✓ shipped. Custody headline on `scrolls status` (cap 1 + cap 2).** `status` now carries a one-line custody headline under its item counts (`custody`): integrity `score`, fidelity `tiers`, drift posture (checked/unverified/unchanged/drifted/rotted/error), and the stale enrichment/summary counts — "how custody stands" without parsing a full `doctor` report. Composed from the *same* `custody_snapshot(run_doctor(paths))` distillation `scrolls maintain` records (network-free), so `status`, `doctor`, and a maintenance snapshot can never disagree (the H21/H25 convergence-by-construction posture — reuses the existing `maintain.custody_snapshot` primitive, no new derivation). `run_doctor` guards a missing store itself, so before `init` the `score` is honestly `null` (no store, not a fabricated 100); an empty initialized library is trivially fully custodied (`100`, the "empty is healthy" posture doctor reports). CLI-only — `status` has no MCP twin. Tests: `tests/test_cli.py` (`test_status_custody_headline_converges_with_doctor` pins the headline + concrete fidelity-mix/drift posture and `== custody_snapshot(run_doctor)`; the two exact-shape `test_status_before_init`/`test_status_after_init` updated for the null-vs-100 split). Docs: `docs/cli.md` (status key table + paragraph + both console examples), `docs/architecture.md` (status bullet), README. Full suite green. | → cap 1, cap 2 |
| H39 | **✓ shipped. `export bundle --format html` — a browser-readable briefing (cap 9, the PRD's "Markdown/HTML briefings").** A new `--format {markdown,html}` flag (default `markdown`) on `export bundle`: `html` renders the *same* scope (every match, no cap) and the *same* per-scroll custody picture — fidelity tier, drift posture, classification provenance, the scope `custody_headline`, and a `--concept` bundle's synthesized summary — as a **self-contained, offline HTML briefing** (inline `<style>`, no scripts, nothing fetched from the network; `color-scheme` follows the reader's light/dark). All dynamic content is HTML-escaped through `html.escape`, so a tag-bearing title/body/raw_text can never inject markup (`test_bundle_html_escapes_dynamic_content` — the custody+security guarantee). **Export-only — not a re-import unit (custody honesty, the slice's load-bearing decision):** the canonical lossless round-trip stays a property of the **Markdown** form; the HTML embeds the *same* sentinel-fenced custody + custody-events JSONL (the shared `_items_block`/`_events_block`) in `<details>`/`<pre>` so the data is *present* for a reader, but `import bundle` consumes the Markdown bundle, and the briefing's `note` says so (no false round-trip claim). New `build_bundle_html` in `bundle.py`; the Markdown `build_bundle` and HTML form now share one `_gather_scope` (single scope resolution — markdown output byte-identical, the round-trip/docs-pinned examples unchanged) and the `custody_headline`/`drift_posture`/`classification_phrase`/`_concept_summary_view` primitives, so the two forms cannot desync (the HTML headline content == `custody_headline` sans markdown `_`; the classification phrase rendered via a tiny `_inline_code` that turns the controlled backtick spans into `<code>`). CLI: `_cmd_export_bundle` picks the builder by `--format`; both print raw to stdout (the artifact-is-the-output exception) and tolerate a missing library (valid empty document, no library created). New custody shape: a shareable briefing a human opens in a browser. Tests: `tests/test_bundle.py` (+11 — self-contained doc/no external CSS-JS, drift+classification render, embedded lossless block present, HTML escaping, headline==shared primitive, concept summary, empty scope, blank-query error, CLI `--format html`/default-markdown/explicit-markdown/before-init/blank-query). Docs: `docs/cli.md` (heading + `--format` paragraph), `docs/architecture.md` (export-bundle bullet — the two forms + shared primitives), README (cheatsheet), `bundle.py` module docstring. HTML validated live with `html.parser` (parses cleanly). Full suite green (2815). Precondition: H35 (the briefing provenance, shipped); H42 (the drift posture, shipped); H45 (the scope headline, shipped). | → cap 9 |
| H40 | **✓ shipped. `scrolls maintain` suggests the explicit repair, never runs it (cap 1, maintenance framing).** The `maintain` report now carries a **`suggested`** block: for each repairable finding the audit surfaces, the explicit on-request command that closes it (`{command, addresses}`, `addresses` naming exactly the present categories the command repairs). New pure `maintain.suggest_repairs(report)` maps the doctor report's finding categories to commands — the three structural fixes `doctor --fix` performs (duplicates / missing scrolls / out-of-sync FTS) **grouped into one** suggestion, `scrolls media` (missing media), `classify --stale` (stale enrichment), `kb --stale` (stale summaries) each their own — over a fixed command order. **The load-bearing decision (surfaced):** suggestions are **by finding, never by the aggregate `issues` count** — an orphan scroll bumps `issues`/the exit code but has **no** on-request repair (doctor never deletes a file it cannot prove it wrote, custody §2.4), so it yields *no* suggestion rather than pointing at a `doctor --fix` that would not remove it (`test_suggest_repairs_omits_orphan_scrolls_which_have_no_repair_command`, `test_maintain_suggests_nothing_for_an_orphan_despite_a_nonzero_exit`). A clean pass is the honest empty `"suggested": []` (G1). The block rides the **live pass only** — derived fresh from this pass's audit, not recorded in the snapshot/log — so `--history`/`--trend` (which replay snapshots) never carry a stale suggestion (`test_maintain_history_does_not_carry_suggestions`). Report-only and idempotent as before: maintain names the command, never runs it; the exit code is unchanged (still mirrors doctor's `issues`). Verified live (clean → `[]`; orphan → `issues:1`, `[]`, exit 1). Tests: `tests/test_maintain.py` (+12 — 7 pure mapping: clean-empty, grouped structural, only-present categories, media route, stale routes, orphan-omitted, deterministic order; 5 end-to-end: clean library, missing-scroll→`doctor --fix`-but-never-runs-it, orphan-no-suggestion, real stale classification→`classify --stale`, history-carries-none). Docs: `docs/cli.md` (`maintain` `suggested` paragraph + populated console example), `docs/architecture.md` (Maintain bullet — `suggest_repairs`, by-finding-not-issues), `maintain.py` docstrings. Full suite green (2827). Precondition: H34 (shipped). | → cap 1, maintenance framing |
| H41 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; mark shipped, prune overtaken slices, keep ≥6 un-started slots. Re-confirm the week plan still maps to `docs/product/mvp.md`. Consider the deferred bi-temporal drift framing only if an agent workflow has shown the event record insufficient (else keep deferred). | maintenance |
| H42 | **✓ shipped. Drift posture per scroll in the bundle briefing (cap 1 + cap 9).** Each briefing entry now names its *custody drift posture* — the verify-ledger verdict an agent reading a shared briefing most needs to weigh: `verified` (confirmed unchanged at the last verify, with `as of <checked_at>`), `unverified` (never re-checked — stated explicitly, never silently "clean"), `drifted`/`rotted`/`error`. Derived through a new shared primitive `custody.drift_posture(event)` over the same `latest_events` `doctor`'s `custody.drift` aggregates (one ledger read for the whole scope, no per-item query), so the per-scroll posture and doctor's counts can never disagree — pinned by `test_bundle_drift_posture_converges_with_the_doctor_aggregate` (postures total doctor's `unchanged`/`drifted`/`rotted`/`unverified`). Report-only and custody-safe: a drifted or rotted scroll is still carried losslessly in the `@generated` JSONL block (raw is sacred; drift is a *recorded posture*, never a drop) — `test_a_drifted_scroll_is_still_carried_losslessly`. The before-init/empty bundle skips the ledger read, staying valid. Tests: `tests/test_bundle.py` (3 new). Docs: `docs/cli.md`, `docs/architecture.md`, `bundle.py` docstring. Full suite green (2528). | → cap 1, cap 9 |
| H43 | **✓ shipped. Pinned `export bundle` into the M2 completeness invariant (cap 7).** `export bundle` is now folded into the cross-surface contract module `tests/test_completeness.py` (the way H37 folded `maintain`): `CHECKED_EMPTY_CASES` (a no-match query → a valid `No matching scrolls.` bundle, exit 0, empty stderr), `BEFORE_INIT_CASES` (no library → that same valid empty importable bundle, never a crash on a missing store), and `COULD_NOT_CHECK_CASES` (a blank query → loud `{"error"}` on stderr, exit 1, empty stdout). So the shareable artifact satisfies the *same* G1 anti-fabrication invariant — checked-and-empty ≠ could-not-check — every read surface does, asserted in one place. G1 surface list is now **eight** (docstring + `docs/cli.md` "The completeness contract" updated to name `export bundle`). Test + docs only; no production change. Full suite green (2531). | → cap 7 |
| H44 | **✓ shipped (with H62, one slice). Per-excerpt provenance tags on `scrolls context` excerpts at `full` budget (cap 8 + cap 10).** H35 put the `classification` view into the *shareable* bundle's briefing; the model-facing `scrolls context` bundle — the one an agent actually drops into its window — showed excerpts without *how the category was derived*. Now each excerpt carries a compact `_classified by \`<engine>\` (<basis\|model …>) · confidence <level>[, <freshness>]_` tag beneath its meta line — the same `classification_provenance` view `show`/`list`/`search` and the bundle briefing carry, rendered through a **new shared `items.classification_phrase`** so the method/confidence reads byte-identical across surfaces (the bundle's `_classification_line` was refactored to delegate to it, dropping its duplicated rendering). Omitted on honest absence (an unclassified or user-set item claims no method). Gated to the `full` budget tier (the H10 depth-honesty; a `full`-only deepening, like `## Excerpts`); reads off the *same* `latest_events` ledger the H47 scope headline already shares (one read). Tests: `tests/test_context.py` (+8 with H62 — classification tag at full, llm `model`, honest-absence omission, shared-phrase parity, absence below full). Docs: `docs/cli.md` (context per-source trust tags paragraph + console example), `docs/architecture.md` (context-bundle bullet), `context.py`/`items.py` docstrings. Full suite green (2621). Precondition: H10, H20 (shipped). | → cap 8, cap 10 |
| H45 | **✓ shipped. Scope custody headline at the top of the bundle briefing (cap 1 + cap 9).** A one-line headline under the title — `_Custody: N scroll(s) · fidelity <tier counts> · drift <posture counts>._` — summarising how custody stands across the *whole* bundle, so a reader gauges the scope without scanning every entry. New pure `bundle.custody_counts(items, verdicts)` tallies the *same* `get_fidelity` + `drift_posture` the per-scroll entries render (over the one `latest_events` ledger read `build_bundle` already does), so the headline totals equal the entries by construction and equal `doctor`'s `custody.tiers`/`custody.drift` for the same scope (the H42 convergence lifted to scope level — bundle `verified` ≡ doctor `unchanged`, `unverified` = held − verdicts). The renderer shows only non-zero tiers/postures in a fixed order (each section still sums to N); an empty scope is the honest `_Custody: 0 scroll(s)._`. The bundle-level counterpart of H38's `status` headline. Tests: `tests/test_bundle.py` (+3 — headline shape over a fidelity/drift mix, totals==entries==doctor convergence, empty-scope honesty). Docs: `docs/cli.md` (export bundle), `docs/architecture.md` (export-bundle bullet via module), `bundle.py` docstring. Full suite green (2559). Precondition: H35, H42 (shipped). | → cap 1, cap 9 |
| H46 | **✓ shipped. `scrolls maintain --history --trend` derived trend summary (cap 1, maintenance framing).** H36 prints the raw run window; a worker had to diff entries itself to answer "is custody improving, holding, or regressing?" `--trend` now wraps the window in a `{trend, runs}` envelope (the opt-in-envelope pattern of `search --stats`, so the bare `--history` array stays the default and the completeness `[]` never regresses) whose `maintain.compute_trend` distils the net `score` change first→last, the net drift movement (Δ`drifted`+Δ`rotted`), and a one-word `posture` — `regressing` if the score dropped *or* more scrolls drifted/rotted (integrity-first), `improving` if the score rose or drift cleared, else `holding`. Pure derivation over the `read_log` entries (no new storage); honest absence: <2 runs is `insufficient-history` with null deltas (a single point has no direction, the H21/H29 posture), and a `None`-score endpoint yields a null score `change`, never a fabricated zero. `--trend` only shapes a `--history` read — passed alone it is a usage error (exit 2), never a silently-ignored flag. Tests: `tests/test_maintain.py` (+11 — pure posture buckets incl. the steady-score/rising-drift regression, the `<2`-run absence, score-null endpoint, first→last span; CLI envelope/bare-array/requires-history). Docs: `docs/cli.md` (heading + trend paragraph + example), `docs/architecture.md`, README. Full suite green. Precondition: H36 (shipped). | → cap 1, maintenance framing |
| H47 | **✓ shipped. Scope custody headline on the `scrolls context` bundle (cap 1 + cap 10).** The model-facing bundle now carries a one-line `_Custody:_` headline — `N scroll(s) · fidelity <tier counts> · drift <posture counts>` over the in-bundle scrolls — alongside its `Coverage:` (scope, H7) and `_Budget:_` (depth, H10) lines, so an agent sees how much of what it is about to read is full-fidelity and how much has drifted. The custody-headline triad (`status` H38 → bundle briefing H45 → context bundle H47) is now driven by **one shared primitive**: the H45 tally/renderer was promoted from `bundle.py` into `custody.py` as `custody_counts(items, verdicts)` + `custody_headline(items, verdicts)` (with canonical `FIDELITY_TIERS`/`DRIFT_POSTURES` orders), so bundle and context emit a byte-identical line over the same `get_fidelity`/`drift_posture` and cannot diverge (this also avoided a `context → bundle` dependency). Gated to `connected`/`full` (like H44) so the leanest `index` tier stays a bare catalog; one `latest_events` ledger read per scope, skipped at `index`. Over an uncapped/uncollapsed whole-library scope the counts equal `doctor`'s `custody` aggregate (`verified` ≡ ledger `unchanged`). CLI + MCP twins share `build_context`, so the headline rides MCP for free. Tests: `tests/test_context.py` (+4 — headline shape over a fidelity/drift mix, index-gating, present-from-connected, doctor convergence); `tests/test_bundle.py` import moved to the new home. Docs: `docs/cli.md` (context budget paragraph + console example), `docs/architecture.md` (bundle bullet — the shared headline), `context.py`/`custody.py` docstrings. Full suite green (2563). Precondition: H38 (shipped), H42 (shipped), H45 (shipped). | → cap 1, cap 10 |
| H48 | **✓ shipped. `scrolls facets drift` — the drift-posture browse aggregate (cap 7 + cap 1).** A new `drift` dimension in `compute_facets` (`facets.py` `_drift_counts`, modeled on `_fidelity_counts`): counts of held items by custody drift posture (`verified`/`unverified`/`drifted`/`rotted`/`error`), read from the same `latest_events` ledger + `custody.drift_posture` primitive `doctor`'s `custody.drift` and the scope custody headlines (H45/H47) read — so the facet converges with all of them by construction (`unverified` = held − verdicts; `verified` ≡ ledger `unchanged`). Scope-honest (same `where`/`params` as every facet; reads only item ids + the ledger, never bodies); inserted into `FIELDS` between `fidelity` and `method` (the custody axes grouped), so the CLI `facets` choices and the MCP `list_facets` passthrough pick it up for free. Completes the "custody axes are all browsable" set (fidelity + method already were). Tests: `tests/test_facets.py` (buckets, scoping, empty shape, **convergence with `doctor.custody.drift`**), `tests/test_cli.py` + `tests/test_mcp.py` (echo + updated empty-shape). Docs: `docs/cli.md` (facets derived-dimensions paragraph + dimension list), `docs/architecture.md` (facets bullet). Full suite green. Precondition: none beyond the shipped ledger. | → cap 7, cap 1 |
| H49 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; mark shipped, prune overtaken slices, keep ≥6 un-started slots. Re-confirm the week plan still maps to `docs/product/mvp.md`. | maintenance |
| H50 | **✓ shipped. Custody-convergence cross-surface invariant (cap 1 + cap 7).** The custody picture (fidelity-tier + drift-posture counts) now rides `status` (H38), the shareable bundle briefing (H45), the `context` bundle (H47), `facets fidelity`/`drift` (H48), and `doctor`'s `custody` block — all deriving from one shared tally, but the convergence was pinned only per-surface. New `tests/test_custody_convergence.py` (the custody-side analogue of `tests/test_completeness.py`): over one seeded fidelity/drift fixture it asserts every surface reports the *same* tier and posture counts for the whole-library scope **and** under a shared `--source` filter, with the one documented vocabulary mapping (`verified` ≡ ledger `unchanged`) — so a change that desyncs any surface fails in one obvious place. Decision-grade: makes "custody reads the same everywhere" a tested contract validating the H47 shared-primitive refactor. Test + docs only (`docs/cli.md` "The completeness contract" gains the sibling-invariant paragraph); no production change. Full suite green. Precondition: H38 (shipped), H45 (shipped), H47 (shipped), H48 (shipped). | → cap 1, cap 7 |
| H51 | **✓ shipped. `scrolls verify --unverified` — recheck only the never-checked held items (cap 1, maintenance framing).** A third `verify` selection (beside a single id/URL and `--all`) that re-captures only the held, hash-bearing items the ledger has *no* verdict for — the `unverified` bucket `doctor`'s `custody.drift`, `facets drift`, and the scope custody headlines all report — made actionable, the verify-axis sibling of `classify --stale` (H27) / `kb --stale` (H31). The selection and the count it clears share **one** primitive: new `custody.unverified_items(items, verdicts)` (`held − verdicts`, order-preserving) backs both `scrolls verify --unverified` *and* `doctor`'s `custody.drift.unverified` (refactored to `len(unverified_items(items, latest))` — convergence by construction), so a re-check clears exactly the bucket those surfaces flag (`test_verify_unverified_clears_the_doctor_signal`). Restricted to hash-bearing items like `--all` — a reference-only capture has no baseline to diff a re-fetch against, so it honestly *stays* unverified (`test_verify_unverified_skips_reference_only_items`). Composes with `--limit` (oldest saved first); exactly-one-selection validation extended (rejects `--all --unverified`); the one live edge stays behind the `cli.live_recapture` seam, so the whole thing scripts offline. Tests: `tests/test_verify_cli.py` (+7 — selects only never-verified, clears the doctor signal, skips reference-only, `--limit` bound, empty/no-op, mutual exclusion), `tests/test_custody.py` (+3 — the predicate: held−verdicts, empty-ledger-is-all, order-preserving). Docs: `docs/cli.md` (verify heading + paragraph + console example), `docs/architecture.md` (facets/drift bullet — the predicate doctor delegates to + the actionability tie). Full suite green (2581). Precondition: the shipped ledger + drift facet (H48). | → cap 1, maintenance framing |
| H52 | **✓ shipped. Custody headline on the `scrolls graph` stats block (cap 1 + cap 2).** `graph`'s `stats` block now carries a `custody` member alongside `items`/`nodes`/`edges`/`clusters`: the same `custody.custody_counts` tally (fidelity-tier + drift-posture count maps) over the whole `stats.items` scope — the graph-surface member that was missing from the custody-headline family (`status` H38, bundle H45, context H47, facets H48). Counts the *whole library* the graph was resolved over, not just the connected nodes, so it is independent of `--all` (like `item_count`/`clusters`; `test_graph_stats_custody_is_independent_of_include_all`) and converges with `doctor`/`facets`/the scope headlines for the same scope by construction (`test_graph_custody_block_converges_with_doctor`, folded into `tests/test_custody_convergence.py` — now six surfaces). JSON, not a Markdown headline (graph emits JSON), so it carries the count maps directly (posture words, like `custody_counts`: `verified` ≡ ledger `unchanged`, `unverified` = held − verdicts). Implementation: the `Graph` dataclass now retains the in-scope `items` (not just `item_count`) so `to_payload` can tally over them; `to_payload(graph, verdicts)` reads `latest_events` via the CLI/MCP callers (kept pure — verdicts passed in), empty graph → honest zero counts. Tests: `tests/test_graph.py` (+3 — fidelity/drift mix, `--all`-independence, doctor convergence), updated exact-shape `stats` assertions in `test_graph.py`/`test_mcp.py`. Docs: `docs/cli.md` (graph `stats.custody` paragraph + example), `docs/architecture.md` (custody-headline family — the graph member), `mcp_server.py` docstring. Full suite green (2584). Precondition: H48 (shipped), H50 (shipped — extends its invariant). | → cap 1, cap 2 |
| H53 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked H54 + H56 shipped (the drift-posture-everywhere pair: browse filter + node-shape parity). Confirmed three earlier slices remain **un-started and still valid** — they were passed over while the custody-headline family (H45–H52) shipped as a coherent theme, not overtaken: **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H44** (classification provenance on `context` excerpts at `full` budget). Appended H58–H59 (per-item drift on the primary browse rows + its cross-surface invariant — the natural completion of the H42/H54/H56 drift-posture theme). ≥6 un-started work slots now: H39, H40, H44, H55, H58, H59. Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). | maintenance |
| H54 | **✓ shipped. `scrolls list --drift <posture>` — browse by custody drift posture (cap 7 + cap 1).** A `--drift {verified,unverified,drifted,rotted,error}` filter on `scrolls list` (+ the MCP `list_scrolls` twin) enumerates the held items at a given custody drift posture — the *which ones* to `facets drift`'s *how many*. The one filter that is **not** a stored column: applied after the SQL facets over a single `latest_events` read by a new shared selector `custody.items_in_posture(items, verdicts, posture)` — the read-side sibling of `unverified_items` (`unverified_items(i,v) == items_in_posture(i,v,"unverified")`) over the *same* `drift_posture`/`latest_events` `facets drift`, the scope headlines, and `doctor`'s `custody.drift` all read. So the rows `--drift X` returns total `facets drift`'s count for X over the same scope (drill-from-the-count convergence by construction); under `--stats`, `matched` is the post-drift count, so it equals the facet count, not the library total. A closed vocabulary (argparse `choices` on the CLI; `list_items` raises `ValueError` for the MCP twin — never a silent empty), and a valid posture nothing is in is the honest `[]`. The read-side companion of H48's aggregate and the read-side sibling of `verify --unverified`'s (H51) act-side selection. Tests: `tests/test_custody.py` (+4 — the predicate: per-posture selection, `==unverified_items`, order-preserving, honest-empty), `tests/test_cli.py` (+5 — per-posture rows, count==`facets drift`, honest-empty, `--stats` scope echo + post-drift `matched`, reject), `tests/test_mcp.py` (+2 — twin convergence, reject), and folded into the H50 custody-convergence invariant (`tests/test_custody_convergence.py` — the enumeration drilled from each posture's canonical count). Docs: `docs/cli.md` (list heading + drift paragraph + console example), `docs/architecture.md` (facets/drift bullet — the selector doctor's count and the filter share), README. Full suite green (2595). Precondition: the shipped ledger (H48). | → cap 7, cap 1 |
| H55 | **✓ shipped. `scrolls maintain` rechecks the unverified set first (cap 1, maintenance framing).** maintain's bounded **recheck** ran `verify --all` over the held items in `list_items` order, capped by `--limit` — so a `--limit`-bounded pass re-verified the same already-checked head every run and could never reach the never-checked tail, spinning without improving *coverage*. Now the held set is ordered through a new pure `custody.recheck_order(items, verdicts)` (the coverage-first sibling of the `unverified_items`/`items_in_posture`/`items_checked_before` selector family): the **never-checked** items first — exactly the `unverified_items` set, in input/oldest-saved order — then the **already-verified** oldest-verdict-first (ascending `checked_at`, a *stable* sort so a same-second tie keeps oldest-saved order, the `latest_events` tie posture). So a `--limit`-bounded maintenance pass spends its budget on *new* coverage until every held item carries a verdict, then revisits the stalest (a just-rechecked item gets a fresh `checked_at` and falls to the back), so successive bounded passes cycle the whole library — monotone coverage progress. The ordering only reorders (returns a permutation), so an **unbounded** pass checks the same set with the same counts; an **empty ledger** leaves input order untouched, so a fresh library's first pass behaves exactly as before. `_recheck_held_items` now reads `latest_events` once and threads it through `recheck_order` (the same ledger read every selector uses). Report-only and idempotent (custody §2.4); the live edge stays behind the `cli.live_recapture` seam, so the whole thing scripts offline. Verified live (`recheck_order` puts never-checked first then oldest-verdict-first; empty ledger unchanged). Tests: `tests/test_custody.py` (+7 — never-checked-first, verified-oldest-first, full ordering, head≡`unverified_items`, empty-ledger-unchanged, permutation, same-second tie-by-input-order), `tests/test_maintain.py` (+3 — a bounded pass skips the already-verified head for a never-checked item, successive bounded passes monotonically shrink `unverified` to 0, an unbounded pass checks the whole set regardless of a prior verdict). Docs: `docs/cli.md` (maintain recheck step — coverage-first ordering), `docs/architecture.md` (Maintain subsystem + surfaces bullet), `custody.py` docstring. Full suite green (2837). Precondition: H34 (maintain, shipped), H51 (the `unverified_items` primitive, shipped). | → cap 1, maintenance framing |
| H56 | **✓ shipped. Drift posture travels with the node shape — `related` hits + `graph` nodes (cap 7 + cap 1).** The shared node shape carried the custody *fidelity* tier (ADR 0100) but not the *drift* posture; now a `drift` field (the `custody.drift_posture` value — `verified`/`unverified`/`drifted`/`rotted`/`error`) rides both node-shape surfaces. `related` hits: a `drift` field on `RelatedHit`, populated in `scored_related` from one `latest_events` read per call (parallel to `fidelity`). `graph` nodes: added in `graph.to_payload` from the *same* `verdicts` the `stats.custody` tally already reads — so the per-node posture and its contribution to the scope count can never disagree, and graph building stays uncoupled from the ledger (the item-intrinsic `fidelity` stays on the `Node`; the ledger-derived `drift` is a payload enrichment, the H52 posture-in-payload split). Both read the same `drift_posture`/`latest_events` primitive the bundle briefing (H42), `facets drift` (H48), and `doctor`'s `custody.drift` use, so a node reads the same posture whichever surface reaches it (per-item parity — the counterpart of H52's scope-level convergence). MCP twins (`get_related_scrolls`, `get_link_graph`) carry it for free via the shared payloads. Tests: `tests/test_related.py` (+2 — posture per hit, honest `unverified` default, CLI echo), `tests/test_graph.py` (+2 — posture per node, `graph`≡`related` parity), `tests/test_mcp.py` (+1 — both twins carry `drift` and agree). Docs: `docs/cli.md` (related + graph node shape + examples; `works` rep shape clarified as the fidelity-bearing subset), `docs/architecture.md` (related + link-graph bullets). Full suite green (2600). Precondition: the shipped ledger; H42 (the posture primitive, shipped). | → cap 7, cap 1 |
| H57 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; mark shipped, prune overtaken slices, keep ≥6 un-started slots. Re-confirm the week plan still maps to `docs/product/mvp.md`. | maintenance |
| H58 | **✓ shipped. Drift posture on the primary browse rows — `list` rows + `search` hits (cap 7 + cap 1).** The per-item `drift` posture now rides `scrolls list`'s `item_summary` (→ `list` + MCP `list_scrolls`) and `scrolls search`'s `SearchHit`/`hit_payload` (→ `search` + MCP `search_scrolls`), beside the `fidelity` tier they already carried — so every browse/landing surface reports the *same* two-axis per-item custody picture: `fidelity` (how much is held) and `drift` (whether the source moved). `item_summary` gains a `drift` kwarg (default `"unverified"` = `drift_posture(None)`, the honest never-checked posture); `SearchHit` gains a `drift` field populated in `search_items`' existing annotation pass from one `latest_events` read per call (alongside `works`). Both callers read the ledger *once* and pass `drift_posture(verdicts.get(id))` per row (the way `works` membership is passed in), so the surface reads the ledger once, not per row. All four surfaces derive the posture through the same `custody.drift_posture` over `latest_events`, so a given item reads the same posture wherever an agent reaches it, and the posture a `list` row shows is exactly the one its `--drift` filter (H54) selects on. The completion of the per-item drift-everywhere parity H56 began. Tests: `tests/test_search.py` (posture per hit, honest default, primitive match), `tests/test_cli.py` (rows carry drift, row drift == `--drift` value, search echo, list≡search≡related≡graph parity), `tests/test_mcp.py` (both twins carry + agree), `tests/test_fidelity.py` (shape + passed posture); exact-shape assertions updated across cli/mcp/fidelity. Docs: `docs/cli.md` (list + search key lists, MCP tool table), `docs/architecture.md` (per-item drift everywhere). Full suite green (2608). Precondition: H56 (shipped). | → cap 7, cap 1 |
| H59 | **✓ shipped. Per-item drift-posture cross-surface invariant (cap 7 + cap 1).** The per-item counterpart of the H50 scope-level invariant, added to `tests/test_custody_convergence.py` (its natural home — that module already pins the scope-level counts). Over one ring-linked fixture spanning all four postures (`verified`/`drifted`/`rotted`/`unverified`), it asserts a given item reads the *same* `drift` on every surface that carries it — `list` rows, `search` hits, `related` hits (excluding its anchor), `graph` nodes, and the bundle briefing — and that each whole-library-enumerating surface's per-item posture counts total `facets drift`'s count for that posture (tying the per-item axis back to the aggregate). A `_bundle_postures` helper parses the briefing's per-scroll custody lines so the Markdown surface is checked alongside the JSON ones. Makes "the per-item custody picture reads the same everywhere" a tested contract. Test + docs only; module docstring broadened, `docs/cli.md` "A sibling invariant" section gains the per-item paragraph. Full suite green (2610). Precondition: H56 (shipped), H58 (shipped). | → cap 7, cap 1 |
| H60 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked H58 + H59 shipped — the per-item drift-posture theme (browse rows + cross-surface invariant) is **complete**; the per-item custody picture now reads identically on `list`/`search`/`related`/`graph`/bundle, pinned by H59. Three long-deferred slices remain **un-started and still valid** (not overtaken — passed over while the drift-posture theme shipped as a coherent block): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H44** (classification provenance on `context` excerpts at `full` budget). **H55** (`maintain` rechecks the unverified set first) is also un-started and valid. Appended **H61** (the per-item custody axes on the *inspect* surface — `show`/`get_scroll` carry `fidelity`+`drift`, the one per-item surface H58's parity skipped), **H62** (per-excerpt `drift` tag on `context` excerpts at `full` — the drift sibling of H44's classification tag), and **H63** (next refresh checkpoint). ≥6 un-started work slots now: H39, H40, H44, H55, H61, H62. Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). | maintenance |
| H61 | **✓ shipped. Per-item custody axes on the inspect surface — `scrolls show` + MCP `get_scroll` (cap 1 + cap 2).** H58 put the two per-item custody axes (`fidelity` + `drift`) on every *browse/landing* surface, but the *inspect* surface showed neither as a derived field — `_cmd_show`/`get_scroll` returned the full raw record plus the `classification` view, leaving a reader to re-derive the fidelity tier and with no drift posture at all (the ledger isn't in the item row). Now both payloads add a derived `fidelity` (via `get_fidelity`) and `drift` (via `custody.drift_posture` over a single-id `item_events` read — newest verdict, `unverified` when none), so the full-record view carries the same two-axis custody picture as `list`/`search` — completing the per-item parity to the one surface H58 skipped. Tests: `tests/test_cli.py` (both axes present, honest `unverified` default, drift matches the `list` row for the same item), `tests/test_mcp.py` (twin carries + agrees with `list_scrolls`), and folded into the H59 per-item invariant as the **sixth** surface (`tests/test_custody_convergence.py`). Docs: `docs/cli.md` (show section + MCP `get_scroll` row), `docs/architecture.md` (per-item drift everywhere — the inspect surface). Full suite green (2613). Precondition: H58 (shipped). | → cap 1, cap 2 |
| H62 | **✓ shipped (with H44, one slice). Per-excerpt drift posture on `context` excerpts at `full` budget (cap 1 + cap 10).** The custody-drift sibling of H44's classification tag: each `full`-tier excerpt now carries a compact `_drift \`<posture>\`_` tag beneath its meta line — the `custody.drift_posture` over the item's latest verify-ledger verdict (`verified`/`unverified`/`drifted`/`rotted`/`error`) — so an agent dropping the model-facing bundle into its window sees *per-source* whether each excerpt's source has moved, not just the scope-level `_Custody:_` headline (H47). Always shown, with `unverified` stated explicitly (never silently "clean", the drift block's honesty on the per-excerpt axis). Reads the same `latest_events` ledger the scope headline already shares (one read), through the same `custody.drift_posture` every other surface uses, so the per-excerpt posture and the scope count cannot disagree. Gated to `full` (like H44, the H10 depth-honesty — `index`/`connected` stay lean catalogs). Landed in one slice with H44 (the roadmap's "land the two per-excerpt tags coherently"): the classification tag omits on absence, the drift tag is always present, so a classified item shows both lines and an unclassified one shows only the drift line. Tests: `tests/test_context.py` (drift at full, drifted/unverified postures, ledger-primitive match, absence below full). Docs: shared with H44. Full suite green (2621). Precondition: H10, H47 (shipped). | → cap 1, cap 10 |
| H63 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked the per-excerpt `context` tag pair shipped (**H44** classification + **H62** drift, one slice — the shared `items.classification_phrase` extraction lands with them) and **H64** shipped — the **per-item drift-posture theme is now fully complete across all seven surfaces** (`list`/`search`/`related`/`graph`/bundle/`show`/`works`), pinned by the H59 per-item invariant. The **per-excerpt provenance theme** (context excerpts now carry classification + drift tags at `full`) is complete too. Three long-deferred slices remain **un-started and still valid** (not overtaken — passed over while the drift/provenance themes shipped as coherent blocks): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H55** (`maintain` rechecks the unverified set first). With the per-item + scope custody pictures both complete, the next custody-deepening horizon turns to **portability and the compiled/maintenance surfaces**: appended **H65** (per-item custody markers on the compiled `library/` KB list pages — the human-readable browse surface, the one place the per-item picture isn't), **H66** (`scrolls history <id>` — the per-item custody ledger timeline, the browsable form of the append-only events `verify` writes), **H67** (the verify-ledger custody events travel in the shareable bundle so custody itself is portable — cap 9 lossless round-trip), and **H68** (next refresh checkpoint). Bi-temporal drift framing stays deferred (no agent workflow has shown the event record insufficient). ≥6 un-started work slots now: H39, H40, H55, H65, H66, H67. Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). | maintenance |
| H64 | **✓ shipped. Drift posture on the `works` representation shape — `scrolls works` + MCP `get_works` (cap 1 + the works lens).** The per-item drift posture rode every browse/landing/inspect surface (H42/H56/H58/H61) except the one H56 deliberately scoped out — the `works.Representation` shape ("the fidelity-bearing subset"), which carried only `fidelity`. Now `works.to_payload` adds a `drift` field beside each representation's `fidelity`, populated from one `latest_events` read passed in by the CLI/MCP callers (the H56 graph-node split — the item-intrinsic `fidelity` stays on the `Representation` dataclass, the ledger-derived `drift` is the payload enrichment), so a reader of a multi-representation work sees not just which form is held in full but which have drifted ("prefer the canonical, but note it drifted"). Reads the same `custody.drift_posture`/`latest_events` every other surface does, so a representation's posture equals that item's `list` row by construction (`unverified` when never re-checked); the pure caller (no verdicts) reads `unverified`, honest. Folded into the H59 per-item invariant as the **seventh surface** (`tests/test_custody_convergence.py` — its own DOI-sharing seed, since the ring fixture forms no work). Tests: `tests/test_works.py` (+3 — posture per representation, unverified default, list-row agreement), `tests/test_mcp.py` (+1 — twin carries + agrees with `list_scrolls`), updated exact-shape assertion. Docs: `docs/cli.md` (works rep shape — no longer "the fidelity-bearing subset" — + both console examples), `docs/architecture.md` (works bullet + per-item-drift-everywhere section, now seven surfaces), `works.py`/`mcp_server.py` docstrings. Full suite green (2626). Precondition: H56 (the posture-on-payload pattern, shipped). | → cap 1, cap 7 |
| H65 | **→ renumbered to H89** (custody-headline/per-item theme block kept these rows contiguous). The compiled-`library/`-pages custody-marker slice is live as **H89** with current context (the per-item picture now also carries `last_checked`); this row is retired to avoid a duplicate work-item. No work lost — same intent, same preconditions. | → cap 1, cap 2 |
| H66 | **✓ shipped. `scrolls history <id>` — the per-item custody ledger timeline (cap 1, maintenance framing).** `scrolls verify` appends an append-only custody **event** per check, but no read surface showed the *full* history — `show`/`list` carry only the *latest* `drift` posture, `doctor`/`facets` only aggregate counts. A focused `scrolls history <id>` (id or URL, ADR 0028) now emits the item's complete ledger as JSON, newest first — each `{checked_at, status, prior_hash, observed_hash, detail}` (the verify-`results` row shape minus the redundant per-row `id`, since every event is the same item) — so an agent sees *when* a source drifted and *how often* it was re-checked, not just its current posture. The browsable form of the append-only ledger, the per-item counterpart of `maintain --history`'s scope-level trajectory. New CLI subcommand + MCP `get_scroll_history` twin, both routed through **one shared pair of primitives** `custody.event_payload`/`item_history` (so CLI and MCP read byte-identical), reusing `item_events`. **Honest empty-vs-error split (decision):** H66's draft text said "honest empty `[]` for a never-verified *or unknown* item", but the *tested* completeness contract treats an unknown id as a **loud could-not-check** everywhere (`show`/`related`/`works <ref>` all in `COULD_NOT_CHECK`), so `history` follows that for cross-surface consistency — a known-but-never-verified item is the empty `[]` (checked-and-empty, G1), an *unknown* ref is `{"error"}` on stderr + exit 1, so a typo can never masquerade as "no history". Folded into the M2 completeness invariant as the **ninth** surface (`tests/test_completeness.py`: checked-empty `[]`, unknown-id error, + the MCP twin). Tests: `tests/test_verify_cli.py` (6 — newest-first, 5-field shape, primitive convergence, never-verified `[]`, URL resolution, unknown-id error), `tests/test_mcp.py` (3 — twin newest-first/shape/primitive parity, never-verified `[]`, unknown raises; documented-tools set updated), `tests/test_custody.py` (4 — `event_payload` shape, `item_history` newest-first + serializer convergence, never-checked `[]`). Docs: `docs/cli.md` (`### scrolls history` heading + the completeness-contract surface list now nine + the MCP tool table), `docs/architecture.md` (the per-item drift section — latest posture everywhere → full timeline via `history`), README (cheatsheet + custody prose), `custody.py` docstrings. Full suite green (2642). Precondition: the shipped ledger. | → cap 1, cap 7 |
| H67 | **✓ shipped. The verify-ledger custody events travel in the shareable bundle — portable custody (cap 9).** `export bundle` carried each in-scope item losslessly and *read* its drift posture into the briefing (H42), but the **custody events themselves** — the append-only ledger proving when/how the source was checked — did not travel, so a recipient inherited the items without their drift *history* (only the exporter's last-seen posture, frozen in prose). Now the in-scope items' `custody_events` ride a **second sibling `@generated` JSONL region** (`custody.events_for_items`/`dump_events_export` → a new `_EVENTS_REGENERATED_BY` block), restored on `import bundle` through the new `custody.import_events`. The **idempotent restore rule**: dedup by the content 5-tuple `(item_id, checked_at, status, prior_hash, observed_hash)` — *not* the per-library autoincrement id (never exported), *not* `detail` (the 5-tuple identifies the check) — so a re-import is a custody no-op (the `INSERT OR IGNORE` posture on the event axis; within-batch dups also dedup). Inserts are sorted oldest-`checked_at` first, so a fresh target's restored ids run in time order and `latest_events` picks the chronologically-latest verdict — the imported posture equals the exporter's. The **items block stays the first region and byte-identical to `export items`** (round-trip untouched, ADR 0082/0099); a pre-H67 bundle with no second region imports items only (backward-compatible) — read via the new multi-region `generated.generated_bodies` (singular `generated_body`/`splice` unchanged, so the single-fence compiled pages are unaffected). The import report gains a nested `events: {imported, skipped}`. Verified end-to-end live (export A → import fresh B restores the ledger newest-first; re-import skips both events, ledger doesn't grow). Tests: `tests/test_custody.py` (+8 — export-dict shape/round-trip, `events_for_items` scoping/pre-v7 tolerance, fresh-restore, content-dedup no-op, id/detail-ignored, checked_at-ordered recency), `tests/test_bundle.py` (+6 — events block present + parse, cross-library round-trip with posture restored, re-import dedup, empty-events block, pre-H67 backward-compat, corrupt-events-block reported), `tests/test_generated.py` (+2 — `generated_bodies` multi-region + marker-less empty). Docs: `docs/cli.md` (export/import bundle — the events block + the dedup key + the `events` report key), `docs/architecture.md` (portable-custody bullet), README (cheatsheet + custody prose), `bundle.py`/`custody.py` docstrings. Full suite green (2662). Precondition: H42 (the posture in the briefing, shipped); the shipped ledger. | → cap 9, cap 1 |
| H68 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked **H67 shipped** — portable custody: the verify ledger now travels in the shareable bundle (a second `@generated` region) and restores deduped on import, so "lossless round-trip" holds from the item to its custody record. The per-item ledger theme (H66 `history` → H69 `--limit` → H70 head≡posture invariant → H67 portable) is a coherent, complete block. Long-deferred-but-valid slices remain **un-started and still valid** (passed over while the ledger/portability theme shipped, not overtaken): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H55** (`maintain` rechecks the unverified set first), **H65** (per-item custody markers on the compiled `library/` pages), **H71** (`history --since`). Appended **H72** (whole-library portable custody — the verify ledger also travels in `export items`/`import items` via a sibling `export events`/`import events`, the backup-path counterpart of H67), **H73** (a portable-custody round-trip invariant — the posture an item reads survives export→import, pinned in the convergence suite), and **H74** (next refresh checkpoint). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Then **H72 was taken and shipped in the same run** (whole-library portable custody — `export`/`import events` — the natural completion of the H67 bundle-scoped portability), leaving ≥6 un-started work slots: H39, H40, H55, H65, H71, H73. Week plan still maps to `docs/product/mvp.md` (custody-deepening + portability, no new adapters). | maintenance |
| H72 | **✓ shipped. Whole-library portable custody — `scrolls export events` / `import events` (cap 9).** H67 made custody portable for the *scoped shareable bundle*; the *whole-library backup* path still lost the ledger — a backup-and-restore of an entire library dropped every custody event. Now `scrolls export events [--source/--category/--tag]` streams the verify ledger as lossless JSONL (the custody sibling of `export items`: resolves the matching items via the same facets, then their events through `custody.events_for_items`/`dump_events_export`), and `scrolls import events <path>` restores them through the same idempotent `custody.import_events` H67 added — dedup by the content 5-tuple, never the per-library autoincrement id — so a full backup→restore carries custody, not just items, and a re-import is a custody no-op. Kept a **separate** stream (a new `src/scrolls/events_export.py`, mirroring `items_export.py`: JSONL framing + `load_events_export` validation, malformed-line rejection, unknown-key/forward-compat tolerance incl. the dropped `id`), so the `export items` JSONL stays byte-identical (the H67 first-region discipline, lifted to the backup path). Empty/pre-`init` → an empty document, never a crash (the `export items` posture, *not* folded into the M2 completeness invariant — `export events` is a raw stream like `export items`, which isn't either). Verified end-to-end live (export A → import fresh B restores the ledger newest-first; re-import skips all events). Tests: `tests/test_events_export.py` (11 — dump/load round-trip, item_id-carrying row shape, empty doc, blank-line skip, missing-file/malformed/non-object/missing-field errors, unknown-key+`id` tolerance), `tests/test_cli.py` (+7 — JSONL emit, fresh-library round-trip, idempotent re-import, source-facet scoping, empty/before-init empty doc, missing-file error). Docs: `docs/cli.md` (`### scrolls export events` + `### scrolls import events` — the docs-completeness guardrail `test_docs.py` enforces one heading per command), `docs/architecture.md` (the custody-sibling bullet), README (cheatsheet + backup prose). Full suite green (2681). Precondition: H67 (the event serializers + `import_events`, shipped). | → cap 9 |
| H73 | **✓ shipped. Portable-custody round-trip invariant — the posture survives export→import (cap 7 + cap 9).** H59/H70 pin that an item reads the same drift posture on every *read* surface and that `history`'s head implies it. H67/H72 make the ledger portable; the load-bearing property now pinned is that **the drift posture an item reads after an export→import is exactly the posture it read before** — custody travels losslessly, not just the item. Two round-trip cases in `tests/test_custody_convergence.py` (its natural home — already the per-item invariant's): the four-posture ring fixture is seeded in library A and round-tripped into a fresh B both ways — (1) `export bundle "topic"` → `import bundle` (H67 scoped portability) and (2) `export items` + `export events` → `import items`/`import events` (H72 whole-library portability) — and B is asserted to reproduce A's per-item posture on every surface (`list`/`search`/`show`/`history` head/re-exported bundle briefing) **and** A's `facets drift` aggregate. The bundle case also pins the import report (`imported: 4`, `events: {imported: 3, skipped: 0}` — web:4 never checked); the events case pins idempotent re-import (`skipped: 3`). Makes "custody itself round-trips" a tested contract — the portability analogue of the per-item convergence invariant. Test + docs only (module docstring + `docs/cli.md` "A sibling invariant" both broadened); no production change. Full suite green (2683). Precondition: H67 (shipped), H72 (shipped), H59 (shipped). | → cap 7, cap 9 |
| H74 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked **H67/H72/H73 shipped** — the **portable-custody theme is complete**: the verify ledger now travels both in the *scoped* shareable bundle (H67, a second `@generated` region) and as the *whole-library* `export events`/`import events` stream (H72), with the round-trip posture-preservation pinned as a cross-library invariant (H73). "Lossless round-trip is a guarantee" now holds for the item *and* its custody record. Un-started-and-still-valid slices (passed over while the ledger/portability theme shipped, not overtaken): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H55** (`maintain` rechecks the unverified set first), **H65** (per-item custody markers on the compiled `library/` pages), **H71** (`history --since`). Appended **H75** (incremental custody backup — `export events --since <ISO>`, the time-windowed sibling of H71/H72 for an incremental ledger backup). ≥6 un-started work slots now: H39, H40, H55, H65, H71, H75. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening + portability, no new adapters). | maintenance |
| H75 | **✓ shipped (with H71, one slice). Incremental custody backup — `scrolls export events --since <ISO>` (cap 9).** H72 exports the *whole* ledger; a maintenance worker backing up custody after each sweep re-exported everything every time. `--since <ISO>` now returns only the events `checked_at >= <ISO>` (the time-windowed sibling of `history --since`, H71, and the count-bounded `history --limit`, H69), so a worker appends only what is new since the last backup — and `import events`' content-dedup (H72) makes the union idempotent regardless of overlap (`test_export_events_since_union_reimports_idempotently` pins the full+overlapping-incremental → fresh-library no-op). Threaded as a `since=` keyword on `custody.events_for_items` (a pure post-id-scope filter; the bundle/H72 callers' default `None` keeps the untouched-window shape), so it composes with the `--source`/`--category`/`--tag` facets (window then scope). Landed in **one slice with H71** because both `--since` boundaries normalize through the *same* new shared primitive `custody.parse_since` (over `dates.to_utc_iso`, ADR 0024) — a `Z` suffix / different offset / date-only all normalize to the stored `+00:00` shape before the lexicographic `checked_at >=` compare, so the ordering is correct and not a string-shape accident, and validation comes for free: a malformed `--since` is a loud usage error (exit 2, the `maintain --trend` precedent), an empty window an empty document (the `export events` posture). Verified end-to-end live. Tests: `tests/test_custody.py` (parse_since normalize/reject, `events_for_items` since-window within id-scope), `tests/test_cli.py` (+5 — window cut, boundary inclusive, empty-window empty doc, malformed-since exit 2, composes-with-source, union-reimport-dedups). Docs: `docs/cli.md` (`export events` `--since` paragraph + example), `docs/architecture.md` (incremental-backup bullet), README. Precondition: H72 (shipped); paired with H71. | → cap 9 |
| H69 | **✓ shipped. `scrolls history <id> --limit N` — bound a long per-item ledger to the most recent N checks (cap 1 + cap 7).** A heavily-rechecked item (every `maintain` pass appends a verdict) accumulates a long custody ledger; `history` returned *all* of it. `--limit N` now returns only the most recent N events (newest first, oldest dropped) — the per-item counterpart of `maintain --history [N]`'s bounded window, **default unbounded** so the H66 shape is unchanged. **Design (decision):** the bare array stays the default and `--limit` just slices (no opt-in envelope) — `history` carries no `--stats` surface to protect and the count is implicit in the request, so a slice mirrors `list --limit` exactly; this keeps the completeness `[]` (limit 0 → honest empty, over-count → all) and the unknown-id error unchanged. Threaded as a keyword `limit=` through one primitive `custody.item_history` (a single `events[:limit]` slice) shared by the CLI and the MCP `get_scroll_history(item_id, limit=None)` twin, so both bound identically. Tests: `tests/test_custody.py` (most-recent-N, limit-0 `[]`, over-count, None≡unbounded), `tests/test_verify_cli.py` (CLI most-recent-N, limit-0 honest-empty, over-count≡full), `tests/test_mcp.py` (twin most-recent-N + limit-0 + unbounded default). Docs: `docs/cli.md` (heading + `--limit` paragraph + console example + MCP table sig), `docs/architecture.md`, README, `custody.py` docstring. Full suite green (2648). Precondition: H66 (shipped). | → cap 1, cap 7 |
| H70 | **✓ shipped. Fold `scrolls history` into the per-item custody-convergence invariant — the timeline head ≡ the posture everywhere (cap 1 + cap 7).** H59/H64 pin that a given item reads the *same* `drift` posture on every browse/inspect surface. `history` now exposes the *full* ledger; the load-bearing tie this pins is that **the posture the head of the ledger `history` returns implies — `drift_posture` of its newest event — equals the `drift` field every one of those surfaces shows for the same item** (and `[]` ⇒ `unverified`, the never-checked posture they all default to). Added as `test_history_head_agrees_with_the_per_item_drift_posture` in `tests/test_custody_convergence.py` (beside the H59/H64 per-item invariant), over the existing four-posture ring fixture: for each item, `history`'s head maps through the shared `drift_posture` to exactly the canonical posture `list`/`search`/`show`/… report, and the never-checked `web:4` is the honest `[]` ⇒ `unverified`. Makes "the ledger an agent reads back is the same ledger the postures summarize" a tested contract — the per-item-timeline analogue of H59. Test + docs only (module docstring + `docs/cli.md` "A sibling invariant" both broadened); no production change. Full suite green (2643). Precondition: H66 (shipped), H59 (shipped). | → cap 1, cap 7 |
| H71 | **✓ shipped (with H75, one slice). `scrolls history <id> --since <ISO>` — the per-item ledger window by time (cap 1 + cap 7).** The time-axis sibling of H69's count cap (`--limit N`): returns only the checks `checked_at >= <ISO>`, so a maintenance worker can ask "what has this item's source done *since the last sweep*" without reading (or capping by count of) the whole ledger. Composes with `--limit` **window then cap** (`--since` cuts the time range, `--limit` caps what remains). Threaded as a `since=` keyword on the shared `custody.item_history` primitive (a pure filter the CLI `_cmd_history` and the MCP `get_scroll_history(item_id, limit=None, since=None)` twin both pass through, so they window identically). The boundary normalizes through the new shared `custody.parse_since` (over `dates.to_utc_iso`, ADR 0024 — `Z`/offset/date-only all become the stored `+00:00` shape, so the lexicographic compare is apples-to-apples); honest empty `[]` when nothing falls in the window (checked-and-empty, never an error), and a malformed `--since` is a loud usage error (exit 2, the `maintain --trend` precedent) validated *before* the item lookup, so a typo'd boundary on an unknown item is a usage error not a missing-item one. Tests: `tests/test_custody.py` (item_history since-window / boundary-inclusive / compose-with-limit / empty-window), `tests/test_verify_cli.py` (+6 — window cut, date-only boundary, compose-with-limit, empty-window `[]`, malformed exit 2, malformed-beats-unknown-id), `tests/test_mcp.py` (+2 — twin windows like the CLI + primitive parity, malformed raises). Docs: `docs/cli.md` (`history` `--since` paragraph + example + MCP table sig), `docs/architecture.md` (per-item ledger bullet), README, `custody.py` docstrings. Full suite green (2702). Precondition: H66 (shipped); paired with H75. | → cap 1, cap 7 |
| H76 | **Buffer refresh checkpoint** (maintenance rule, executed 2026-06-16). Marked **H71 + H75 shipped** (one `--since` slice): the per-item ledger and the whole-library backup are now both **time-windowable** through the one shared `custody.parse_since` normalizer — `history <id> --since` answers "what has this source done since the last sweep" and `export events --since` is an incremental custody backup whose overlapping union re-imports idempotently. The **time-axis sibling of the count cap** (`--limit`, H69) is complete on both the read and the backup surfaces. Un-started-and-still-valid slices (passed over while the ledger/portability/windowing themes shipped, not overtaken): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H55** (`maintain` rechecks the unverified set first), **H65** (per-item custody markers on the compiled `library/` pages). Appended **H77** (the verdict-axis ledger filter `history --status <verdict>`, the third filter axis beside time and count), **H78** (an incremental-backup posture round-trip invariant — the union-is-idempotent property pinned as a cross-library convergence contract), and **H79** (the act-side `verify --stale-before <ISO>`, completing the `--since` family across read/backup/recheck) to keep ≥6 un-started work slots. **H77 and H78 were then taken and shipped in the same run** (the per-item ledger now has all three filter axes — count/time/verdict — and the incremental-backup union is pinned as lossless for custody), and **H80** (`verify --drift <posture>`, the recheck-side counterpart of `list --drift`) appended, leaving ≥6 un-started: H39, H40, H55, H65, H79, H80. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening + portability, no new adapters). | maintenance |
| H77 | **✓ shipped. `scrolls history <id> --status <verdict>` — the verdict-axis ledger filter (cap 1 + cap 7).** The third filter axis on the per-item ledger, beside the time window (`--since`, H71) and the count cap (`--limit`, H69): returns only the checks whose `status` is one of the closed set `unchanged`/`drifted`/`rotted`/`error` (the raw event verdict `history` emits, *not* the reader-facing drift posture — so `unchanged`, not `verified`) — "show me only the times this source actually *changed*", so an agent triaging a long ledger reads the drift/rot events without scanning the steady-state `unchanged` re-checks. A new `status=` keyword on the shared `custody.item_history`, applied **verdict → time → count** (verdict and time are AND filters, order immaterial; `limit` caps what remains), threaded through the CLI `_cmd_history` and the MCP `get_scroll_history` twin so both filter identically. Closed vocabulary validated *in the primitive* (`ValueError` on an unknown verdict — the `list_items`/`--drift` H54 pattern, so CLI and MCP inherit it; the CLI also guards via argparse `choices`, exit 2); a verdict nothing matches is the honest empty `[]`. Tests: `tests/test_custody.py` (per-verdict selection, verdict→window→cap compose, unknown-status reject, honest-empty, None≡whole), `tests/test_verify_cli.py` (+4 — per-verdict rows, composes with `--since`/`--limit`, no-match `[]`, closed-vocab argparse exit 2), `tests/test_mcp.py` (+2 — twin filters + primitive parity, unknown-status raises). Docs: `docs/cli.md` (`history` `--status` paragraph + example + MCP table sig), `docs/architecture.md` (the three filter axes), README, `custody.py` docstrings. Full suite green. The per-item ledger now has **all three filter axes** (count/time/verdict). Precondition: H66 (shipped); composes with H69/H71. | → cap 1, cap 7 |
| H78 | **✓ shipped. Incremental-backup posture round-trip invariant — the windowed union preserves custody (cap 7 + cap 9).** H73 pins that an item's drift posture survives a *whole-ledger* export→import; H75 adds the incremental (`--since`) backup whose overlapping union re-imports idempotently. Now pinned: **a full `export events` plus an overlapping `export events --since` incremental backup, restored as their union in one import into a fresh library, is as lossless for custody as the whole-ledger path** — the union dedups (no double-count) and the fresh library reproduces the source's per-item posture on every surface (`list`/`history` head) and its `facets drift` aggregate. Added to `tests/test_custody_convergence.py` (its natural home — already the per-item + H73 round-trip invariants') with a new **staggered-ledger** seed (an old + a newer verdict per item, so `--since` partitions with *real* overlap while the four postures stay) — `imported: 6, skipped: 3` over the 9-row union. **Decision (surfaced):** restored via a *single* import of the concatenated union, not two separate imports — `import_events` sorts each batch by `checked_at`, so a single union import keeps restored ids chronological and `latest_events`' `MAX(id)` resolves the newest verdict; importing an incremental *before* an older full as *separate* calls would give the older events higher ids and mis-resolve the posture (a pre-existing `latest_events` order-sensitivity, not introduced here; the natural "restore all my backup rows in one stream" path is order-safe). Test + docs only; no production change. Full suite green (2712). Precondition: H75 (shipped), H73 (shipped). | → cap 7, cap 9 |
| H79 | **✓ shipped. `scrolls verify --stale-before <ISO>` — staleness-bounded recheck (cap 1, maintenance framing).** The **act-side** `--since` sibling that completes the `--since` family across all three custody surfaces — the per-item *read* (`history --since`, H71) and the *backup* (`export events --since`, H75) already window by time; this windows the *recheck*. A fourth `verify` selection (beside a single id/URL, `--all`, and `--unverified`, H51) re-captures only the held, hash-bearing items whose newest ledger verdict **predates** the boundary — "re-verify everything not seen since the last sweep" — so a worker bounds work to what is *stale*, not the whole library. Reuses `latest_events` + the new shared `custody.items_checked_before(items, verdicts, boundary)` selector (the read-side sibling of `unverified_items`/`items_in_posture`; an item never checked is trivially stale, so a far-future boundary subsumes `--unverified`), the boundary normalized through the same `custody.parse_since` (so a `Z`/offset/date-only all work). **Boundary decision (surfaced):** the boundary is *exclusive* — an item last checked exactly *at* it is **fresh**, not stale — making `--stale-before B` the exact complement of the `checked_at >= B` window `history --since`/`export events --since` keep (the roadmap draft's prose said "predates", which this honors; its "boundary inclusive" test-bullet shorthand was the ambiguity resolved here in favor of the clean complement). Composes with `--limit` (oldest-saved first, so a bounded pass makes monotone coverage progress, the H55 ordering); the one live edge stays behind the `cli.live_recapture` seam (scripts offline); a malformed boundary is a loud usage error (exit 2, the `export events --since` precedent), validated before any item lookup. Tests: `tests/test_custody.py` (+5 — the predicate: strictly-before selection, at-boundary-is-fresh, never-checked-is-trivially-stale, future-boundary-subsumes-unverified, order-preserving), `tests/test_verify_cli.py` (+9 — selects only stale/never-checked, at-boundary fresh, future subsumes unverified, date-only boundary, composes with `--limit`, skips reference-only, empty library, malformed exit 2, mutual exclusion). Docs: `docs/cli.md` (verify heading + `--stale-before` paragraph + console example), `docs/architecture.md` (the `--since` family across read/backup/recheck), README (cheatsheet line + custody prose). Full suite green (2726). Precondition: the shipped ledger; H51 (`--unverified`); pairs with H55. | → cap 1, maintenance framing |
| H80 | **✓ shipped. `scrolls verify --drift <posture>` — recheck the items at a given posture (cap 1, maintenance framing).** The recheck-side counterpart of `list --drift` (H54, the read enumeration) and the verify-axis sibling of `--unverified` (H51): a fifth `verify` selection re-capturing only the held, hash-bearing items currently at a chosen drift posture — `--drift drifted` to re-confirm whether a changed source moved again or settled, `--drift error` to retry a transient failure, `--drift rotted` to confirm a 404 is permanent. "Re-check the ones worth re-checking", so a maintenance worker targets the suspect set instead of `--all`. Reuses the shared `custody.items_in_posture` selector (the same one `list --drift` enumerates and `facets drift` counts), so the set rechecked equals the rows `list --drift X` shows (`test_verify_drift_rechecks_exactly_the_list_drift_rows`) and a recheck moves the posture it targeted (`test_verify_drift_recheck_moves_the_posture`); a closed vocabulary (argparse `choices`, like `list --drift` — typo → exit 2), restricted to hash-bearing items like `--all`/`--unverified`/`--stale-before` (a reference-only item has no baseline). Composes with `--limit` (oldest saved first); mutually exclusive with the other selections; the live edge stays behind the `cli.live_recapture` seam. Tests: `tests/test_verify_cli.py` (+8 — selects only the posture's items, ≡ `list --drift` rows, recheck moves the posture, composes with `--limit`, skips reference-only, empty-posture no-op, closed-vocab reject, mutual exclusion). Docs: `docs/cli.md` (verify heading + `--drift` paragraph + console example), `docs/architecture.md` (the one selector trio behind both the read enumeration and the act-side recheck), README (cheatsheet + custody prose). With H80 the verify command carries **all five selections** (id, `--all`, `--unverified`, `--stale-before`, `--drift`) over one trio of `custody` selectors. Full suite green (2735). Precondition: the shipped ledger; H54 (`list --drift`), H51 (`--unverified`). | → cap 1, maintenance framing |
| H81 | **✓ shipped. Verify-selection family convergence invariant — the batch selections relate as documented (cap 1 + cap 7).** With `verify` now carrying four batch selections (`--all`, `--unverified` H51, `--stale-before` H79, and `--drift` H80) over one shared trio of `custody` selectors (`unverified_items` / `items_checked_before` / `items_in_posture`), the documented relationships are pinned as a tested contract — the verify-axis sibling of the read-side `tests/test_custody_convergence.py` invariants (H59/H73/H78), in that module. Over the four-posture ring fixture: (1) `verify --drift <posture>` re-captures exactly the rows `list --drift <posture>` enumerates for every posture (the act-side ≡ read-side drill, by the shared `items_in_posture` — snapshotting each `list --drift` set before any verify mutates the ledger); (2) `verify --stale-before <future>` re-checks a *superset* of `--unverified` (every never-checked item is trivially stale) and clears the `doctor` `custody.drift.unverified` signal it subsumes; (3) every batch selection (`--all`/`--unverified`/`--stale-before`/`--drift`) skips reference-only items identically (no baseline hash). Makes "the verify selections are the actionable face of the custody read surfaces" a tested guarantee. Test + docs only (the convergence module's docstring + `docs/cli.md` "A sibling invariant" both broadened); no production change. Full suite green (2738). Precondition: H79 (shipped), H80 (shipped). | → cap 1, cap 7 |
| H82 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked **H79 + H80 + H81 shipped** (one run — the act-side `--since` window completing the family across read/backup/recheck; the posture-targeted recheck completing the **verify-selection family** so `verify` carries all five selections — id, `--all`, `--unverified`, `--stale-before`, `--drift` — over the one trio of `custody` selectors `unverified_items`/`items_checked_before`/`items_in_posture`; and the convergence invariant pinning that the selections relate as documented). The verify-selection theme is **complete and pinned**. Un-started-and-still-valid slices (passed over while the verify-selection theme shipped, not overtaken): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H55** (`maintain` rechecks the unverified set first), **H65** (per-item custody markers on the compiled `library/` pages). Appended **H83** (maintain bounds its recheck to the stale set — the recurring, scheduled use of H79) and **H84** (per-item `last_checked` timestamp on the browse rows — the time axis of the per-item custody picture, beside the drift posture) to keep ≥6 un-started slots. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening + portability, no new adapters). ≥6 un-started work slots now: H39, H40, H55, H65, H83, H84. | maintenance |
| H83 | **✓ shipped. `scrolls maintain` bounds its recheck to the stale set (cap 1, maintenance framing).** maintain's scheduled recheck ran `verify --all` capped by `--limit` — re-verifying *everything* every pass (fresh items before stale ones). Now the recheck is **stale-bounded by default**: it reads the timestamp of its *own* last recorded run (`<root>/.maintenance/last-run.json`'s `recorded_at`, the same `previous` snapshot the delta already loads — now loaded once, up front) and re-verifies only the held, hash-bearing items *not seen since then* — `custody.items_checked_before` windowed by that boundary, so a scheduled pass does the **new work, not the whole library**. The boundary is derived by a new pure `maintain.last_run_boundary(previous)` (normalizes the `recorded_at` through the shared `custody.parse_since`, so the ledger compare is the exact `+00:00`-shape complement of the `history --since` / `export events --since` `>=` window; **degrades to `None` = recheck-all** on a missing/blank/corrupt `recorded_at` — the snapshot's degrade-safely posture on the boundary axis). The stale set is still ordered **coverage-first** through `recheck_order` (H55), so a `--limit`-bounded pass still prioritizes never-checked items and advances coverage — H83 generalizes the H55 bound from "the whole held set" to "the stale set since last run". **`--all` recovers the pre-H83 behavior** (ignore the boundary, recheck every held item), composes with `--limit` (`--all --limit N` == the old `--limit N`), and conflicts with `--no-recheck` / `--history` (a recheck-scope flag can't skip the recheck or shape a read-only history — rejected exit 2, the `--trend requires --history` precedent, not silently ignored); a **first run** (no baseline) has no boundary, so it also rechecks everything (the never-checked-is-trivially-stale property makes the first-run full recheck a natural special case). The recheck report gains a self-describing `scope` (`stale`/`all`/`null` when skipped) + `since` (the boundary, `null` for all/skipped) — live-pass only, like `suggested` (not recorded in the snapshot/log, so `--history` carries none). Report-only and idempotent as before; the live edge stays behind the `cli.live_recapture` seam (scripts offline). Verified live (the `--all` conflicts exit 2; the skipped report carries `scope:null`). Tests: `tests/test_maintain.py` (+10 — 3 pure `last_run_boundary`: normalize/no-baseline/degrade; 2 direct `_recheck_held_items`: stale-set-only within a boundary, none-boundary-rechecks-all; 5 end-to-end: first-run-all-then-default-skips-the-just-seen, `--all` re-rechecks all, `--all --limit` composes, the two conflict errors; the two drift-on-second-pass tests updated to `--all` so they still force the recheck). Docs: `docs/cli.md` (maintain heading + recheck step — stale default, `--all`, `scope`/`since`), `docs/architecture.md` (Maintain subsystem + surfaces bullet), README (cheatsheet + `--all` line), `maintain.py` docstrings. Full suite green (2847). Precondition: H34 (maintain, shipped), H79 (the `items_checked_before` selector + `parse_since`, shipped); composes with H55. | → cap 1, maintenance framing |
| H84 | **✓ shipped. Per-item `last_checked` timestamp on the browse rows (cap 1 + cap 2).** The per-item custody picture rode every surface as two axes — `fidelity` (how much is held) and `drift` (whether the source moved) — but not *when* the drift verdict was taken. Now a derived `last_checked` field (the `checked_at` of the item's latest ledger verdict, `null` when never re-checked — the honest-absence null counterpart of the `unverified` posture) rides beside `drift` on the primary browse/landing/inspect rows (`list` rows + MCP `list_scrolls`, `search` hits + `search_scrolls`, `show` + `get_scroll`), through a **new shared `custody.last_checked(event)` primitive** — the time-axis sibling of `drift_posture(event)` over the *same* `latest_events` verdict, so the caller reads the ledger once (the way `drift`/`works` are passed into `item_summary`/`SearchHit`) and the timestamp and posture can never come from different reads. So an agent reads not just whether a source moved but *as of when*, and can pick a `verify --stale-before <ISO>` (H79) boundary straight from a row. Deterministic (a stored timestamp reported verbatim, never wall-clock-relative — the H21 idempotence posture). Folded into the per-item convergence invariant as the time-axis counterpart of the H70 posture tie: the timestamp every latest-posture surface shows equals the `checked_at` of that item's `history` head, and `null` ⇔ the empty timeline ⇔ never re-checked (`test_last_checked_agrees_with_the_history_head_checked_at`). Tests: `tests/test_custody.py` (the primitive — timestamp / `None`), `tests/test_fidelity.py` (`item_summary` carries it + default null), `tests/test_search.py` (`SearchHit` populated from the ledger, primitive match, honest absence), `tests/test_cli.py` (`show` carries it, list≡search≡show parity), `tests/test_mcp.py` (all three twins agree), `tests/test_custody_convergence.py` (the history-head tie). Docs: `docs/cli.md` (list/search/show key lists + MCP table + the sibling-invariant paragraph), `docs/architecture.md` (per-item custody — the time axis), README. Full suite green (2746). Precondition: H58 (the per-item drift field, shipped); H66 (`history`, shipped). | → cap 1, cap 2 |
| H85 | **✓ shipped. `scrolls list --stale-before <ISO>` — browse the stale set (cap 7 + cap 1).** With `last_checked` now on every browse row (H84), the next read affordance is to *filter* by it: a `--stale-before <ISO>` flag on `scrolls list` (+ the MCP `list_scrolls` twin) enumerating the held items whose newest verdict predates the boundary — the **read-side sibling of `verify --stale-before`** (H79, the act-side recheck), exactly as `list --drift` (H54) is the read-side sibling of `verify --drift` (H80). Reuses the one shared `custody.items_checked_before` selector `verify --stale-before` acts on (so the rows `list --stale-before B` shows are *exactly* the set `verify --stale-before B` would re-check — drill-from-the-window convergence, pinned in `tests/test_custody_convergence.py`) and the same `custody.parse_since` boundary normalizer. Applied after the SQL filters over *one* `latest_events` read shared with `--drift` (both ledger filters compose — AND — at no extra query); the boundary is **exclusive** (an item checked exactly at it is fresh, the complement of the `history --since`/`export events --since` `>=` window) and a never-checked item is trivially stale (included). A malformed boundary is a loud usage error (exit 2 at the CLI — validated *before* the store read, so a typo never reads as a checked-and-empty `[]`; the MCP twin surfaces the `ValueError`), the `--since` family precedent. Composes with the stored facets (window then scope) and `--stats` (post-filter `matched`); each returned row's own `last_checked` (H84) shows *why* it is stale. The read-enumeration completion of the `--stale-before` family (read/backup/recheck/enumerate). Tests: `tests/test_cli.py` (window cut, exclusive boundary, date-only, composes with facets/`--stats`/`--drift`, malformed exit 2, malformed-beats-missing-store), `tests/test_mcp.py` (twin selects the stale set, malformed raises), `tests/test_custody_convergence.py` (`list --stale-before` rows ≡ `verify --stale-before` recheck set). Docs: `docs/cli.md` (list heading + `--stale-before` paragraph + MCP table), `docs/architecture.md` (the read-enumeration of the stale set), README. Full suite green (2754). Precondition: H79 (the selector, shipped); H84 (the row timestamp, shipped). | → cap 7, cap 1 |
| H86 | **✓ shipped. Per-item `last_checked` rides the node-shape surfaces — `related` hits + `graph` nodes (cap 7 + cap 1).** H84 put the time axis (`last_checked`) on the primary browse/inspect rows (`list`/`search`/`show`); the node-shape surfaces carried the `drift` posture (H56) but not *when* it was taken. Now a `last_checked` field rides beside `drift` on `RelatedHit` (`related.py`) and the `graph` node payload (`graph.py` `to_payload`), populated from the *same* `latest_events` read those surfaces already do for `drift` through the shared `custody.last_checked(verdicts.get(id))` — the H56 payload-enrichment split lifted to the time axis (the item-intrinsic `fidelity` stays on the dataclass; the ledger-derived `drift`+`last_checked` are payload enrichments, so graph building stays uncoupled from the ledger). So an agent reaching an item by relation or graph traversal sees staleness too, at parity with the browse rows — the node-shape completion of H84, mirroring how H56 extended `drift` after H58 put it on the browse rows. MCP twins (`get_related_scrolls` via `dataclasses.asdict`, `get_link_graph` via the shared payload) carry it for free; the CLI renderers (`_related_rows`/`graph_payload`) likewise. Folded into the per-item convergence invariant beside the H84 list/search/show tie (`graph` nodes + `related` hits read the same `checked_at` as the `history` head). Tests: `tests/test_related.py` (+2 — timestamp per hit, honest `null`, primitive match), `tests/test_graph.py` (+2 — timestamp per node, honest `null`, `graph`≡`related`≡`list` parity), `tests/test_mcp.py` (+1 — both twins carry + agree with `list_scrolls`), `tests/test_custody_convergence.py` (the H84 tie broadened to the node surfaces). Verified live (related/graph emit the timestamp; never-checked → `null`). Docs: `docs/cli.md` (related + graph node shape + examples + MCP table rows — also brought the `get_related_scrolls`/`get_link_graph` rows current to `drift`, stale since H56 — + the convergence-invariant prose), `docs/architecture.md` (per-item time axis now on node surfaces + related/graph bullets), README (custody prose). Full suite green (2759). Precondition: H84 (the primitive + browse-row precedent, shipped); H56 (the node-shape drift, shipped). | → cap 7, cap 1 |
| H87 | **✓ shipped. Per-item `last_checked` on the `works` representation shape — `scrolls works` + MCP `get_works` (cap 7 + cap 1).** H86 completed the time axis (`last_checked`) on the node-shape surfaces (`related`/`graph`), leaving the one per-item surface H64 added `drift` to but H84/H86 skipped: the `works.Representation` shape, which carried `fidelity`+`drift` but not *when* the verdict was taken. Now a `last_checked` field rides beside each representation's `drift` in `works.to_payload`, populated from the *same* `latest_events` read the CLI/MCP callers already pass in for `drift` through the shared `custody.last_checked` (the H64/H56 payload-enrichment split — the item-intrinsic `fidelity` on the `Representation` dataclass, the ledger-derived `drift`+`last_checked` as payload enrichments, so the dataclass stays ledger-free). So a reader of a multi-representation work sees not just which form is held in full and which drifted but *as of when* — **completing the time-axis parity to all seven per-item surfaces** (`list`/`search`/`show`/`related`/`graph`/bundle/`works`), exactly as H64 completed the drift axis. MCP `get_works` twin carries it for free via the shared payload. Folded into the per-item convergence invariant's `works` seed (`test_works_representation_agrees_on_an_items_drift_posture` extended with the timestamp parity: each representation's `last_checked` equals its `list` row, drifted preprint carries the verdict's timestamp, never-checked record `null`). Tests: `tests/test_works.py` (+3 — payload timestamp, honest-`null` default, CLI list-row agreement; exact-shape rep assertion updated), `tests/test_mcp.py` (twin parity extended), `tests/test_custody_convergence.py` (the works seed's time sibling). Docs: `docs/cli.md` (works rep shape + both examples + MCP table row), `docs/architecture.md` (per-item time axis now all seven surfaces + works bullet), `works.py` docstrings. Full suite green. Precondition: H86 (the node-shape precedent, shipped); H64 (the works drift split, shipped). | → cap 7, cap 1 |
| H88 | **✓ shipped. Consolidated per-item `last_checked` cross-surface invariant — the time-axis analogue of H59 (cap 7 + cap 1).** With `last_checked` on all seven per-item surfaces (`list`/`search`/`show` H84, `related`/`graph` H86, `works` H87, and the bundle briefing's `as of <checked_at>` line H42), the time-axis parity was pinned piecemeal across several tests. Now `test_every_surface_agrees_on_an_items_last_checked` in `tests/test_custody_convergence.py` — the exact mirror of the H59 `test_every_surface_agrees_on_an_items_drift_posture` — asserts over the one ring fixture that a given item reads the *same* `last_checked` on `list` rows / `search` hits / `graph` nodes / `related` hits / `show` *and* the bundle briefing (a new `_bundle_last_checked` helper parses the `as of <date>` / "never re-checked" prose the way `_bundle_postures` parses the posture), with `null` ⇔ the briefing's never-checked ⇔ the JSON null. The `works` representation's timestamp parity stays in its own seed (the ring forms no work), extended in H87. Makes "the staleness an agent reads is the same everywhere" one tested contract — the time-axis capstone of the H84→H87 theme. Test + docs only (`docs/cli.md` per-item-invariant prose gains the time-axis-mirror paragraph); no production change. Full suite green. Precondition: H87 (the last surface, shipped); H59 (the pattern mirrored, shipped). | → cap 7, cap 1 |
| H89 | **✓ shipped. Per-item custody markers on the compiled `library/` KB list pages (cap 1 + cap 2) — H65 brought current.** The per-item custody picture rode every agent-facing JSON surface *and* the bundle/context Markdown, but the **compiled human-readable `library/` list pages** the KB compiler writes still rendered each item by title/link alone. Now each per-item row on the source/category/concept/tag list pages trails a compact `· <fidelity> · <drift>` marker (`· full · drifted` / `· reference · unverified`) — the same two-axis picture `list`/`search` carry, via a new `kb._custody_marker` over `items.get_fidelity` + `custody.drift_posture`, the never-checked posture honestly `unverified` (never silently "clean"). `compile_kb` reads `latest_events` **once** for the whole compile (the bundle/context pattern) and threads the `verdicts` dict through `_write_page`/`_consolidated_body` to `_item_line` (a new `custody=` arg trailing the existing note, so the note is preserved, not replaced); the consolidated-work nested representation bullets carry it too. **Scope decision (surfaced):** the marker is on the *per-item* rows of the four group list pages only — the bold work heading and the `index`/`graph`/`works` rollup rows carry none (they are not per-item rows; the index `Recent` teaser stays a bare row), pinned by `test_kb_index_and_graph_pages_omit_the_custody_marker`. Refresh-safe: the marker is part of the page body, so it renders *inside* the `@generated` fence (ADR 0102) — a recompile refreshes it (e.g. after a re-verify moves the posture) without touching a hand annotation outside the block (`test_kb_custody_markers_are_refresh_safe`). Report-only — a derived read, never a stored or mutated field (custody §2.4). Verified live (a drifted full item → `· full · drifted`, a never-checked reference item → `· reference · unverified`). Tests: `tests/test_kb.py` (+6 — markers on list pages, drift-ledger reflection incl. `unchanged`→`verified`, concept+tag pages, consolidated reps, refresh-safe, scope boundary; 3 existing exact-shape assertions updated for the marker; `make_rendered` gains `raw_text`/`content_hash` for full-fidelity fixtures). Docs: `docs/library-format.md` (group-page + consolidation examples now show the marker + the docs-guardrail `example-category-consolidation` pinned block; a new marker paragraph), `docs/architecture.md` (KB compiler bullet + the per-item-custody section — the human-readable surface the picture previously skipped). Supersedes the long-deferred H65 (same intent; this is the live row). Full suite green (2769). Precondition: the shipped ledger; H58 (the per-item posture primitive). | → cap 1, cap 2 |
| H90 | **✓ shipped. Per-excerpt `last_checked` on `scrolls context` excerpts at `full` budget (cap 1 + cap 10) — the time-axis sibling of H62.** H62 put a per-excerpt `_drift \`<posture>\`_` tag beneath each `full`-tier `context` excerpt's meta line (whether that source moved); H44 the classification tag. The remaining per-excerpt axis was *when* the verdict was taken. Now the drift tag carries it: `_drift \`<posture>\` · last seen <checked_at>_` (the `custody.last_checked` of the same latest verdict), or `_drift \`<posture>\` · never re-checked_` when the ledger holds no verdict — the honest-absence counterpart of the `unverified` posture, never a faked time. Read from the *same* `latest_events` the scope `_Custody:_` headline (H47) and the drift posture (H62) already do (one read), through the shared `custody.last_checked` (added to `context.py`'s imports), so the per-excerpt staleness equals what every other surface reports for that item. Gated to `full` (the H10/H44/H62 depth-honesty — `index`/`connected` stay lean catalogs, no per-excerpt tag). CLI + MCP twins share `build_context`, so the timestamp rides MCP for free (verified live: a drifted item → `· last seen 2026-06-14T…`, a never-checked item → `· never re-checked`). So an agent dropping the model-facing bundle into its window reads per-source not just whether each excerpt drifted but as of when, and can pick a `verify --stale-before <ISO>` boundary by inspection — the per-excerpt completion of the time axis, exactly as H62 completed the per-excerpt drift posture. Tests: `tests/test_context.py` (drift tag carries the timestamp at full; never-checked → `never re-checked`; the posture+timestamp match the `drift_posture`/`last_checked` primitives; absence below full unchanged; one block-length bound widened for the longer tag). Docs: `docs/cli.md` (per-source trust tags paragraph + console example), `docs/architecture.md` (context-bundle bullet), `context.py` module + `_provenance_tags` docstrings. Full suite green (2770). Precondition: H62 (the per-excerpt drift tag, shipped); H47 (the shared ledger read, shipped); H84 (the `last_checked` primitive, shipped). | → cap 1, cap 10 |
| H91 | **✓ shipped. Fold the compiled `library/` per-item marker into the per-item cross-surface invariant (cap 1 + cap 7).** H89 put the `· <fidelity> · <drift>` marker on the compiled list-page rows but pinned it only in `tests/test_kb.py`; the per-item convergence suite asserted the *JSON* surfaces (`list`/`search`/`show`/`related`/`graph`/`works`) and the bundle briefing agree on an item's `drift`/`fidelity`, but not this human-readable Markdown surface. Now folded in as the surface the per-item picture previously skipped — a new `_library_markers` helper parses the `· <fidelity> · <drift>` suffix off a compiled group page (the greedy-`.*`-then-anchored-tail parse, the way `_bundle_postures`/`_bundle_last_checked` parse the briefing prose, H59/H88), and `test_compiled_library_page_agrees_on_the_per_item_custody_marker` asserts each item's compiled-page `(fidelity, drift)` equals the canonical `(get_fidelity, drift_posture)` *and* the JSON `list` surface's `fidelity`+`drift`, over a new fidelity×posture seed (`_seed_marker_fixture` — full/partial/reference crossed with verified/drifted/rotted/unverified, all source `web` so they land on one `sources/web.md` page; the ring seed has no `markdown_path`, so the KB skips it — hence the dedicated seed). Makes "the compiled library reads the same per-item custody picture as the agent surfaces" a tested contract, completing the H89 convergence story. Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" both broadened); no production change. Full suite green (2770). Precondition: H89 (shipped), H59 (the pattern mirrored, shipped). | → cap 1, cap 7 |
| H92 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked **H93 shipped** — the per-item `last_checked` time axis now rides the compiled `library/` page marker (`· <fidelity> · <drift> · checked <ts>` / `· never checked`), folded into the H91 compiled-page convergence test (the marker timestamp ties to the `history` head, the H88 tie on the compiled surface). The **per-item custody picture (fidelity + drift + last_checked) is now complete on the compiled human-readable surface too**, at parity with all seven JSON surfaces and the bundle/context Markdown. Long-deferred-but-valid slices remain **un-started and still valid** (passed over while the per-item-marker theme shipped as a coherent block, not overtaken): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H55** (`maintain` rechecks the unverified set first), **H83** (`maintain` bounds its recheck to the stale set), **H94** (per-excerpt `context` tag ≡ inspect-surface parity invariant). Appended **H95** (scope custody headline on the compiled `library/` group list pages — the human-readable scope-level custody, the compiled counterpart of the bundle/context headlines H45/H47) and **H96** (library-wide custody headline on the compiled `library/index.md` landing page — the compiled counterpart of `status`'s headline H38) to keep ≥6 un-started slots. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). **H95 + H96 were then taken and shipped in the same run** (the scope custody headline now rides the compiled group list pages *and* the landing `index.md` whole-library headline — the human-readable counterparts of the bundle/context scope headlines and `status`). Appended **H97** (fold the compiled scope headlines into the custody-convergence invariant — the scope-level analogue of H91's compiled-page per-item convergence) to keep ≥6 un-started work slots: H39, H40, H55, H83, H94, H97. | maintenance |
| H93 | **✓ shipped. Per-item `last_checked` on the compiled `library/` page marker (cap 1 + cap 2) — the time-axis completion on the human-readable surface.** H89 gave the compiled list-page rows a two-axis `· <fidelity> · <drift>` marker; the *time* axis (`last_checked`, when the drift verdict was taken) rode every JSON browse/inspect/node/works surface (H84/H86/H87) and the bundle briefing (H42) but not the compiled pages. `kb._custody_marker` now appends the `custody.last_checked` of the item's latest verdict as a third token — `· checked <checked_at>` (the verbatim ledger timestamp) or `· never checked` for the honest-absence `last_checked(None) is None` — from the same one `latest_events` read per compile H89 already does, so a human auditing staleness while browsing the library reads *as of when* each row's posture was taken and can pick a `verify --stale-before <ISO>` boundary by inspection (the H84 rationale on the human-readable surface). **Open question resolved (recorded):** the extra token does *not* warrant keeping the marker two-axis — the staleness read is load-bearing and the compiled pages are the human-facing browse surface; **and the timestamp is shown verbatim, not date-only**, because every other custody-time surface renders the stored value verbatim (bundle `as of <checked_at>` H42, context `last seen <checked_at>` H90) — a date-only truncation would make the compiled page the *only* surface that desyncs from the `history` head, breaking the clean H88/H91 convergence tie. Deterministic (stored timestamp verbatim, the H21 posture — never a wall-clock-relative "x ago"), refresh-safe (inside the `@generated` fence — a re-verify refreshes both the posture *and* the timestamp on recompile), report-only (a derived read, never a stored field, custody §2.4). Tests: `tests/test_kb.py` (+1 — `test_kb_list_page_marker_carries_last_checked_timestamp`: `checked <ts>` for a verdict-bearing row, `never checked` honest absence; the refresh-safe test extended to assert the timestamp axis refreshes; three exact-shape assertions updated), folded into the H91 compiled-page convergence test (`tests/test_custody_convergence.py` — the marker is now a `(fidelity, drift, last_checked)` triple equal to the canonical primitives *and* the JSON `list` surface, with the marker's timestamp asserted equal to the `history` head's `checked_at`, the H88 tie on the compiled surface; the `_library_markers` parser captures the time token, `None` for `never checked`). Docs: `docs/library-format.md` (the marker shape + both prose paragraphs + the `example-category-consolidation` pinned block now carry the time token), `docs/architecture.md` (KB compiler bullet + the per-item-custody section). Full suite green (2771). Precondition: H89 (the marker, shipped); H84 (the `last_checked` primitive + browse-row precedent, shipped). | → cap 1, cap 2 |
| H94 | **✓ shipped. Per-excerpt `context` tag ≡ inspect-surface parity invariant (cap 7 + cap 10).** H44/H62/H90 put the classification + drift + `last_checked` tags on the `full`-tier `context` excerpts — the bundle an agent actually drops into its window — each *claimed* to read the same as the `show`/`list` surfaces, but the drift + `last_checked` tie to the *inspect* surface (`show`) was pinned only obliquely (`test_context_excerpt_drift_matches_the_ledger_primitives` ties the tag to the ledger primitive, not to `show`). Now folded into the per-item cross-surface invariant (`tests/test_custody_convergence.py`) as a surface in its own right: over the four-posture `_seed_linked_drift_postures` fixture, the `_drift \`<posture>\` · last seen <ts>_` tag parsed off each excerpt (new `_context_excerpt_tags` helper, the way `_bundle_postures`/`_bundle_last_checked` parse the briefing — the meta line's id paired with the drift tag that follows, a classification tag between skipped) is asserted to equal the `drift`/`last_checked` `scrolls show` reports for that item *and* the canonical `drift_posture`/`last_checked` primitives — with `never re-checked` ⇔ the `unverified`/`null` honest absence (`web:4`), and every in-scope item rendering a parsed tag (none silently dropped). Mutation-checked non-vacuous (perturbing the production drift tag to a fixed posture fails the test). Makes "the model-facing bundle's per-source custody reads the same as the inspect surface" a tested contract — the `context`-excerpt counterpart of the per-item drift/last_checked invariant already pinning `list`/`search`/`graph`/`related`/bundle/`show`. Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" per-item paragraph both broadened); no production change. Full suite green (2882). Precondition: H90 (shipped), H62 (shipped); H59 (the convergence-suite pattern). | → cap 7, cap 10 |
| H95 | **✓ shipped. Scope custody headline on the compiled `library/` group list pages (cap 1 + cap 2).** The per-item picture rode each compiled list-page *row* (H89/H93), but the compiled pages carried no *scope-level* custody summary — the bundle briefing (H45), the `context` bundle (H47), `status` (H38), and the `graph` stats block (H52) all carry a `_Custody: N scroll(s) · fidelity <tier counts> · drift <posture counts>._` headline, but the human-readable `library/` pages did not. `kb._write_page` now emits that one-line headline under each group page's count line (`sources/`/`categories/`/`concepts/`/`tags/`), built from the **shared `custody.custody_headline(members, verdicts)`** the bundle/context/`status`/`graph` headlines already share — so the compiled headline is byte-identical to those surfaces and its tier/posture totals equal the page's own per-row markers by construction (every scroll has one fidelity tier and one drift posture). Over the one `latest_events` read per compile `compile_kb` already does (H89); refresh-safe (inside the `@generated` fence — a re-verify refreshes it on recompile), report-only. **Scope decision (recorded):** scoped to the four group list pages only — the `index`/`graph`/`works` rollup pages, rendered by their own functions (`_write_index`/`_write_graph_page`/`_write_works_page`, not `_write_page`), carry no page-scoped headline; the whole-library custody headline on the landing `index.md` is the separate H96 (so the two don't double up). Tests: `tests/test_kb.py` (+4 — headline over a fidelity/drift mix == the shared primitive, totals == the page's per-row markers, refresh-safe across a re-verify, rollup-pages-omit-it scope boundary; the concept-page exact-shape test updated). Docs: `docs/library-format.md` (a new shape bullet + the source-page and `example-category-consolidation` examples now carry the headline), `docs/architecture.md` (KB compiler bullet). Full suite green (2775). Precondition: H47 (the shared `custody_headline` primitive, shipped); H89 (the per-compile ledger read, shipped). | → cap 1, cap 2 |
| H96 | **✓ shipped. Library-wide custody headline on the compiled `library/index.md` landing page (cap 1 + cap 2).** The compiled `index.md` is the library's landing/browse entry point but carried no custody summary; `scrolls status` carries a one-line whole-library custody headline (H38) an agent or human reads without parsing `doctor`. `kb._write_index` now emits the same `custody.custody_headline` in the index header block (after the graph/works lines) — the compiled counterpart of `status`'s headline, so opening the library landing page answers "how custody stands" at a glance through the same shared primitive (byte-identical to `status` and the H95 group-page headlines). **Scope decision (recorded):** the headline is over the *rendered/compiled* library — the `items` `compile_kb` already filters to those with a `markdown_path`, the same set the index's own count line counts — **not** all held items. Rationale: a page's headline must describe that page's scope; reading all held items would make the headline's `N` disagree with the index's own count line whenever unrendered items exist (an internal contradiction on one page). It therefore equals `status`/`doctor`'s custody aggregate exactly when every held item is rendered (the common case), pinned by `test_kb_index_carries_a_library_wide_custody_headline` (which also asserts the byte-identical-to-`custody_headline` shape and `doctor` convergence). Refresh-safe inside the `@generated` fence; the empty initialized library is the honest `_Custody: 0 scroll(s)._`. Tests: `tests/test_kb.py` (+3 — the index headline == the shared primitive over the rendered library + `doctor` convergence, the empty-library honest zero, and the `graph`/`works`-omit-it boundary updated to note the index now carries the whole-library one). Docs: `docs/library-format.md` (index prose + `example-kb-index` pinned block carry the headline), `docs/architecture.md` (KB compiler bullet). Full suite green (2777). Precondition: H38 (the `status` headline precedent, shipped); H95 (the group-page headline, the page-scoped sibling, shipped). | → cap 1, cap 2 |
| H97 | **✓ shipped. Fold the compiled `library/` scope custody headlines into the custody-convergence invariant (cap 1 + cap 7).** H95/H96 put a `_Custody:_` scope headline on the compiled group list pages (over the page's members) and the landing `index.md` (over the rendered library), each built from the shared `custody.custody_headline` and *claimed* to converge with `doctor`/`facets`/`custody_counts` for its scope — but that scope-level convergence was pinned only per-surface in `test_kb.py`. Now folded into the cross-surface invariant `tests/test_custody_convergence.py` (the scope-level analogue of H91's compiled-page *per-item* convergence): a new `test_compiled_library_pages_agree_on_the_scope_custody_headline` over a two-source fidelity/drift seed (`_seed_compiled_scope_fixture` — three `web` scrolls + one `arxiv`, all rendered, so a `--source web` filter genuinely narrows the index scope to a group page's scope) parses the `_Custody:_` headline off `sources/web.md` (a new `_library_headline` helper, the scope-headline counterpart of `_library_markers`, splits the `· `-delimited sections into non-zero tier/posture maps) and asserts its totals equal `custody_counts(web_members, verdicts)` *and* `facets fidelity`/`facets drift --source web`; and the `index.md` headline equals `custody_counts` over the whole rendered library *and* `doctor`'s `custody` aggregate *and* the whole-library facets (all items rendered). Also pins the H95 scope boundary — the `graph.md`/`works.md` rollup pages carry no headline (`_library_headline` → `None`). Mutation-checked non-vacuous (perturbing `custody_headline` fails the test). Makes "the compiled library's *scope* custody summary reads the same as the agent aggregates" a tested contract, completing the compiled-surface custody theme (per-item H91/H93 + scope H97). Test + docs only; no production change. Full suite green (2778). Precondition: H95 (shipped), H96 (shipped); H50/H91 (the convergence-suite pattern). | → cap 1, cap 7 |
| H98 | **✓ shipped. Custody counts in the `search`/`list --stats` scope envelope (cap 7 + cap 1).** The opt-in `--stats` envelope (roadmap H6) reported `{scope, stats: {matched, returned, truncated}, results}` — *how many* matched, but not *what custody those matches stand in*. Now `stats` carries a `custody` member: the `custody.custody_counts` (fidelity-tier + drift-posture count maps) over the **matched** scope (the full match set, not just the returned page) — the browse-surface counterpart of the `graph` `stats.custody` block (H52), so an agent paging results sees "of the N matched, how much is held in full and how much drifted" without a second `facets` call. **Design decisions (surfaced):** (1) the new shape core is `custody.tally_custody((fidelity, drift) pairs)` — `custody_counts` (item+ledger sourced) now delegates to it, and the search envelope folds each hit's *own* `fidelity`/`drift` (the H58 per-item parity) through the same core, so item-sourced and hit-sourced tallies agree by construction. (2) For `list` (filter-only) the matched set is the already-fetched uncapped `matched_items` + the one `verdicts` read, so the tally is nearly free and equals `facets fidelity`/`drift` for the same scope. (3) For `search` the matched scope is *query*-scoped (the contentless reference pointer a `list` includes is not full-text matched), and the full match set is re-read uncapped **only when truncated** (the page is reused otherwise). (4) The envelope is **CLI-only** — the MCP `search_scrolls`/`list_scrolls` twins return bare arrays, no envelope — so this is CLI-only, no MCP change. The bare-array default (no `--stats`) is unchanged, so the G1 empty `[]` and the list contract never regress; the empty-store `list --stats` carries a zeroed custody for a stable shape. Tests: `tests/test_scope.py` (custody member present-when-passed/omitted-when-not, sums-to-matched), `tests/test_custody.py` (`tally_custody` fold + empty + `custody_counts` delegation parity), `tests/test_cli.py` (list/search matched-scope tally, matched-not-page under truncation, opt-in absent from bare array, empty-store zeroed; 6 existing G2 exact-shape assertions updated via a `_core_stats` helper that drops the new key), `tests/test_custody_convergence.py` (`list --stats` custody == facets, whole-library + `--source`-scoped). Docs: `docs/cli.md` (search/list `--stats` paragraphs + examples + the sibling-invariant family), `docs/architecture.md` (the custody-headline family + the shared `tally_custody` core). Full suite green (2789). Precondition: H6 (the envelope, shipped); H52 (the stats-custody pattern, shipped); H58 (per-hit fidelity/drift, shipped). | → cap 7, cap 1 |
| H99 | **✓ shipped. `scrolls related --stats` carries the custody member too — the `--stats`-envelope custody family is now complete across all three browse surfaces (cap 7 + cap 1).** H98 put `stats.custody` on the `search`/`list --stats` envelopes; `related --stats` used the *same* `scope_envelope` (H6) but passed no custody tally. Now `_cmd_related` folds the full scored `hits` (each already carrying its `fidelity`/`drift`, H56) through the same `custody.tally_custody` H98 uses, over the **matched** (pre-cap) related set — so `search`/`list`/`related` all carry the `{tiers, drift}` count maps in their `--stats` envelope. The tally is over the anchor's related *neighbourhood* and `scored_related` excludes the anchor, so it answers "of the N items related to this one, how much is held in full / has drifted" without folding in the anchor's own custody (a sharp exclusion test: the anchor is `full`+`rotted`, a posture no neighbour has, and the tally shows `rotted: 0`). Covers the whole matched set past a `--limit` cap (`test_…_covers_the_matched_set_past_the_cap`); present as the zeroed shape on an isolated anchor; opt-in (the bare array is unchanged). CLI-only (the MCP `get_related_scrolls` returns a bare array, no envelope). Tests: `tests/test_related.py` (+6 — matched-set tally summing to `matched`, anchor-excluded, past-the-cap, empty-present, opt-in; the two existing exact-shape `stats == {…}` assertions now drop `custody` via a `_core_stats` helper, the H98 precedent). Docs: `docs/cli.md` (related `--stats` paragraph + example + "custody reads the same everywhere" sibling-invariant paragraph now names all three browse surfaces), `docs/architecture.md` (custody-headline family bullet). Full suite green (2794). Precondition: H98 (the `tally_custody` core + envelope custody member, shipped); H56 (per-hit fidelity/drift, shipped). | → cap 7, cap 1 |
| H100 | **✓ shipped. Custody tally on the `scrolls works` stats block (the works-surface entry in the `stats.custody` family — cap 7 + cap 1).** The `--stats`-envelope custody member rode all three browse surfaces (H98/H99) and the `graph` stats block (H52), but `scrolls works` — which emits a `{scope, stats, works}`-shaped object with an always-on `stats` companion — carried `items`/`works` counts and *no* custody tally, even though each `Representation` already carries `fidelity`+`drift` per item (H64). Now `works.to_payload` folds the **reported works' representations** (the same `(fidelity, drift)` pairs each rep entry renders) through the shared `custody.tally_custody` H98/H99 use, emitting a `stats.custody` member — so a reader of the works lens sees "of the multi-representation works in scope, how much is held in full / has drifted" without a second `facets` call. **Scope decision (recorded):** the tally is over the representation *entries* (not the library `stats.items` denominator), so its totals equal the rendered reps by construction; an item that represents two works contributes to both, exactly as it is rendered twice (no silent dedup). No new ledger read — the same `verdicts` already threaded for the per-rep `drift`; CLI + MCP twins share `to_payload`, so the member rides MCP for free (`works` has no bare-array form to protect). Tests: `tests/test_works.py` (+5 — tally over the reported reps, totals==rep entries, two-work item counted per entry, zeroed when no work reported, CLI end-to-end parity; exact-shape `stats ==` assertions drop `custody` via a `_core_stats` helper, empty/echo carry `ZERO_CUSTODY`), `tests/test_mcp.py` (+1 — twin carries it; exact-shape via `_works_core_stats`). Docs: `docs/cli.md` (works `stats.custody` paragraph + examples + the `stats.custody` family), `docs/architecture.md` (custody-headline family + works bullet). Full suite green (2800). Precondition: H64 (per-rep `drift`, shipped); H98/H99 (the `tally_custody` core, shipped). | → cap 7, cap 1 |
| H101 | **✓ shipped. Fold the browse-surface `stats.custody` family into the custody-convergence invariant (cap 7 + cap 1).** H98/H99/H100 put a `stats.custody` member on `search`/`list`/`related`/`works` (and H52 on `graph`), each *claimed* to fold the same `custody.tally_custody` over its matched scope, but the convergence was pinned only per-surface (e.g. `test_list_stats_custody_member_converges_with_facets`). Now folded into `tests/test_custody_convergence.py` (its home for scope-level convergence): over one seeded fixture each browse-surface `stats.custody` member is asserted to equal `tally_custody` over *its own* returned per-item `fidelity`/`drift` fields — so the envelope aggregate can never desync from the per-item axes (H56/H58/H64) it sums — with the `related` anchor excluded, the `works` reps folded, and `graph --all` node ≡ item scope; the `list`/`search` cases also equal `facets fidelity`/`drift` for the same filters. The `works` case carries its own DOI seed (the ring forms no work) and pins a concrete full+drifted / reference+unverified mix. Mutation-checked non-vacuous (perturbing `tally_custody` fails both new tests). Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" both broadened to name the `stats.custody` family invariant); no production change. Full suite green (2802). Precondition: H100 (shipped). | → cap 7, cap 1 |
| H102 | **✓ executed (2026-06-16).** Buffer refresh checkpoint (maintenance rule). Marked **H100 + H101 shipped** (one run — the works-surface `stats.custody` member completing the family across `search`/`list`/`related`/`works`/`graph`, and the cross-surface invariant pinning that each `stats.custody` member reads the same as the per-item fields it aggregates). The **`stats.custody` family is now complete and pinned.** Long-deferred-but-valid slices remain **un-started and still valid** (passed over while the `stats.custody` family shipped as a coherent block, not overtaken): **H39** (`export bundle --format html`), **H40** (`maintain` suggests the explicit repair), **H55** (`maintain` rechecks the unverified set first), **H83** (`maintain` bounds its recheck to the stale set), **H94** (per-excerpt `context` tag ≡ inspect-surface parity invariant). Appended **H103** (custody headline in the `scrolls maintain` report — the maintenance-surface entry of the custody-headline family) and **H104** (per-source custody breakdown in `scrolls doctor` — makes the custody audit actionable per source) to keep ≥6 un-started work slots: H39, H40, H55, H83, H94, H103, H104. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). | maintenance |
| H103 | **✓ shipped. Custody headline in the `scrolls maintain` report (cap 1, maintenance framing).** Every scope custody surface carried the shared one-line `custody.custody_headline` — the bundle briefing (H45), the `context` bundle (H47), the compiled `library/` pages (H95/H96) — *except* the scheduled worker's own primary report: `maintain` recorded the custody snapshot (score / fidelity tiers / drift posture counts) but rendered no one-line headline. Now the pass report carries a **`headline`** — the same `custody_headline` line — so an unattended worker's JSON log reads "how custody stands" at a glance without assembling the raw counts. **Decisions (surfaced):** (1) rendered from the **snapshot the run records**, not a fresh items+ledger read — a recorded snapshot has only the counts, so this is the one path that also lets the `--history`/`--trend` reads render a headline from each stored snapshot; it converges by construction with the `custody` block it sits beside and equals `custody_headline` over the post-maintenance library (`test_maintain_report_carries_the_one_line_custody_headline`). (2) The snapshot's `drift` uses doctor's vocabulary (`checked`/`unchanged`/…); the new pure `maintain.snapshot_headline` maps it into postures by the documented `verified ≡ unchanged` rule and delegates to a **new shared `custody.render_custody_headline(n, tiers, drift)`** the item-sourced `custody_headline` was refactored onto (one formatter, byte-identical line — `custody_headline` unchanged). (3) The history headline is rendered at **read time** (so a pre-H103 log entry renders one too — forward-compatible) and the stored `log.jsonl` keeps the bare `{recorded_at, snapshot, delta}`. (4) **`status` deliberately left untouched** — its `custody` block is the structured snapshot (H38), and adding a rendered string there is a separate surface; H103 is scoped to the maintenance report, the one scope-custody *Markdown-headline* gap. Report-only, network-free at `--no-recheck`; verified live (mixed-fidelity `_Custody: 3 scroll(s) · fidelity full 2, reference 1 · drift unverified 3._`; empty → `_Custody: 0 scroll(s)._`). Tests: `tests/test_custody.py` (+3 — `render_custody_headline` from counts / empty / delegation parity), `tests/test_maintain.py` (+7 — `snapshot_headline` posture-mapping / empty / forward-compat; report carries it == `custody_headline`; empty-library zero; `--history`/`--trend` runs each carry it, stored log stays bare). Docs: `docs/cli.md` (maintain `headline` paragraph + all four console examples), `docs/architecture.md` (Maintain bullet + subsystem index). Full suite green (2871). Precondition: H34 (maintain, shipped); H38 (the snapshot primitive, shipped); H47 (the shared headline renderer, shipped). | → cap 1, maintenance framing |
| H104 | **✓ shipped. Per-source custody breakdown in `scrolls doctor` (cap 1).** `doctor`'s `custody` block reported one whole-library fidelity-tier / drift-posture aggregate; an operator triaging *which* source's custody is weakest (most reference-only, most drifted) had to enumerate per source by hand. Now the block carries an optional **`by_source`** map — the same `{tiers, drift}` aggregate split per source via a new pure `custody.custody_counts_by_source(items, verdicts)` (`custody_counts` grouped by `source`, keys sorted; empty library → the honest empty map) — so the audit names the source to target a `verify --drift`/`media`/recapture at. Built off the **same** held-filtered `latest_events` `_check_custody_drift` already reads (no second ledger read), so the per-source tallies **sum to the whole-library `custody` block by construction** (the H50 convergence posture, per source: every item lands in exactly one source group, so summing the groups re-counts the library) and each per-source tally equals `facets fidelity`/`drift --source <name>`. Report-only like the rest of the custody block — a weak per-source picture is a view, never an `issue` or the exit code. Verified live (`web` 1 full + 1 reference / 2 unverified, `arxiv` 1 full / 1 unverified, sorted keys). Tests: `tests/test_custody.py` (+4 — `custody_counts_by_source`: sorted-keyed grouping, per-source posture, sums-to-whole, empty map), `tests/test_doctor.py` (+5 — empty map, multi-source grouping, per-source posture, sums-to-whole-block, never-feeds-issues; the exact-shape CLI custody-block assertion updated), `tests/test_custody_convergence.py` (+1 — `test_doctor_by_source_converges_with_the_per_source_tally_and_facets`: each per-source tally == `custody_counts(source_members)` == `facets fidelity`/`drift --source X`, and sums to doctor's own block — the per-source sibling of the whole-library convergence). Docs: `docs/cli.md` (doctor custody block — the `by_source` paragraph), `docs/architecture.md` (Doctor bullet). Full suite green (2881). Precondition: the shipped custody block; H48 (the drift tally, shipped). | → cap 1 |
| H105 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; mark shipped, prune overtaken slices, keep ≥6 un-started slots. Re-confirm the week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). Consider the deferred bi-temporal drift framing only if an agent workflow has shown the event record insufficient (else keep deferred). | maintenance |
| H106 | **✓ shipped. `maintain`'s `doctor --fix` suggestion converges with what `doctor --fix` actually repairs (cap 1 + cap 7).** H40 hardcodes the structural categories `maintain` routes to `doctor --fix` (`duplicates`/`missing_scrolls`/`fts`) in `_REPAIR_COMMANDS`; if `doctor`'s `fix=True` path ever gains or loses a repairable category, the suggestion could silently drift from doctor's real capability — suggesting a command that no longer closes the gap (the very honesty H40's orphan-omission protects). Pinned in `tests/test_maintain.py` (its home — the existing `suggest_repairs` unit + end-to-end tests live there; this is the stronger contract against doctor's *real* behavior, not the synthetic-report mapping): a new `_seed_all_structural_drift` builds one library carrying **every** structural finding doctor distinguishes — a mergeable duplicate pair, a deleted scroll, an out-of-sync FTS index, an orphan scroll, *and* a missing captured-media file — then `run_doctor(home)` (report mode) feeds `suggest_repairs`, and `run_doctor(home, fix=True)` runs the real fix. Three tests: (1) the set `suggest_repairs` routes to `scrolls doctor --fix` is **exactly** the set `fix=True` transitions to a repaired status (`merged`/`rewritten`/`rebuilt`) — `{duplicates, missing_scrolls, fts}` — via a generic `_categories_doctor_fix_repaired` that scans *all* list categories (so a future change that starts repairing media/orphans, or stops repairing a structural one, fails here, not just in the table); (2) the orphan, which `fix=True` leaves `found`, is in **neither** set (not repaired, no `suggested` command names it — the H40 honest-absence point pinned against doctor's real behavior); (3) the sibling — missing media stays `found` (doctor reports, never fixes) and routes to `scrolls media`, never the grouped `doctor --fix`. Mutation-checked non-vacuous (dropping `fts` from `_REPAIR_COMMANDS` breaks the convergence assert — verified live). Makes "maintain only suggests a command that genuinely closes the gap" a tested contract rather than a hand-maintained constant. Test + docs only (`docs/architecture.md` Maintain bullet + `docs/cli.md` `suggested` paragraph note the mapping is pinned against doctor's real `fix=True` behavior); no production change. Full suite green (2885). Precondition: H40 (shipped). | → cap 1, cap 7 |
| H107 | **Buffer refresh checkpoint** (maintenance rule). Re-derive the next post-MVP horizon; mark shipped, prune overtaken slices, keep ≥6 un-started slots. Re-confirm the week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). Consider the deferred bi-temporal drift framing only if an agent workflow has shown the event record insufficient (else keep deferred). | maintenance |
| H108 | **✓ executed (2026-06-16).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102 today). Marked **H55 shipped** (coverage-first recheck ordering via the new `custody.recheck_order` — never-checked first, then oldest-verdict-first; a bounded `maintain` pass now advances custody coverage instead of re-checking the same head). With H39/H40/H55 all shipped, the un-started **work** slices fell to five (H83, H94, H103, H104, H106), below the ~6 floor — appended **H109** + **H110** below to restore the buffer. The recheck-ordering work makes **H83** (maintain bounds its recheck to the *stale* set using its own last-run timestamp) the natural next pick: H55 orders never-checked-first, H83 generalizes the bound from never-checked to "not seen since the last sweep" via `verify --stale-before` (H79) + `custody.items_checked_before`. Bi-temporal drift framing stays correctly deferred. ≥6 un-started work slots now: H83, H94, H103, H104, H106, H109, H110. | maintenance |
| H109 | **✓ shipped. `scrolls maintain` reports recheck *coverage* (cap 1, maintenance framing).** H55 makes a bounded recheck advance custody coverage, but the report's `recheck` block showed only *this pass's* `checked` count — a worker could not see how close the library is to fully covered without diffing `custody.drift.unverified` across runs. Now the recheck report carries a **`coverage`** member — `{verified, total}` over the held, **hash-bearing (verifiable)** items via a new pure `custody.recheck_coverage(items, verdicts, checked_ids=())`: `total` is how many *can* carry a verdict (a reference-only capture has no baseline hash to diff, so it is excluded — coverage can reach full, never stuck below 100% on unverifiable items), `verified` how many now do. So a single report answers "N of M verifiable items carry a verdict" and the H55 coverage-first progress is *visible* directly. **Post-recheck by construction (decision):** an item is verified-after if it had a verdict before this pass (in `verdicts`) *or* was re-checked in it (id in `checked_ids`) — folding this pass's events into the one `latest_events` read the recheck already does (no second ledger read), so the figure agrees with the post-maintenance `doctor` audit (which runs *after* the recheck): `coverage.verified` ≡ `custody.drift.checked`, and `total − verified` ≡ `custody.drift.unverified` when every held item is hash-bearing — both from the one `unverified_items` predicate doctor's `unverified` reports on (convergence by construction). Reuses `custody.unverified_items`; report-only; `--no-recheck` still carries the figure via a standalone read (`_maintain_coverage`, a read not the live edge). The console example in `docs/cli.md` was also brought current (it predated H83 — now shows `scope`/`since`/`coverage` and uses `--all` so the all-rechecked second run is consistent). Tests: `tests/test_custody.py` (+5 — pure `recheck_coverage`: verified-of-hash-bearing, post-recheck `checked_ids` fold, total-is-caller's-set, empty honest-zero, verified-never-double-counted), `tests/test_maintain.py` (+6 — report carries coverage, post-recheck advances within a bounded pass `1→2→3`, converges with the doctor audit's `checked`/`unverified`, present under `--no-recheck`, reference-only excluded from the denominator). Docs: `docs/cli.md` (maintain recheck step + corrected example), `docs/architecture.md` (Maintain subsystem + subsystem-index bullets), `custody.py` docstring. Full suite green (2857). Precondition: H34 (shipped), H55 (the coverage-first ordering, shipped). | → cap 1, maintenance framing |
| H110 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H83 shipped** (the scheduled recheck is now stale-bounded by the last run's `recorded_at` via `maintain.last_run_boundary` + `custody.items_checked_before`, with `--all` recovering the whole-library recheck; H83 generalizes the H55 coverage-first bound from "the whole held set" to "the stale set since last run"). With H83 shipped the un-started **work** slices fell to five (H94, H103, H104, H106, H109) — appended **H111** below to restore the ≥6 floor. **H109** (maintain reports recheck *coverage*) is the natural next pick: H83 makes a default pass do only the stale work, and H109 surfaces *how close to fully covered* the library is (`{verified, total}` over the held set) so the monotone-coverage progress is visible in one report. Bi-temporal drift framing stays correctly deferred. ≥6 un-started work slots now: H94, H103, H104, H106, H109, H111. | maintenance |
| H111 | **✓ shipped. maintain's default stale recheck ≡ `verify --stale-before <last-run>` — a convergence invariant (cap 1 + cap 7).** H83 stale-bounds maintain's default recheck to the held items not seen since the last run; H79's `verify --stale-before <ISO>` is the explicit act-side selection over the *same* `custody.items_checked_before` selector. The load-bearing tie now pinned in `tests/test_custody_convergence.py` (its home for the verify-selection-family invariants, H81): over a seeded mixed-staleness fixture (an old verdict before the boundary → stale, one *at* and one *after* it → fresh by the exclusive compare, a never-checked item → trivially stale, a reference-only item → unverifiable/excluded), the set a default `scrolls maintain` pass re-verifies — captured via a recording `cli.live_recapture` stub, the set each pass actually touches — equals the set `scrolls verify --stale-before <last-run recorded_at>` selects (run in a fresh library B to keep the ledger pristine), both `{web:1, web:4}` over the crafted boundary `maintain.last_run_boundary` derives off the recorded snapshot. Also pins the never-checked `--unverified` bucket is subsumed by that stale set (`{web:4} ⊆ {web:1, web:4}`, trivially stale at any boundary) and that `maintain --all` ignores the boundary and rechecks the whole hash-bearing set `{web:1..4}` (the H83 escape hatch). **Mutation-checked non-vacuous on two levels:** forcing `last_run_boundary → None` fails the boundary-derivation assertion, and ignoring the boundary in `_recheck_held_items` (recheck the whole set) fails the behavioral `maintained == canonical`. So "the scheduled pass and the explicit recheck select the same stale set" is a tested guarantee, not a parallel-implementation coincidence. Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" both broadened); no production change. Full suite green (2887). Precondition: H83 (shipped), H79 (shipped), H81 (the verify-selection-family invariant home, shipped). | → cap 1, cap 7 |
| H112 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H109 shipped** (the recheck report now carries a `coverage` member — `{verified, total}` over the verifiable held set via the new pure `custody.recheck_coverage`, post-recheck and converging with the doctor audit's `checked`/`unverified` by construction, so a scheduled worker reads "N of M covered" in one report). With H109 shipped the un-started **work** slices fell to five (H94, H103, H104, H106, H111) — appended **H113** below to restore the ≥6 floor. **H111** (maintain's default stale recheck ≡ `verify --stale-before <last-run>` convergence invariant) is the natural next pick: H109 surfaces *how much* coverage a pass advanced, and H111 pins that the *set* a default pass targets is exactly the explicit `verify --stale-before` selection — the recurring face of the verify-selection family. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). ≥6 un-started work slots now: H94, H103, H104, H106, H111, H113. | maintenance |
| H113 | **✓ shipped. Recheck coverage on the `doctor` custody block (cap 1).** H109 reports recheck `coverage` (`{verified, total}` over the verifiable held set) on the *maintenance* surface; the standalone *audit* (`scrolls doctor`) reported only the raw `custody.drift.checked`/`unverified` counts — the verified and unverified counts but not the hash-bearing *denominator*, so a reader could not see coverage as a fraction without re-deriving the verifiable total. Now doctor's `custody.drift` block carries a **`coverage`** member — the same `custody.recheck_coverage` over the held, hash-bearing items (no `checked_ids`, since doctor never rechecks — a pure read of the current ledger) — so `scrolls doctor` shows "N of M verifiable items carry a verdict" at parity with `maintain`. **Scope decision (surfaced):** the figure lands on the *full doctor report* (the audit), **not** the `status` headline — `maintain.custody_snapshot` (which `status` and `maintain --history` distil) keeps only the scalar `_DRIFT_AXES`, so adding a nested `coverage` to the drift block leaves those surfaces untouched by design; the `status` headline stays a posture summary (coverage is a fraction better read from `doctor`/`maintain`), and the roadmap draft's "and `scrolls status` shows it" was not implemented — recorded here as the deliberate boundary. Report-only like the rest of the drift block (never feeds `issues`/the exit code); `coverage.verified` ≡ `drift.checked` by construction (every verdict-bearing held item is hash-bearing — `verify` never runs on a reference-only item, confirmed across all five selections + the single-id path), pinned in `tests/test_doctor.py` and folded into the convergence suite as a *cross-surface* tie: `doctor custody.drift.coverage` ≡ `maintain --no-recheck recheck.coverage` (both delegate to the one `recheck_coverage` primitive). Verified live (empty library → honest `{verified:0, total:0}`). Tests: `tests/test_doctor.py` (+3 — coverage over a mixed library, denominator excludes reference-only/can-reach-full, `verified` ≡ `checked`; 2 exact-shape drift/custody-block assertions + the no-ledger-table test updated), `tests/test_custody_convergence.py` (+1 — `test_recheck_coverage_converges_across_doctor_and_maintain`). Docs: `docs/cli.md` (doctor drift block — the coverage paragraph), `docs/architecture.md` (Doctor + Maintain bullets). Full suite green (2861). Precondition: H109 (the `recheck_coverage` primitive, shipped); the shipped custody drift block. | → cap 1 |
| H114 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H113 shipped** (the recheck `coverage` fraction now rides the standalone `doctor` audit's `custody.drift` block, at parity with `maintain`'s H109 recheck report and pinned as a cross-surface tie — `doctor` ≡ `maintain --no-recheck` coverage — in the convergence suite). With H113 shipped the un-started **work** slices fell to five (H94, H103, H104, H106, H111) — appended **H115** below to restore the ≥6 floor. **H103** (custody headline in the `maintain` report — the one scope-custody surface still missing the shared `custody_headline`) is the natural next pick: a small, well-grounded completion of the custody-headline family using a shipped primitive. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H94, H103, H104, H106, H111, H115. | maintenance |
| H115 | **✓ shipped. `scrolls maintain` records recheck coverage in the snapshot/trend (cap 1, maintenance framing).** H109/H113 reported coverage as a *point-in-time* fraction (the `maintain` recheck report and the `doctor` audit), but the custody *snapshot* `maintain.custody_snapshot` distils — the one `maintain --history`/`--trend` replay — kept only `score`/`tiers`/drift posture/staleness, **not** coverage, so a worker reading the trend could not see whether *coverage* improves across runs (the monotone-coverage progress H55/H83 drive). Now `custody_snapshot` carries `coverage` (the `{verified, total}` from the post-maintenance audit's `custody.drift.coverage`, H113), so it rides `last-run.json`/`log.jsonl` and each `--history` run replays its own coverage; `compute_delta` diffs it per-key (a count mapping like `tiers`/`drift`); and `compute_trend` (H46) gains a **`coverage_change`** member (`{verified, total}` net first→last — Δ`verified` up as bounded passes verify the never-checked tail, Δ`total` up as new verifiable items are added) so `--trend` answers "is the library getting more covered?". **Load-bearing decision (surfaced):** `coverage_change` is a *separate reported axis kept out of `posture`* — coverage measures *how much has been checked*, not *how faithfully we hold what we have*, so rising coverage is not "improving" integrity and an overdue re-check is not "regressing"; the H46 integrity-first `posture` rule is unchanged. Honest absence: a pre-H115 snapshot lacking `coverage` reads missing-axis-zero (ADR 0082) in both the delta (before 0, never null on a *present* baseline) and the trend (endpoint reads 0); `<2` runs carries a null `coverage_change`. **Noted consequence:** `status`'s custody block *is* `custody_snapshot(run_doctor(...))`, so it now carries `coverage` too (the convergence-by-construction posture — `status`/`maintain`/`doctor` stay consistent; `tests/test_cli.py` `_custody_headline` helper updated). Pure snapshot/trend layer, no new ledger read. Verified live (two `--limit 1` passes → snapshot `coverage` `{1,3}`→`{2,3}`, `--history` replays each, `--trend` `coverage_change {verified:1, total:0}` with `posture: holding`). Tests: `tests/test_maintain.py` (+10 — snapshot carries coverage + pre-H113 zero default, per-axis delta change, delta first-run-null + pre-H115-baseline-degrades, trend coverage movement / total-grows / posture-independence / pre-H115-endpoint-zero / insufficient-history-null, end-to-end snapshot+log record + `--history`/`--trend` advancing). Docs: `docs/cli.md` (maintain step 4 + `--trend`), `docs/architecture.md` (Maintain subsystem + index), `maintain.py` docstrings. Full suite green (2911). Precondition: H113 (the doctor coverage member, shipped); H46 (`compute_trend`, shipped); H34/H36 (the snapshot + log, shipped). | → cap 1, maintenance framing |
| H116 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H103 shipped** (the `maintain` report and its `--history`/`--trend` runs now carry a one-line `headline` — the shared `custody.custody_headline` rendered from the recorded snapshot via the new pure `maintain.snapshot_headline` + the extracted `custody.render_custody_headline` formatter, mapping the snapshot's `unchanged` to the `verified` posture; `status` deliberately left as the structured-snapshot surface). The **scope custody-headline family is now complete on every surface** — bundle (H45), context (H47), compiled `library/` pages (H95/H96), and the `maintain` report (H103) all emit the byte-identical line, with `status`/`graph` carrying the structured snapshot. With H103 shipped the un-started **work** slices fell to five (H94, H104, H106, H111, H115) — appended **H117** below to restore the ≥6 floor. **H104** (per-source custody breakdown in `scrolls doctor`) is the natural next pick: with the whole-library custody picture complete across surfaces, the next actionable deepening is making the audit name *which source's* custody is weakest. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H94, H104, H106, H111, H115, H117. | maintenance |
| H117 | **✓ shipped. `scrolls status` carries the rendered one-line `headline` string (cap 1 + cap 2) — the status-surface completion H103 deferred.** H103 added the rendered `custody.custody_headline` one-liner to the `maintain` report but deliberately left `status` as the structured-snapshot surface (its `custody` block is the `custody_snapshot` dict; a rendered string was recorded as a *separate* slice). This is that slice: `_cmd_status` now emits a `headline` field beside its `custody` snapshot block, rendered via the shipped `maintain.snapshot_headline(custody)` (→ `render_custody_headline`, no new derivation) — so a reader gets the one-line "how custody stands" without assembling the tiers/drift counts, at full parity with the `maintain` report (both the structured `custody` and the rendered `headline`). Rendered from the *same* snapshot it sits beside, so the line and the block converge by construction and equal `custody_headline(list_items, latest_events)` over the held library (`test_status_carries_rendered_headline_converging_with_the_block` pins the exact non-trivial line `_Custody: 2 scroll(s) · fidelity full 1, reference 1 · drift verified 1, unverified 1._`). Honest absence: before `init` the all-zero snapshot (`score: null`) renders `_Custody: 0 scroll(s)._` (never a fabricated count). CLI-only (`status` has no MCP twin). Tests: `tests/test_cli.py` (the convergence test + the two exact-shape `test_status_before_init`/`test_status_after_init` updated for the new `headline` key). Docs: `docs/cli.md` (status key table + custody-headline paragraph + both console examples), `docs/architecture.md` (status bullet). Full suite green (2912). Precondition: H103 (the `snapshot_headline` renderer, shipped); H38 (the `status` custody block, shipped). | → cap 1, cap 2 |
| H118 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H104 shipped** (the `doctor` `custody` block now carries a `by_source` map — the whole-library fidelity/drift aggregate split per source via the new pure `custody.custody_counts_by_source`, summing to the whole-library block and equal to `facets fidelity`/`drift --source X` by construction, pinned in the convergence suite — so the audit names which source's custody is weakest). With H104 shipped the un-started **work** slices fell to five (H94, H106, H111, H115, H117) — appended **H119** below to restore the ≥6 floor. **H106** (`maintain`'s `doctor --fix` suggestion converges with what `doctor --fix` actually repairs) is the natural next pick: a decision-grade test-only slice that pins H40's hardcoded structural-category mapping against doctor's real `fix=True` behavior, so the suggestion can never silently drift from what the command repairs. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H94, H106, H111, H115, H117, H119. | maintenance |
| H119 | **✓ shipped. `scrolls maintain` names the single weakest source in its report (`attention`) (cap 1, maintenance framing).** H123 threads `doctor`'s whole per-source `by_source` map into the report; this distils it to the **one** source worth flagging. The report now carries an **`attention`** member — the source with the most **actionable loss** (most `drifted` + `rotted` items — the sources *confirmed* moved or gone, the set a follow-up `verify --drift`/`media` targets; tie-broken by most `reference`-only, then source name), as `{source, tiers, drift, reason}` (the flagged source's own tally + a one-line `reason` naming the loss, e.g. `"2 drifted, 1 rotted"`) — so an unattended log flags "source `x` is weakest" without scanning every source. New pure `maintain.weakest_source(by_source)` over the same `report["custody"]["by_source"]` map the report already carries (**no new ledger read**), wired in `_cmd_maintain` beside `by_source` (computed once). **Honest `null` on three counts (decision surfaced):** an **empty** library (nothing to flag), a **single** source (no source *stands out* — the whole-library `custody` block already says everything; `attention` only adds value by discriminating *across* sources, so a one-source library is null even with drift), and a **fully-clean** library (no source carries any drifted/rotted loss — reference-only is the normal capture posture, a **tie-breaker, never a trigger** on its own). Live-pass only (like `suggested` H40 / `by_source` H123 / `scope`/`since` H83 — derived fresh from this pass's audit, never recorded in the snapshot/log, so `--history`/`--trend` carry none); report-only and idempotent, never feeds the exit code. Verified live (clean multi-source pass → `null`; drift one web item → `attention.source == "web"`, `reason "1 drifted"`, == `weakest_source(by_source)`). Tests: `tests/test_maintain.py` (+8 — the selector's drift+rot ranking / reference-then-name tie-break / clean-multi-source-null / single-source-null / empty-null; the report flags the weakest on a live pass and is null on a clean-or-empty pass; `--history`/log carry none). Docs: `docs/cli.md` (maintain `attention` paragraph + the console example now carries it), `docs/architecture.md` (Maintain prose + subsystem-index bullets), `maintain.py` docstrings. Full suite green (2901). Precondition: H104 (the `by_source` map, shipped); H123 (the report member it distils, shipped); H34 (maintain, shipped). | → cap 1, maintenance framing |
| H120 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H94 shipped** (the per-excerpt `context` tag ≡ inspect-surface parity invariant: the model-facing bundle's `_drift <posture> · last seen <ts>_` excerpt tag is now folded into the per-item cross-surface invariant and pinned to equal what `scrolls show` reports — so the per-source custody an agent drops into its window can never silently desync from the inspect surface; mutation-checked non-vacuous). With H94 shipped the un-started **work** slices fell to five (H106, H111, H115, H117, H119) — appended **H121** below to restore the ≥6 floor. **H106** (`maintain`'s `doctor --fix` suggestion converges with what `doctor --fix` actually repairs) remains the natural next pick: a decision-grade test-only slice pinning H40's hardcoded structural-category mapping against doctor's real `fix=True` behavior. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H106, H111, H115, H117, H119, H121. | maintenance |
| H121 | **✓ shipped. Per-source recheck coverage in the `doctor` custody breakdown (cap 1).** H113 put a whole-library `coverage` (`{verified, total}` over the verifiable held set) on `doctor`'s `custody.drift` block; H104 split the custody picture per source into `custody.by_source` (`{tiers, drift}` per source). Now each `by_source` entry also carries its own **`coverage`** `{verified, total}` — of that source's verifiable (hash-bearing) held items, how many carry a verdict — so the audit names not just which source has the most *drift* (H104) but which is least *covered* (most never-checked), the per-source counterpart of H113's whole-library coverage. **Implementation decision (surfaced):** folded into the **shared `custody.custody_counts_by_source`** itself (each group folds through the same `custody_counts` *and* `recheck_coverage` over its hash-bearing slice — no second ledger read), *not* a doctor-local merge — so the change rides every faithful-read consumer together: `doctor`'s `custody.by_source` and the `maintain` report's `by_source` (H123, a faithful read) both gain `coverage` and stay convergent by construction (the planned H127 `maintain by_source ≡ doctor by_source` invariant holds for free), and the H123 `by_source == custody_counts_by_source(...)` test stays valid as-is. `weakest_source`/`attention` (reads only `tiers`/`drift`) is unaffected. The per-source coverage **sums to the whole-library `drift.coverage` by construction** (every item lands in exactly one source group; the H50/H104 sum-to-whole posture on the coverage axis) and each per-source `coverage` equals `recheck_coverage` over that source's hash-bearing items. Report-only like the rest of the custody block (never feeds `issues`/the exit code); honest absence — a source with no hash-bearing items is `{verified: 0, total: 0}` (can reach full, never stuck below 100%). Verified live (`web` 1-of-1 covered with its reference-only item excluded from the denominator, `arxiv` 0-of-1; per-source sums to the whole `{1, 2}`; the `maintain --no-recheck` report carries the identical per-source coverage). Tests: `tests/test_custody.py` (+3 — per-source coverage == `recheck_coverage`, sums-to-the-whole-recheck-coverage, reference-only excluded), `tests/test_doctor.py` (+3 — per-source coverage over a multi-source library, sums-to-`drift.coverage`, reference-only excluded; the exact-shape custody-block assertion updated), `tests/test_custody_convergence.py` (folded into `test_doctor_by_source_converges_with_the_per_source_tally_and_facets`: per-source coverage == `recheck_coverage(source hash-bearing members)` and sums to `drift.coverage`). Docs: `docs/cli.md` (doctor + maintain `by_source` paragraphs + the `maintain` console example now carries it), `docs/architecture.md` (Doctor + Maintain bullets), `custody.py`/`doctor.py`/`maintain.py` docstrings. Full suite green (2918). Precondition: H104 (the `by_source` map, shipped); H113 (the `recheck_coverage` member, shipped). | → cap 1 |

| H122 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H106 shipped** (the `maintain` → `doctor --fix` suggestion is now pinned against doctor's *real* `fix=True` behavior: over a library seeded with every structural finding, the categories `suggest_repairs` routes to `scrolls doctor --fix` are exactly the ones `run_doctor(fix=True)` transitions to a repaired status, with the orphan in neither and missing media routed to `scrolls media` — mutation-checked non-vacuous, so the hand-maintained mapping can never silently drift from what the command repairs). With H106 shipped the un-started **work** slices fell to five (H111, H115, H117, H119, H121) — appended **H123** below to restore the ≥6 floor. **H111** (maintain's default stale recheck ≡ `verify --stale-before <last-run>` convergence invariant) is the natural next pick: the verify-selection-family sibling of H106 (a maintain↔verify contract pinned in the convergence suite), no production change, preconditions all shipped (H83/H79/H81). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H111, H115, H117, H119, H121, H123. | maintenance |
| H123 | **✓ shipped. Per-source custody breakdown in the `scrolls maintain` report (`by_source`) (cap 1, maintenance framing).** `doctor`'s `custody` block carries a `by_source` map (H104 — the whole-library fidelity/drift aggregate split per source via `custody.custody_counts_by_source`), but the scheduled worker's own `maintain` report distilled only the whole-library `custody` snapshot (`custody_snapshot` drops `by_source`), so an unattended log could not name *which* source's custody is weakest without re-running `doctor`. Now the report carries a **`by_source`** member — the per-source breakdown the audit (`run_doctor`) **already produces**, threaded through a new pure `maintain.report_by_source(report)` (a faithful read of `report["custody"]["by_source"]`, honest empty map when absent — the degrade-safely posture; **no new ledger read**). Surfaced **live-pass only** (like `suggested` H40 and the recheck `scope`/`since` H83): derived fresh from this pass's audit, never recorded in the snapshot/log, so `--history`/`--trend` (which replay snapshots) carry none, and the stored `log.jsonl` stays bare. Sorted source keys; because each per-source group folds through the same `custody_counts`, it **sums to the `custody` block beside it by construction** (every item lands in exactly one source group — the H104 sum-to-whole posture on the maintenance surface, with the documented `verified ≡ unchanged`) and equals `custody_counts_by_source` over the post-maintenance held library. The aggregate H119's single-weakest-source `attention` selector derives *from* (so it sequences cleanly before H119). Report-only and idempotent. Verified live (a mixed `arxiv`/`web` library with a drifted + reference-only `web` item → `web` flagged weakest: `drift {unverified 1, drifted 1}` vs `arxiv {verified 1}`; sums to the whole block). Tests: `tests/test_maintain.py` (+6 — pure `report_by_source` faithful read + honest-empty; the report carries it == the audit's `by_source` with concrete per-source shape; sums-to-whole tiers+postures over a drifted-source mix == `custody_counts_by_source`; honest-empty for an uninitialized library; live-pass-only/absent-from-`--history`/log). Docs: `docs/cli.md` (maintain `by_source` paragraph + the console example now carries it), `docs/architecture.md` (Maintain prose + subsystem-index bullets). Full suite green (2893). Precondition: H104 (the `by_source` map, shipped); H34 (maintain, shipped). | → cap 1, maintenance framing |
| H124 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H111 shipped** (the scheduled-recheck ≡ explicit-recheck convergence invariant: a default `maintain` pass re-verifies exactly the set `verify --stale-before <last-run recorded_at>` selects, captured via a recording recapture stub and pinned in `tests/test_custody_convergence.py`, with the `--unverified`-subsumed and `--all`-escape-hatch relations and a two-level mutation check). With H111 shipped the un-started **work** slices fell to five (H115, H117, H119, H121, H123) — appended **H125** below to restore the ≥6 floor. **H123** (per-source custody breakdown in the `maintain` report) is the natural next pick: it surfaces on the scheduled surface the per-source aggregate `doctor` already produces (H104), reuses the shipped `custody.custody_counts_by_source`, and *sequences cleanly before* H119 (whose single-weakest-source `attention` selector derives from that map). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H115, H117, H119, H121, H123, H125. | maintenance |
| H125 | **✓ shipped. `scrolls verify --source <S>` — re-verify one source's held items (the act-side of the per-source custody breakdown) (cap 1, maintenance framing).** H104/H121/H123 make the audit name *which source's* custody is weakest (most drifted, least covered); H119 flags the single weakest in the `maintain` report. The missing piece was the *act*: re-verifying exactly that source without `--all` re-checking the whole library. Added `--source <S>` as a fifth `verify` batch selection (beside `--all`/`--unverified`/`--stale-before`/`--drift`, the exactly-one-selection validation extended to six options), re-capturing the held, hash-bearing items whose `source == S` — the verify-axis sibling of `list --source` and the actionable counterpart of `doctor`/`maintain`'s `by_source` weakest-source signal. **The selection is the one that reads no ledger:** a plain `[i for i in hash_bearing if i.source == S]` item-intrinsic filter (orthogonal to the posture/staleness selectors `unverified_items`/`items_in_posture`/`items_checked_before`), so it stays inlined in `cli.py` rather than added to `custody.py` (which is the verify-ledger module — `source` is an item property, not a ledger-derived one; this keeps `custody.py` focused). Composes with `--limit` (oldest saved first, like the other batch modes); a source nothing is held for is the honest empty no-op (never an error — a closed-vocabulary check is wrong here since sources are open-ended, the decision recorded against `--drift`'s closed `choices`). Live edge stays behind the `cli.live_recapture` seam (scripts offline). **MCP:** `verify`'s only twin is the *single-id* `verify_scroll(item_id)` — there is no batch MCP twin (the other batch selections are CLI-only too), so this is CLI-only by parity, no MCP change. Folded into the verify-selection-family invariant (`tests/test_custody_convergence.py`): the rows `verify --source S` re-captures equal `list --source S`'s held hash-bearing rows (the reference-only row is in the listing but never re-captured — the row-vs-recheck distinction), and re-verifying source S clears exactly that source's `unverified` count in `doctor`'s `custody.by_source[S]` (the two hash-bearing rows → `verified`, the reference-only one honestly *stays* `unverified`, `arxiv` untouched; per-source `coverage` tracks it 2-of-2). Verified live (`--source web` re-checks only web's items; `--source reddit` on an unheld source → empty no-op, exit 0). Tests: `tests/test_verify_cli.py` (+10 — selects only source S's hash-bearing items, ≡ `list --source` held hash-bearing rows, skips reference-only, `--limit` bound, unheld-source empty no-op, empty library, clears the source's `by_source` unverified bucket, three mutual-exclusion rejections incl. id+source), `tests/test_custody_convergence.py` (+2 — `verify --source` ≡ `list --source` held-set tie for every source + per-source bucket/coverage clears; `--source` added to the batch-skip-reference-only loop). Docs: `docs/cli.md` (verify heading + `--source` paragraph + console example + the doctor `by_source` cross-ref + the verify-selection-family invariant prose, now five selections), `docs/architecture.md` (verify bullet — the five selections, `--source` the one reading no ledger), README (cheatsheet). Full suite green (2930). Precondition: the shipped verify batch selections + the `source` field; H104 (the per-source breakdown the signal it acts on, shipped). | → cap 1, maintenance framing |
| H126 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16). Marked **H123 shipped** (the `maintain` report now carries a live-pass-only `by_source` member — the per-source custody breakdown the audit already produces, via the new pure `maintain.report_by_source`, summing to the whole-library `custody` block by construction and absent from `--history`/`--trend`, so an unattended log names which source's custody is weakest without re-running `doctor`). With H123 shipped the un-started **work** slices fell to five (H115, H117, H119, H121, H125) — appended **H127** below to restore the ≥6 floor. **H119** (`maintain` names the single weakest source in an `attention` block) is the natural next pick: H123 surfaces the whole per-source map, and H119 distils it to the one source worth flagging — a pure `weakest_source(by_source)` selector over the map H123 just added, no new ledger read. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H115, H117, H119, H121, H125, H127. | maintenance |
| H127 | **✓ shipped. Maintain `by_source` ≡ doctor `by_source` — a cross-surface convergence invariant (cap 1 + cap 7).** Pinned in `tests/test_custody_convergence.py` (`test_maintain_by_source_converges_with_doctor_by_source`): over the multi-source seed a `maintain --no-recheck` pass's report `by_source` == `doctor`'s `custody.by_source` == `custody_counts_by_source` over the held items == per-source `facets fidelity`/`drift --source <name>`. Mutation-checked non-vacuous — perturbing `custody_counts_by_source` fails the independent `facets` cross-check. Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" broadened); no production change. Full suite green (2933). H123 threads `doctor`'s `custody.by_source` into the `maintain` report and *claims* the two agree (one is a pure read of the other's audit), but that tie is pinned only obliquely (`test_maintain_report_carries_the_per_source_custody_breakdown` asserts the maintain member equals a *re-run* `run_doctor`'s map). Pin it as a first-class entry in `tests/test_custody_convergence.py` (its home for per-source convergence — already holding `test_doctor_by_source_converges_with_the_per_source_tally_and_facets`, H104): over the multi-source fidelity/drift seed, a `maintain --no-recheck` pass's report `by_source` equals `doctor`'s `custody.by_source` for the *same* post-maintenance library **and** `custody_counts_by_source` over the held items **and** (per source) `facets fidelity`/`drift --source <name>` — so the scheduled worker's per-source picture, the standalone audit, the canonical tally, and the browse facets are one number, asserted in one place. `--no-recheck` keeps the pass network-free and the ledger pristine so the maintain audit and a fresh `doctor` read the identical state. Mutation-checked non-vacuous (perturbing `custody_counts_by_source` fails it). The per-source-maintenance sibling of how H101 pinned the `stats.custody` family and H111 the stale-recheck convergence. Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" broadened); no production change. Precondition: H123 (shipped), H104 (shipped). | → cap 1, cap 7 |
| H128 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — not a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh is becoming due and should be the next maintenance checkpoint's task). Marked **H119 shipped** (the `maintain` report now carries an `attention` member — the single weakest source distilled from the H123 `by_source` map via the new pure `maintain.weakest_source`: most `drifted`+`rotted` loss, tie-broken by most `reference`-only then name, `{source, tiers, drift, reason}`; honest `null` on an empty, single-source, or fully-clean library; live-pass only like `by_source`, so `--history`/`--trend` carry none). With H119 shipped the un-started **work** slices fell to five (H115, H117, H121, H125, H127) — appended **H129** below to restore the ≥6 floor. **H125** (`scrolls verify --source <S>` — re-verify one source's held items) is the natural next pick: H104/H119/H123 make the audit *name* which source's custody is weakest (`attention` flags it), and H125 is the missing **act** — the way to re-verify exactly that source without `--all` re-checking the whole library (the verify-axis sibling of `list --source`, completing the per-source read→act loop). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H115, H117, H121, H125, H127, H129. | maintenance |
| H129 | **✓ shipped. `attention` ≡ doctor's max-loss source — the single-flag convergence invariant (cap 1 + cap 7).** Pinned in `tests/test_custody_convergence.py` (`test_maintain_attention_converges_with_doctors_max_loss_source` + `test_maintain_attention_is_null_when_no_source_carries_loss`): a `maintain --no-recheck` pass's `attention.source` == the source maximizing `drifted + rotted` in `doctor`'s `custody.by_source` (the literal `web` pick over the seed), and `attention` is honestly `null` exactly when no source carries actionable loss. Mutation-checked non-vacuous — perturbing `weakest_source`'s ranking key (least-loss) flips the pick to a clean source → `null`, failing the test. Test + docs only; no production change. Full suite green (2933). H119 distils `maintain`'s `attention` from the per-source `by_source` map and *claims* it flags the source doctor's audit would call weakest; H127 pins `maintain by_source ≡ doctor by_source`, but the *distillation* (which source `weakest_source` picks, and the honest-`null` gates) is pinned only in `test_maintain.py` against the report's own map. Pin it as a first-class entry in `tests/test_custody_convergence.py` (its home for per-source convergence, beside H127): over the multi-source fidelity/drift seed, a `maintain --no-recheck` pass's `attention.source` equals the source maximizing `drifted + rotted` in `doctor`'s `custody.by_source` (with the documented `reference`-then-name tie-break), and `attention` is `null` exactly when no source carries drift/rot (the fully-clean gate) — so the scheduled worker's single-source flag can never disagree with the standalone audit's per-source map. The `attention`-axis sibling of H127 (`by_source`) and H111 (stale-recheck). `--no-recheck` keeps the pass network-free and the ledger pristine so the maintain audit and a fresh `doctor` read the identical state; mutation-checked non-vacuous (perturbing `weakest_source`'s ranking key fails it). Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" broadened); no production change. Precondition: H119 (shipped), H127 (the `by_source` convergence home), H104/H123 (shipped). | → cap 1, cap 7 |
| H130 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh is now **overdue** and should be the next maintenance checkpoint's explicit task: mark all completed, prune slices overtaken by events, re-derive the 3-day/week plans, re-confirm the map to `docs/product/mvp.md`). Marked **H115 shipped** (the recheck `coverage` `{verified, total}` now rides the `maintain` snapshot/`log.jsonl`, `compute_delta` diffs it per-key, and `compute_trend` reports a `coverage_change` net-delta axis — deliberately kept out of the integrity-first `posture` — so `--history`/`--trend` replay the monotone-coverage progress H55/H83 drive; `status`'s custody block carries `coverage` for free as the convergence-by-construction consequence). With H115 shipped the un-started **work** slices fell to five (H117, H121, H125, H127, H129) — appended **H131** below to restore the ≥6 floor. **H117** (`scrolls status` carries the rendered one-line `headline` string — the status-surface completion H103 deferred) is the natural next pick: a small, shipped-primitive slice (`maintain.snapshot_headline` over the status snapshot) closing the one custody-headline-family surface still carrying only the structured block. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H117, H121, H125, H127, H129, H131. | maintenance |
| H131 | **✓ shipped. `maintain --trend` reports the enrichment/summary staleness trajectory (cap 1 + cap 8).** H115 added a `coverage_change` axis to `compute_trend`; the snapshot *also* records the cap-8 staleness scalars (`enrichment_stale`/`summaries_stale`, from doctor's `custody.enrichment`/`custody.summaries`, since H34), but the trend distilled only the integrity (`score`/drift) and coverage axes — a worker reading `--trend` could not see whether *enrichment/summary debt* is growing across runs without diffing entries. `compute_trend` (H46) now carries a **`stale_change`** member — `{enrichment, summaries}` net first→last deltas (Δ`enrichment_stale`, Δ`summaries_stale`) — so `--trend` answers "is re-derivable enrichment debt accumulating?" (positive = a `classify --stale`/`kb --stale` refresh is becoming overdue; negative = a refresh cleared it). A **separate reported axis kept out of `posture`**, exactly like H115's `coverage_change`: stale enrichment is a re-derivability signal (a superseded ruleset/changed members), not an integrity loss — the rules category and kb_llm summary are still *held*, so rising staleness means a refresh is due, not that custody regressed, and the integrity-first `posture` rule (H46) is unchanged. Honest absence: `<2` runs is a null `stale_change` (the H115 insufficient-history posture); a pre-stale-tracking endpoint reads 0 (missing-axis-zero, ADR 0082, via `snap.get(key, 0)`). Pure trend layer over the already-recorded snapshot scalars — **no new ledger read, no snapshot change** (the counts already ride `last-run.json`/`log.jsonl`). Verified live (`maintain --history --trend` carries `stale_change`; a real superseded-ruleset item across two offline passes moves `stale_change.enrichment` 0→1 while `posture` stays `holding`). Tests: `tests/test_maintain.py` (+5 — the `{enrichment, summaries}` net movement, posture-independence (a rising stale count alone is still `holding`), staleness clearing (negative delta), the `<2`-run null (folded into `test_trend_under_two_runs_is_not_a_trajectory`), a pre-tracking-endpoint zero, and an **end-to-end** stale-classification-accumulates-across-runs test exercising the real snapshot→log→trend chain). Docs: `docs/cli.md` (maintain `--trend` paragraph + the `--trend` example trend object — also brought the H115 `coverage_change` and the example snapshots' `coverage` current, a stale-since-H115 example fixed in the same blocks), `docs/architecture.md` (Maintain subsystem + subsystem-index bullets), `maintain.py` `compute_trend` docstring. Full suite green (2938). Precondition: H46 (`compute_trend`, shipped); H115 (the separate-reported-axis precedent, shipped); H25/H29 (the stale counts in the snapshot, shipped). | → cap 1, cap 8 |
| H132 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh is now **overdue** and should be the next maintenance checkpoint's explicit task: mark all completed, prune slices overtaken by events, **re-derive the stale Day-3 "Next:" pointer** (it still lists H39/H40/H55/H83/H94/H103/H104 as "next", all now shipped), re-derive the 3-day/week plans, re-confirm the map to `docs/product/mvp.md`). Marked **H117 shipped** (`scrolls status` now carries the rendered one-line `headline` string beside its structured `custody` snapshot block, via the shipped `maintain.snapshot_headline` — closing the one custody-headline-family surface that carried only the structured block; rendered from the same snapshot so the line, the block, and `custody_headline` over the held library converge by construction). With H117 shipped the un-started **work** slices fell to five (H121, H125, H127, H129, H131) — appended **H133** below to restore the ≥6 floor. **H121** (per-source recheck coverage in the `doctor` custody breakdown) is the natural next pick: it makes per-source custody actionable on the coverage axis (which source is *least covered*, the counterpart of H104's *most drifted*), a pure fold over the same ledger `_check_custody_drift` reads, reusing the shipped `custody.recheck_coverage` grouped by source — preconditions all shipped (H104, H113). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H121, H125, H127, H129, H131, H133. | maintenance |
| H133 | **✓ shipped. `scrolls status` carries the per-source custody breakdown (`by_source`) — the status-surface counterpart of H123 (cap 1 + cap 2).** `status` distilled only the flat whole-library `custody` snapshot (`custody_snapshot` drops `by_source`, exactly as `maintain` did before H123), so a human reading `status` could not see the per-source split without running `maintain`/`doctor`. `_cmd_status` now captures the `run_doctor(paths)` report it *already makes* for the custody headline into a variable and adds a **`by_source`** member — `maintain.report_by_source(report)`, a faithful read of `report["custody"]["by_source"]` (H104): **no new audit, no new ledger read**. Source keys sorted (`custody_counts_by_source` already returns them sorted); sums to the `custody` block beside it by construction (every item lands in exactly one source group, the H104 sum-to-whole posture) and equals `doctor`'s `custody.by_source`, `maintain --no-recheck`'s `by_source`, and `custody_counts_by_source` over the held library — so `status`, `maintain`, `doctor`, and `facets fidelity`/`drift --source <name>` read one number. Honest absence: an empty/uninitialized library is the empty `{}` map (`run_doctor` guards the missing store, `report_by_source` degrades to `{}`). CLI-only (`status` has no MCP twin). Verified live (before-init `{}`; a multi-source library's `status.by_source` byte-matches `doctor`'s). Tests: `tests/test_cli.py` (`test_status_by_source_breakdown_converges_with_doctor` — == `report_by_source(run_doctor)` / `doctor.custody.by_source` / `custody_counts_by_source`, sorted keys, concrete per-source picture, sums-to-whole; the two exact-shape `test_status_before_init`/`test_status_after_init` updated for the honest-empty `{}`), fold into `tests/test_custody_convergence.py` (`test_status_by_source_converges_with_maintain_and_doctor` — `status` ≡ `maintain` ≡ `doctor` ≡ canonical tally). Docs: `docs/cli.md` (status `by_source` paragraph + key-table row + after-init console example), `docs/architecture.md` (status bullet). Also corrected a stale H131 test citation to `test_trend_under_two_runs_is_not_a_trajectory` (the docs guardrail flagged the truncated form). Full suite green (2940). Precondition: H104 (the `by_source` map, shipped); H123 (the `report_by_source` faithful-read primitive, shipped); H38/H117 (the `status` custody block + headline, shipped). | → cap 1, cap 2 |
| H134 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh remains overdue and should be the next maintenance checkpoint's explicit task: mark all completed, prune slices overtaken by events, re-derive the 3-day/week plans). Marked **H121 shipped** (per-source recheck `coverage` now rides each `doctor`/`maintain` `by_source` entry via a coverage fold inside the **shared** `custody.custody_counts_by_source` — `{verified, total}` over that source's hash-bearing held items, summing to the whole-library `drift.coverage` by construction, reference-only items excluded from each source's denominator — so the audit names which source is least *covered*, not just most drifted; the shared-builder choice keeps `doctor`'s and `maintain`'s `by_source` convergent for free). **Refreshed the stale Day-3 "Next:" pointer** (it listed H39/H40/H55/H83/H94/H103/H104 — all shipped — as "next"; re-pointed at the live un-started per-source-custody set). With H121 shipped the un-started **work** slices fell to five (H125, H127, H129, H131, H133) — appended **H135** below to restore the ≥6 floor. **H125** (`scrolls verify --source <S>` — re-verify one source's held items) remains the natural next pick: it is the *act* completing the per-source read→act loop — H104/H119/H121/H123 name which source is weakest / least-covered, and H125 re-verifies exactly that source without `--all` re-checking the whole library. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H125, H127, H129, H131, H133, H135. | maintenance |
| H135 | **✓ shipped. Per-source enrichment staleness in the `doctor` custody breakdown (cap 8 + cap 1).** H104/H121 split the *custody* picture per source (`custody.by_source` → `{tiers, drift, coverage}`); the cap-8 *re-derivability* axes stayed whole-library only — `custody.enrichment.stale` (classifications produced under a superseded ruleset, H25) reported a single count, so an operator triaging *which* source carries the most stale-classification debt had to enumerate by hand. `doctor`'s `custody.enrichment` block now carries a **`by_source`** map — the re-derivability counterpart of H121's per-source coverage — naming which source has the most categories to refresh with `classify --stale`. A flat `{source: stale_count}` of the **offending sources only** (a clean source is omitted, the `items`-list posture; empty `{}` when nothing is stale), source keys sorted; every stale classification has exactly one source, so the per-source counts **sum to the whole-library `enrichment.stale` by construction** (the H104/H121 sum-to-whole posture on the enrichment axis). **Surface decision (resolved → Option B):** a `by_source` sub-map *under `custody.enrichment`*, **not** a member folded into `custody.by_source` — so the shared `custody.custody_counts_by_source` (and the `maintain` report that faithfully reads it, H123/H127) stay byte-identical, `weakest_source`/`attention` (which reads `by_source` tiers/drift) is unperturbed, and `custody.py` (the verify-ledger module) stays free of any classification coupling (the H125 layering posture). Computed inside `_check_enrichment_provenance` (where the stale items + their ids already are — group by `item.source`; **no new ledger/db read**). **The summary axis has no per-source counterpart (recorded decision):** a concept summary is keyed by *slug*, and a concept's members span sources, so a stale summary cannot be attributed to one source and summed to the whole without double-counting — which would break the sum-to-whole convergence the per-source breakdown rests on; the enrichment axis decomposes cleanly (per-item, one source, and `classify --stale` is the per-item refresh it points at) and the summary axis genuinely does not (`kb --stale` acts on concepts, not sources), so summary debt stays whole-library. Report-only (never feeds `issues`/the exit code). Verified live (`{'arxiv': 1, 'web': 2}`, sums to `stale`, a current-only source omitted, `issues: 0`). Tests: `tests/test_doctor.py` (+6 — empty-when-clean, empty-library, per-source grouping with sorted keys, sums-to-whole, clean-source-omitted, never-feeds-issues; the exact-shape custody-block assertion updated), `tests/test_custody_convergence.py` (+1 — `test_enrichment_by_source_converges_with_the_per_source_stale_classifications`: the per-source count == an independent `is_stale_classification` re-derivation grouped by source, sums-to-whole, clean source omitted). Docs: `docs/cli.md` (doctor enrichment `by_source` paragraph + the summary-axis decision), `docs/architecture.md` (Doctor bullet — the per-source re-derivability split + the summary-axis exclusion), `doctor.py` docstrings (both `_check_enrichment_provenance` and `_check_summary_provenance`). Full suite green (2947). Precondition: H25 (the stale-ruleset count, shipped); H104 (the per-source grouping pattern, shipped). | → cap 8, cap 1 |
| H136 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh is now **overdue** and should be the next maintenance checkpoint's explicit task: mark all completed, prune slices overtaken by events, re-derive the 3-day/week plans, re-confirm the map to `docs/product/mvp.md`). Marked **H125 shipped** (`scrolls verify --source <S>` — the fifth `verify` batch selection, re-capturing one source's held hash-bearing items as the act-side of `doctor`/`maintain`'s per-source `by_source` breakdown; the one selection reading no ledger, kept inlined in `cli.py`; CLI-only by parity since `verify`'s only MCP twin is single-id; folded into the verify-selection-family invariant — `verify --source S` ≡ `list --source S` held hash-bearing rows + clears the source's `by_source` unverified bucket). The **per-source custody read→act loop is now complete**: `doctor`/`facets`/`maintain` *name* which source is weakest (H104/H119/H121/H123), and `verify --source` is the *act* that re-verifies exactly it. With H125 shipped the un-started **work** slices fell to five (H127, H129, H131, H133, H135) — appended **H137** below to restore the ≥6 floor. **H127** (maintain `by_source` ≡ doctor `by_source` convergence invariant) is the natural next pick: a decision-grade test-only slice pinning H123's faithful-read claim against doctor's real audit, preconditions all shipped (H123/H104). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H127, H129, H131, H133, H135, H137. | maintenance |
| H137 | **✓ shipped. `maintain`'s `attention` block names the `verify --source <S>` recheck command (cap 1, maintenance framing).** H119 distils the single weakest source into the `maintain` report's `attention` member (`{source, tiers, drift, reason}` — the source with the most actionable `drifted`+`rotted` loss); H125 ships `scrolls verify --source <S>`, the *act* that re-verifies exactly that source. The missing bridge — an unattended worker reading `attention` had to *assemble* the recheck command by hand — is closed: the `attention` block now carries a **`command`** field, the literal `scrolls verify --source <attention.source>`, so the report names not just *which* source is weakest but the *exact* command to re-check it. **Placement decision (the slice's load-bearing one):** the command is a *recheck*, not a `doctor --fix` repair, so it rides `attention` beside the source it names, **not** the `suggested` block — pinned by an assertion that no `suggested` entry carries the `attention.command` (`test_maintain_report_flags_the_weakest_source`). Built inside `maintain.weakest_source` (where the source is known), so the `attention` block stays self-contained and `attention == weakest_source(by_source)` convergence is untouched; honest `null` exactly when `attention` is (the whole block absent — empty/single-source/fully-clean library); rides the **live pass only** like the rest of `attention` (never recorded in the snapshot/log, so `--history`/`--trend` carry none — the existing `test_maintain_history_does_not_carry_attention` subsumes the command). **Decision resolved:** only the single weakest `attention` source carries a command (not a `command` per `by_source` entry) — the block's whole purpose is to distil to the one worth acting on; a per-entry command is browse-noise the operator can derive from the source key. Pure derivation over the already-computed `attention.source` (no new audit/ledger read); source slugs are single tokens, so no shell-quoting (matches the `suggested` command form). Tests: `tests/test_maintain.py` (+1 pure: the command names the weakest source; updated `test_maintain_report_flags_the_weakest_source` to assert the live command + the not-in-`suggested` placement), `tests/test_custody_convergence.py` (the command names doctor's max-loss source — folded into the H129 `attention` doctor-tie). Docs: `docs/cli.md` (maintain `attention` paragraph + console example), `docs/architecture.md` (both Maintain bullets). Full suite green (2948). Precondition: H119 (the `attention` block, shipped); H125 (the `verify --source` command it names, shipped). | → cap 1, maintenance framing |
| H138 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh is now **overdue** and should be the next maintenance checkpoint's explicit task: mark all completed, prune slices overtaken by events, re-derive the 3-day/week plans, re-confirm the map to `docs/product/mvp.md`). Marked **H127 + H129 shipped** in one run (the per-source `maintain`↔`doctor` convergence pair: a `maintain --no-recheck` report's `by_source` ≡ `doctor`'s `custody.by_source` ≡ `custody_counts_by_source` ≡ per-source `facets`, and its single-source `attention.source` ≡ doctor's max-loss source with the honest-`null` gate — both first-class entries in `tests/test_custody_convergence.py`, mutation-checked non-vacuous). The **per-source custody theme is now complete on the read/act/report/convergence axes**: `doctor`/`facets`/`maintain` name the weakest source (H104/H119/H121/H123), `verify --source` re-verifies it (H125), and the scheduled report can never silently desync from the audit (H127/H129). Two slices shipped in one run dropped the un-started **work** slots to four (H131, H133, H135, H137) — appended **H139 + H141** below to restore the ≥6 floor. **H131** (`maintain --trend` enrichment/summary staleness trajectory) is the natural next pick: a pure `compute_trend` layer over the already-recorded snapshot scalars, preconditions all shipped (H46/H115/H25/H29). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H131, H133, H135, H137, H139, H141. | maintenance |
| H139 | **✓ shipped. `scrolls status` carries the single weakest-source `attention` flag — the status-surface counterpart of H119 (cap 1 + cap 2).** `_cmd_status` now distils the per-source `by_source` map it already reads into an **`attention`** member via the shared `maintain.weakest_source` primitive `maintain`'s own `attention` uses — `{source, tiers, drift, reason, command}` (the most `drifted`+`rotted` loss, with the `scrolls verify --source <S>` recheck command, H137), or honest `null` on the same three gates `maintain` uses (empty / single-source / fully-clean). Derived from the audit `status` *already makes* (**no new ledger read** — `report_by_source(report)` is now computed once and threaded to both members), so the flag names `doctor`'s max-loss source and equals `maintain`'s `attention` by construction. CLI-only (`status` has no MCP twin). Verified live: `status.attention` is byte-identical to `maintain --no-recheck`'s over the same multi-source drifted library. Tests: `tests/test_cli.py` (`test_status_attention_names_the_weakest_source` — names the weakest source + tally + reason + command, == `weakest_source(report_by_source(run_doctor))`; `test_status_attention_is_null_when_nothing_stands_out` — single-source-with-drift → null; the two exact-shape `test_status_before_init`/`test_status_after_init` extended with `attention: null`), folded into `tests/test_custody_convergence.py` (`test_status_attention_converges_with_maintain_and_doctor` ≡ the H129 sibling on the status surface, `test_status_attention_is_null_with_no_cross_source_loss`). Docs: `docs/cli.md` (status `attention` paragraph + key-table row + both console examples + the populated-form note), `docs/architecture.md` (status bullet), convergence module docstring. Full suite green (2952). Precondition: H119 (`weakest_source`, shipped); H38/H117 (the `status` custody block + headline, shipped). | → cap 1, cap 2 |
| H140 | **Buffer refresh checkpoint** (maintenance rule). The full re-derivation is **overdue** (last full refresh H102, 2026-06-16): mark all completed, prune slices overtaken by events, re-derive the stale Day-3 "Next:" pointer and the 3-day/week plans, re-confirm the map to `docs/product/mvp.md`, keep ≥6 un-started work slots. Consider the deferred bi-temporal drift framing only if an agent workflow has shown the event record insufficient (else keep deferred). | maintenance |
| H141 | **✓ shipped. Per-source custody breakdown in the `export bundle` briefing headline (cap 9 + cap 1).** H45 put a *whole-scope* custody headline at the top of the briefing; a multi-source shared briefing named no per-source split, so a recipient could not see *which* source's custody is weakest within the scope without re-deriving it. Now a **per-source breakdown** rides under the scope headline — `_By source:_` then one bullet per source (`- ``<source>`` — N scroll(s) · fidelity … · drift …`), the per-source counterpart of the scope headline. Built from the shared `custody.custody_counts_by_source` over the bundle's gathered scope (the same `get_fidelity` + `drift_posture` over the one `latest_events` read `build_bundle` already does), so it **sums to the scope headline by construction** and equals `doctor`'s `custody.by_source` for the in-bundle items (`test_bundle_per_source_breakdown_converges_with_doctor_by_source`). A *derived read view outside* the `@generated` JSONL fence, so the lossless round-trip is untouched (`test_per_source_breakdown_preserves_the_round_trip`). Renders only when ≥2 sources are in scope; a single-source/empty scope omits it (the whole-scope headline already says everything). **HTML parity:** `build_bundle_html` carries the same breakdown (`_by_source_html`) from the *same* structured `custody.custody_source_breakdown` the Markdown `render_custody_by_source` renders, so the two forms cannot desync (the H45 shared-primitive posture). New `custody.custody_sections` (extracted from `render_custody_headline` — the non-zero fidelity/drift sections both surfaces fold) + `custody_source_breakdown` (structured) + `render_custody_by_source` (Markdown), the renderer **H145 (`index.md`)/H149 (`context`) reuse** so the per-source line reads identically everywhere. Tests: `tests/test_bundle.py` (+7 — Markdown breakdown over a multi-source fidelity/drift scope, converges with `custody_counts_by_source` + sums-to-headline, single-source omission, empty-scope no-op, round-trip preserved, HTML parity + HTML single-source omission), `tests/test_custody_convergence.py` (+1 — the bundle line ≡ `render_custody_by_source(doctor.custody.by_source)`, non-vacuous). Docs: `docs/cli.md` (export bundle briefing + `--format html` list), `docs/architecture.md` (export-bundle bullet — the shared per-source renderer), `bundle.py` module docstring. Full suite green (2960). Precondition: H45 (the scope headline, shipped); H104 (`custody_counts_by_source`, shipped); H39 (the HTML briefing, shipped). | → cap 9, cap 1 |
| H142 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh remains **overdue** and is still the next dedicated checkpoint's explicit task, H140). Marked **H131 shipped** (`maintain --trend` now reports a `stale_change` member — `{enrichment, summaries}` net first→last deltas — beside `coverage_change`, a separate reported axis kept out of the integrity-first `posture`, so a worker reading the trend sees whether re-derivable enrichment/summary debt is accumulating; pure `compute_trend` layer over the already-recorded snapshot scalars, verified end-to-end). **Refreshed the stale Day-3 "Next:" pointer** (it listed H131 as the head of the live un-started set, now shipped). With H131 shipped the un-started **work** slices fell to five (H133, H135, H137, H139, H141) — appended **H143** below to restore the ≥6 floor. **H133** (`scrolls status` carries the per-source `by_source` breakdown — the status-surface counterpart of H123) is the natural next pick: it surfaces on `status` the per-source map the `run_doctor(paths)` call it *already makes* produces, reusing the shipped `maintain.report_by_source` (no new audit/ledger read), and folds into the `status`/`maintain`/`doctor` per-source convergence family — preconditions all shipped (H104/H123/H38/H117). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H133, H135, H137, H139, H141, H143. | maintenance |
| H143 | **✓ shipped. Trend-axis ≡ telescoped per-run delta convergence invariant (cap 1 + cap 7).** `compute_trend` (H46/H115/H131) reports the net first→last movement on three axes — `drift_change`, `coverage_change`, `stale_change` — by reading only the window's *endpoints* (`runs[0]`/`runs[-1]`), while `compute_delta` (H34) records the *per-run* before/after/change between consecutive snapshots. The load-bearing property neither layer pins is that **the trend's net change equals the telescoped sum of the per-run deltas across the window** — i.e. the endpoint-only aggregation and the recorded step-by-step deltas agree, so the trend can never silently disagree with the history a worker also reads. Pin it in `tests/test_custody_convergence.py` (its home for cross-surface custody convergence, beside the H111 stale-recheck and H127/H129 per-source ties): over a multi-run window (a real sequence of recorded snapshots, or hand-built run dicts) assert `trend["drift_change"]` == Σ consecutive `compute_delta` drift changes (drifted+rotted), `trend["coverage_change"][k]` == Σ the per-run coverage changes, `trend["stale_change"][k]` == Σ the per-run `enrichment_stale`/`summaries_stale` changes, and `trend["score"]["change"]` == last−first (the telescoping identity for scalars). Cover the honest-absence edges (a `None`-score endpoint → null trend change but the deltas still telescope on the count axes; a `<2`-run window has no trajectory). Mutation-checked non-vacuous (perturbing one endpoint in `compute_trend` breaks the sum). Makes "the trajectory and the step-by-step history are the same numbers" a tested contract — the trend-layer analogue of the per-surface convergence invariants. Test + docs only (the convergence module docstring + `docs/cli.md` "A sibling invariant" broadened); no production change. **Done:** `tests/test_custody_convergence.py` (+5 — the core identity on every axis over a non-monotone 4-run window, the recorded-`delta` form telescoped back through the real `append_log_entry`/`read_log` path, the `None`-score endpoint and `<2`-run honest-absence edges, and the mutation-check that perturbing one endpoint moves trend and telescope together). Mutation-verified non-vacuous: rewriting `compute_trend`'s `runs[-1]` endpoint to `runs[-2]` fails both the identity and the recorded-history tests. Docstrings broadened in both homes. Full suite green. Precondition: H131 (the third trend axis, shipped); H115 (`coverage_change`), H46 (`compute_trend`), H34 (`compute_delta`) — all shipped. | → cap 1, cap 7 |
| H144 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh remains **overdue** and is still the next dedicated checkpoint's explicit task, H140). Marked **H133 shipped** (`scrolls status` now carries the per-source `by_source` custody breakdown — a faithful `report_by_source` read of the `run_doctor` map it already computes for the headline, sorted keys, sums to the `custody` block, ≡ `maintain`/`doctor`/`custody_counts_by_source`; CLI-only; honest-empty `{}` before init; folded into the per-source convergence invariant). **Refreshed the stale Day-3 "Next:" pointer** (it listed H133 as the head of the live un-started set, now shipped). With H133 shipped the un-started **work** slices fell to five (H135, H137, H139, H141, H143) — appended **H145** below to restore the ≥6 floor. **H135** (per-source enrichment/summary staleness in the `doctor` custody breakdown) is the natural next pick: the re-derivability counterpart of H121's per-source coverage — a pure grouping over the stale items `doctor`'s `_check_enrichment_provenance`/`_check_summary_provenance` already compute, so the per-source breakdown names which source is most stale on *every* custody axis; preconditions all shipped (H25/H29/H104). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H135, H137, H139, H141, H143, H145. | maintenance |
| H145 | **✓ shipped. Per-source custody breakdown on the compiled `library/index.md` landing page (cap 1 + cap 2).** H96 put a whole-library `_Custody:_` headline in the compiled `index.md` header; H133/H141 carry the per-source `by_source` split on JSON `status` and the `export bundle` briefing — but the human-readable compiled landing page named no per-source split. Now the landing `index.md` follows its headline with a **`_By source:_` breakdown** — one bullet per source naming that source's fidelity tiers and drift postures — through the **same shared `custody.render_custody_by_source`** (over `custody_counts_by_source(items, verdicts)`) the `export bundle` briefing (H141) uses, so the compiled landing page, the shareable briefing, and JSON `status` (H133) read **byte-identical** per-source lines. Scoped to the **rendered** library the index heads (the same `items` its headline + count line cover, one `latest_events` read per compile), so the bullets sum to the headline by construction (every scroll lands in exactly one source group) and equal `doctor`'s `custody.by_source` over the rendered scope. Written **inside** the `@generated` sentinel fence (ADR 0102) like the rest of the compiled header, so a re-compile refreshes it (after a re-verify moves a posture) and a hand annotation outside the fence survives; a **single-source/empty** library omits the split (the helper's `<2`-source no-op — the whole-library headline already says everything). **Rendering decision (surfaced):** the helper's trailing spacer line is dropped on the index (`by_source_lines[:-1]`) because a breakdown is present only with ≥2 sources, so `## Sources` always follows and supplies the separator — keeping the compiled header a single clean blank between sections, while the `_By source:_` *bullets* stay byte-identical for the H151 cross-surface invariant. Verified live (a 3-source library renders sorted `arxiv`/`web`/`youtube` bullets summing to the headline). Tests: `tests/test_kb.py` (+4 — the breakdown == the shared primitive over the rendered library, sums-to-the-headline, single-source omission, refresh-safe inside the fence), `tests/test_custody_convergence.py` (+1 — `test_compiled_index_per_source_breakdown_converges_with_doctor_by_source`: the compiled bullets == `render_custody_by_source` over both `doctor.custody.by_source` and `custody_counts_by_source`, under the headline — the compiled-surface counterpart of the bundle tie). Docs: `docs/library-format.md` (index prose + the `example-kb-index` pinned block now carries the breakdown), `docs/architecture.md` (KB-compiler bullet), convergence-module docstring broadened. Full suite green (2965). Precondition: H96 (the index custody headline, shipped); H104 (`custody_counts_by_source`, shipped); H141 (the shared `render_custody_by_source`, shipped). | → cap 1, cap 2 |
| H146 | **✓ executed (2026-06-17).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh remains **overdue** and is still the next dedicated checkpoint's explicit task, H140). Marked **H135 shipped** (the `doctor` `custody.enrichment` block now carries a per-source `by_source` map — a flat `{source: stale_count}` of the offending sources only, the re-derivability counterpart of H121's per-source coverage, summing to the whole-library `enrichment.stale` by construction; resolved the surface decision in favor of Option B — a sub-map *under* `custody.enrichment`, not folded into `custody.by_source` — so the shared `custody_counts_by_source`/`maintain`/`attention` stay untouched and `custody.py` stays free of classification coupling; and **recorded the decision that the summary axis has no per-source counterpart** because a concept summary spans sources and would double-count). With H135 shipped the un-started **work** slices fell to five (H137, H139, H141, H143, H145) — appended **H147** below to restore the ≥6 floor. **H137** (`maintain`'s `attention` block names the `verify --source <S>` recheck command) is the natural next pick: a pure derivation over the already-computed `attention.source` (no new audit/ledger read) bridging the per-source read→act loop H125 completed, preconditions all shipped (H119/H125). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H137, H139, H141, H143, H145, H147. | maintenance |
| H147 | **✓ shipped. Per-source enrichment staleness in the `scrolls maintain` report (cap 8 + cap 1, maintenance framing).** H135 put the per-source stale-classification map on the *audit* (`doctor`'s `custody.enrichment.by_source`); the scheduled worker's own `maintain` report distilled only the whole-library snapshot scalar (`enrichment_stale`), so an unattended log could not name *which* source's `classify --stale` to run without re-running `doctor`. Now the report carries an **`enrichment_by_source`** member — the maintenance-surface counterpart of H135, exactly as H123 surfaced `doctor`'s `custody.by_source` on `maintain`: a **faithful read** of `report["custody"]["enrichment"]["by_source"]` the audit already produces (new pure `maintain.report_enrichment_by_source(report)` mirroring `report_by_source`; **no new ledger/db read**). Resolved the surface decision in favor of a **standalone member** (not nested in the drift `by_source` entries), so the H123 `by_source` ≡ `custody_counts_by_source` byte-identity and the H127 convergence stay untouched — mirroring how H135 kept it out of `custody.by_source`. Surfaced **live-pass only** like `by_source`/`attention`/`suggested` (derived fresh from this pass's audit, never recorded in the snapshot/log, so `--history`/`--trend` carry none); a flat `{source: stale_count}` of the offending sources only (sorted keys); sums to the report's whole-library `enrichment_stale` by construction (every stale item has exactly one source). Honest empty `{}` when no source carries stale debt and before init (degrade-safely posture). Report-only and idempotent. Verified live (multi-source seed → `{"arxiv": 1, "web": 2}`, sums to `enrichment_stale` 3; `--history` carries none; uninitialized → `{}`). Tests: `tests/test_maintain.py` (+7 — pure faithful read + empty map, the member == the audit's `custody.enrichment.by_source` with a concrete `{arxiv:1, web:2}` shape, sums-to-whole, clean-library/uninitialized honest-empty, live-pass-only/absent-from-`--history`/log); folded the maintain↔doctor enrichment-by_source tie into `tests/test_custody_convergence.py` (extended the H135 enrichment-by_source test with the H127-sibling §3 on the enrichment axis). Docs: `docs/cli.md` (maintain `enrichment_by_source` paragraph + both console examples), `docs/architecture.md` (Maintain bullet). Full suite green (2977). Precondition: H135 (the audit's per-source enrichment map, shipped); H123 (the `report_by_source` faithful-read pattern, shipped). | → cap 8, cap 1 |
| H148 | **✓ executed (2026-06-18). Buffer refresh** (maintenance rule). Marked **H147 + H149 shipped** (per-source enrichment staleness in the `maintain` report; the per-source `_By source:_` custody breakdown on the `scrolls context` model-facing bundle — the readable per-source line now reads byte-identical across `status`/`export bundle`/compiled `index.md`/`context`). **Re-derived the stale Day-3 "Next:" pointer** (it still listed H147/H149 as live; now points at the live un-started set). **Prune:** scanned the table — no work slices are overtaken by events; the per-source custody theme (H104→H153) is still the coherent live block and every queued slice (H150–H153) still has a concrete path and PRD map. **Week plan re-confirmed** against `docs/product/mvp.md` (custody-deepening + portability, no new adapters — still accurate). Appended **H154** (`classify --stale --source <S>` — the enrichment-axis per-source refresh, sibling of `verify --source` H125) and **H155** (per-source `by_source` member on the browse-stats `stats.custody` envelopes) to restore the ≥6 floor. The **full re-derivation of the 3-day narrative** (it has accreted into a backward-looking changelog, against maintenance-rule §4) stays the **next dedicated checkpoint's task (H156)** — the day/week plans are still in-horizon (through 2026-06-19 / 2026-06-23) and map correctly, so a full rewrite was not blocking. Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). ≥6 un-started work slots now: H150, H151, H152, H153, H154, H155. | maintenance |
| H149 | **✓ shipped. Per-source custody breakdown on the `scrolls context` model-facing bundle (cap 1 + cap 10).** H47 put a *whole-scope* `_Custody:_` headline on the `context` bundle (the one an agent drops into its window); H133/H141/H145 carry the per-source `by_source` split on JSON `status`, the `export bundle` briefing, and the compiled `library/index.md` — but the model-facing `context` bundle named no per-source split, so an agent saw how custody stands across the whole scope but not *which source in the bundle* is weakest without re-deriving it. Now a **`_By source:_` breakdown** rides under the `context` `_Custody:_` headline through the **same shared `custody.render_custody_by_source`** (over `custody_counts_by_source` of the in-bundle scrolls — the one `latest_events` read the scope headline already does) the `export bundle` briefing (H141) and compiled `index.md` (H145) use, so the per-source line reads **byte-identical** across the three readable surfaces, sums to the scope headline by construction, and equals `doctor`'s `custody.by_source` for the same scope. The decision the slice's draft flagged was already resolved by H141/H145: the renderer lives in `custody.py` (`render_custody_by_source`), so context just adds the one line beside the headline — no new derivation, no `context → bundle` coupling. Gated to `connected`/`full` like the scope headline (H47) so the leanest `index` tier stays a bare catalog; a single-source/empty scope omits the split (the helper's `<2`-source no-op — the whole-scope headline already says everything); CLI + MCP twins share `build_context`, so the line rides MCP for free. Verified live (a 3-source `arxiv`/`web` bundle renders sorted bullets summing to the headline). Tests: `tests/test_context.py` (+7 — the breakdown over a multi-source fidelity/drift bundle, sums-to-the-scope-headline + shared-primitive convergence, `connected`-up presence, `index`-gating, single-source omission, empty-scope no-op, MCP parity), `tests/test_custody_convergence.py` (+1 — `test_context_per_source_breakdown_converges_with_doctor_by_source`: the context bullets == `render_custody_by_source` over both `doctor.custody.by_source` and `custody_counts_by_source`, the model-facing-surface counterpart of the bundle/compiled ties). Docs: `docs/cli.md` (context budget section — the `_By source:_` paragraph + a multi-source console example), `docs/architecture.md` (context-bundle bullet), `context.py` docstring, convergence-module docstring broadened. Full suite green (2985). Precondition: H47 (the context scope headline, shipped); H104 (`custody_counts_by_source`, shipped); H141 (the shared `render_custody_by_source`, shipped). | → cap 1, cap 10 |
| H150 | **✓ shipped. Per-source custody breakdown on the `scrolls graph` stats block (`stats.custody.by_source`) (cap 1 + cap 2).** H52 put a whole-scope `custody` member (`{tiers, drift}`) on the `graph` `stats` block; H133/H141/H145/H149 carry the per-source `by_source` split on JSON `status`, the `export bundle` briefing, the compiled `library/index.md`, and the `context` bundle — but the `graph` stats block named no per-source split, so a reader of the link graph saw how custody stands across the resolved library but not *which source* is weakest without dropping to `status`/`doctor`. Now a `by_source` member rides under `stats.custody` — the shared `custody.custody_counts_by_source` over the same in-scope `stats.items` the whole-scope `custody` tally already folds (the `Graph` dataclass retains the in-scope items after H52; the one `latest_events` read the CLI/MCP callers already pass for the tally feeds it — `to_payload` computes the scope once and threads it through both `custody_counts` and `custody_counts_by_source`, staying pure). A `{source: {tiers, drift, coverage}}` map (sorted keys): it **sums to the whole `stats.custody` block** beside it by construction (the H104 sum-to-whole posture, per source — every item lands in exactly one source group) and **equals `doctor`'s `custody.by_source` / `status`'s `by_source`** over the same whole-library scope, independent of `--all` (it counts the resolved library, not just the connected nodes — like the existing graph custody member). JSON, not a Markdown headline (graph emits JSON), so it carries the count maps directly (`verified` ≡ ledger `unchanged`). CLI + MCP twins share `to_payload`, so the member rides MCP for free; an empty graph → the honest empty `{}` map. **Decision (surfaced):** `by_source` lives *under* `stats.custody` (not a sibling), so the H101 per-item-fold invariant — `stats.custody == tally over nodes` — was relaxed to compare the `tiers`/`drift` axes the per-item fold produces, with the per-source split tied separately in the H150 convergence test (the per-item fold has no per-source axis). Verified live (a 3-source `arxiv`/`web` graph renders sorted bullets summing to the headline; `--all`-independent). Tests: `tests/test_graph.py` (+4 — per-source split over a multi-source fidelity/drift graph, sums-to-`stats.custody`, `--all`-independence, `doctor` convergence; updated exact-shape `stats` assertions across `test_graph.py`/`test_mcp.py` and the `_all_reference_unverified` helper now takes a per-source count map), `tests/test_custody_convergence.py` (+1 — `test_graph_by_source_converges_with_doctor_and_the_per_source_tally`: the graph block == `doctor.custody.by_source` == JSON `status`'s `by_source` == `custody_counts_by_source` over the held items, summing to its own whole `custody` block — the graph-surface counterpart of the H133 status tie; the H101 fold relaxed as above). Docs: `docs/cli.md` (graph `stats.custody.by_source` paragraph + multi-source example), `docs/architecture.md` (custody-headline family — the graph member), `graph.py`/`mcp_server.py` docstrings, convergence-module docstring broadened. Full suite green (2990). Precondition: H52 (the graph `stats.custody` member + the retained in-scope items, shipped); H104 (`custody_counts_by_source`, shipped). | → cap 1, cap 2 |
| H152 | **✓ shipped (2026-06-18). Per-source custody breakdown on the compiled multi-source group list pages — `categories/`, `concepts/`, `tags/` (cap 1 + cap 2).** H145 put a `_By source:_` breakdown under the landing `index.md` headline; the compiled **group list pages** carried a scope `_Custody:_` headline (H95) but no per-source split — yet a `categories/<cat>.md`, `concepts/<slug>.md`, or `tags/<tag>.md` page can span several sources (a `ml` category over `arxiv`+`web`), so a reader browsing a multi-source group page saw how custody stands across it but not *which source* is weakest. `kb._write_page` now emits the same `_By source:_` breakdown under each **multi-source** group page's headline, through the shared `custody.render_custody_by_source` over `custody_counts_by_source(members, verdicts)` (the page's members, the one `latest_events` read the compile already does — `compile_kb` reads the ledger once and threads `verdicts` to `_write_page`), so it sums to that page's headline by construction and equals `doctor`'s `custody.by_source` for the page's scope. **Decision (resolved as the slice planned):** the multi-source gate is the helper's natural `<2`-source no-op — **no special-casing `sources/`** — so the rule is uniform (**≥2 sources ⟹ a split**): every `sources/*.md` page (always one source) and any single-source category/tag omits it, a multi-source category/concept/tag shows it. The breakdown is appended right after the `[headline, ""]` so its trailing spacer separates it from the first item bullet (both the consolidated-work and singleton bodies follow); inside the `@generated` fence (refresh-safe — a re-verify refreshes the bullet on recompile, an annotation outside survives). Verified live via `test_docs.py` (the `example-category-consolidation` fixture — `paper` over `arxiv`+`crossref` — now renders the breakdown, the pinned block updated to byte-match real compiler output). Tests: `tests/test_kb.py` (+5 — the breakdown on a multi-source category page byte-identical to `render_custody_by_source` + sitting under the headline, sums-to-the-page-headline, `sources/*.md` single-source omission, single-source-category omission, refresh-safe; the `_category_bullets` helper now excludes the backtick-wrapped breakdown bullets), `tests/test_custody_convergence.py` (+1 — `test_compiled_group_page_per_source_breakdown_converges_with_doctor_by_source`: a multi-source category whose scope == the whole rendered library, the compiled group-page bullets == `render_custody_by_source` over both `doctor.custody.by_source` and `custody_counts_by_source`, under the page headline, the `sources/*.md` page omitting it). Docs: `docs/library-format.md` (group-page section — the `_By source:_` paragraph + the `example-category-consolidation` pinned block now multi-source), `docs/architecture.md` (KB-compiler bullet — the group-page breakdown beside the index one), convergence-module docstring broadened. Full suite green (2996). Precondition: H145 (the index breakdown + the shared renderer wiring, shipped); H95 (the group-page headline, shipped); H104 (`custody_counts_by_source`, shipped). | → cap 1, cap 2 |
| H151 | **✓ shipped (2026-06-18). Per-source custody breakdown rendered-line cross-surface invariant (cap 1 + cap 7).** The four per-surface ties (bundle briefing, `context`, compiled `index.md`, compiled group page → `doctor`'s `custody.by_source`) are consolidated into one byte-identity invariant in `tests/test_custody_convergence.py` (`test_readable_per_source_breakdown_is_byte_identical_across_surfaces`). Over one multi-source seed (`_seed_unified_per_source_fixture`: web 3 + arxiv 2, all rendered, all category `ml`, every title "topic", so all five readable surfaces scope to the whole library) it asserts the `export bundle` briefing, **its HTML form** (the `<li>` parsed back to the same Markdown bullet via `_html_by_source_bullets`), the `context` bundle (`connected`+), the compiled `index.md`, and the compiled `categories/ml.md` group page all render **byte-identical** `_By source:_` bullets — each equal to `render_custody_by_source` over *both* `custody_counts_by_source` and `doctor`'s `custody.by_source` — and that each surface's bullets sum to its own scope headline (`_sum_by_source_bullets` ≡ the parsed `_library_headline`) and to `doctor`'s whole-library custody (the module spine). The readable-line analogue of the JSON `by_source` convergence (`status`/`graph`/`doctor`, H133/H150; H157 consolidates the JSON axis). Non-vacuous (the two sources carry different tier/drift mixes — the bullets differ) and mutation-checked (perturbing the HTML renderer's per-source count breaks the tie). Test + docs only (convergence module docstring + `docs/cli.md` "A sibling invariant" broadened with the H151 readable-breakdown paragraph); no production change. Full convergence module green (45). Precondition: H141/H145/H149/H152 (all shipped). | → cap 1, cap 7 |
| H153 | **✓ shipped (2026-06-18). Per-source `coverage` rides the weakest-source `attention` flag (cap 1, maintenance framing).** `maintain.weakest_source` now returns `{source, tiers, drift, coverage, reason, command}` — the flagged source's recheck `coverage` (`{verified, total}`, H121) rides along beside its `tiers`/`drift`, a pure read of the same tally (**no new ledger read**, `.get`-degrade-safe to the honest `{0,0}` fraction), so it equals `doctor`'s `custody.by_source[<source>].coverage` for the flagged source by construction. Because both `maintain` and `status` distil the one `weakest_source` primitive (H139 parity), the coverage rides both surfaces' `attention` identically — so the flag names the source, the loss, the act, *and* how much of the weak source is even checked, in one place. Honest absence unchanged (`null` on empty/single-source/fully-clean). Tests: `tests/test_maintain.py` (dedicated `coverage`-on-flag unit test + degrade-to-`{0,0}`, plus the live-pass `attention.coverage == report.by_source[<S>].coverage` tie), `tests/test_custody_convergence.py` (folded into the H129 maintain + H139 status `attention` ties — `attention.coverage` == `doctor`'s per-source coverage). Docs: `docs/cli.md` (maintain + status `attention` paragraphs, table row, maintain example JSON), `docs/architecture.md` (both weakest-source bullets). Full suite green (2999). Precondition: H119/H137 (the flag + command, shipped), H121 (per-source coverage, shipped). | → cap 1, maintenance framing | and already carries that source's own `tiers`/`drift` tally + the `scrolls verify --source <S>` recheck command (H137), but it **drops** the per-source recheck `coverage` (`{verified, total}`) the same tally carries (H121, folded in by `custody_counts_by_source`). So an unattended worker reads "source `web` is weakest — 2 drifted" and the exact recheck command, but not *how much of that weak source is even checked* — the `coverage` figure that tells it whether the drift is the whole story or just the verified slice of a barely-covered source. Thread the flagged source's `coverage` into the `attention` dict (a pure read of the tally `weakest_source` already receives — **no new ledger read**, mirroring how `tiers`/`drift` ride along), so the flag names the source, the loss, the act, *and* the coverage in one place. Rides `maintain`'s and `status`'s `attention` identically (both call the one `weakest_source` primitive — the H139 parity), and converges with `doctor`'s `custody.by_source[<source>].coverage` for the flagged source by construction (it *is* that value). Honest absence unchanged: `attention` stays `null` exactly when no source carries actionable loss (a single source, an empty/clean library). Tests: `tests/test_maintain.py` (the `coverage` key on the flag == the flagged source's `by_source` coverage, present whenever `attention` is), `tests/test_custody_convergence.py` (fold into the H129/H139 `attention` ties — `attention.coverage` == `doctor`'s per-source coverage for that source). Docs: `docs/cli.md` (maintain/status `attention` paragraph), `docs/architecture.md` (weakest-source bullet). Precondition: H119/H137 (the flag + command, shipped), H121 (per-source coverage, shipped). | → cap 1, maintenance framing |
| H154 | **✓ shipped (2026-06-18). `scrolls classify --stale --source <S>` — the enrichment-axis per-source refresh (cap 8, maintenance framing).** A new pure selector `classify.stale_classifications(items, source=None)` is the one home behind both whole-library `--stale` and its per-source narrowing: it filters the `is_stale_classification` set (the predicate `doctor`'s stale report already shares) by `item.source == S`. `classify --stale --source <S>` re-runs the rules engine over exactly that source's stale items — so a worker triaging "source `web` carries 2 stale categories" (`doctor`'s `custody.enrichment.by_source`) refreshes just those. The convergence holds by construction: the count it refreshes equals `doctor`'s `custody.enrichment.by_source[S]`, and the refresh restamps each to the live ruleset, clearing that source's entry from the offenders-only map (verified end-to-end: `{arxiv:1, wikipedia:2}` → refresh `wikipedia` → `{arxiv:1}`). `--source` is a *narrowing* of `--stale`, not a standalone selection (`--source` without `--stale` is a loud usage error); rules-only (the existing `--engine llm` reject covers it); a source with no stale debt is the honest network-free no-op. The summary axis has no per-source counterpart (a concept summary spans sources, so `kb --stale` stays whole-library), so this completes the per-source refresh on the one enrichment axis that decomposes. `classify.py` (the selector) + `cli.py` (the `--source` flag + source-requires-stale guard) + tests (`tests/test_classify.py` — 5 selector tests; `tests/test_cli.py` — 5 CLI-path tests incl. count==doctor + clears-entry + no-op source + usage-error; `tests/test_custody_convergence.py` — the per-source refresh↔report tie beside the H135 enrichment-by_source convergence) + docs (`docs/cli.md` classify `--source` paragraph + doctor cross-ref + example, `docs/architecture.md` enrichment bullet). Full suite green (3010). Precondition: H27/H135/H125 (all shipped). | → cap 8, maintenance framing |
| H155 | **✓ shipped (2026-06-18). Per-source `by_source` member on the browse-stats `stats.custody` envelopes (cap 7 + cap 1).** The `stats.custody` family (H98–H101) carried a whole-scope `{tiers, drift}` member on every browse-stats envelope — `search`/`list --stats` (H98), `related --stats` (H99), the always-on `works` stats (H100) — but only the *whole-scope* counts; H133/H141/H145/H149 split the readable surfaces per source and H150 split the `graph` stats, leaving the browse-stats envelopes the one `stats.custody`-bearing family with no per-source split. Each envelope's `stats.custody` now carries a **`by_source`** member — a new pure `custody.tally_custody_by_source(triples)` (the `by_source` analogue of `tally_custody`, sibling of `custody_counts_by_source`) folded over each hit/representation's own `(source, fidelity, drift)` — a `{source: {tiers, drift}}` map with sorted keys, empty `{}` for an empty scope. Built the graph-H150 way (`custody["by_source"] = …` into the dict the envelope already emits, so `scope_envelope` is unchanged, docstring-only). **Decision resolved → lean `{tiers, drift}`, no per-source `coverage`:** the browse-stats family folds precomputed `(fidelity, drift)` pairs and its whole-scope `stats.custody` carries no coverage; coverage counts strictly `content_hash`-bearing held items (`recheck_coverage`) which a `(fidelity, drift)` pair cannot recover (a raw-only capture is `full` yet hash-less), so deriving it would force a new read/dataclass change the slice's *no new ledger read* constraint rules out — coverage stays a `doctor`/`graph` axis (those fold the heavier items+ledger `custody_counts_by_source`). The per-source entries sum to the whole-scope block beside them by construction and — for an uncapped whole-library scope — equal `doctor`'s `custody.by_source` *on the tiers/drift axes*. `works` rides MCP `get_works` too (shared `to_payload`), so the split travels there for free. Verified live (multi-source library: `list`/`search --stats` `by_source` byte-matches `doctor`'s tiers/drift, sorted keys, sums-to-whole; `works` honest `{}` when no multi-rep work). Tests: `tests/test_custody.py` (+5 — the helper: grouping/sorted keys, honest-empty, lean-no-coverage, sums-to-whole, == `custody_counts_by_source` on tiers/drift), `tests/test_cli.py` (+5 — list/search `--stats` multi-source split, sums-to-whole, doctor convergence, past-the-cap, empty `{}`), `tests/test_related.py` (+2), `tests/test_scope.py` (+1 — the split rides the envelope intact), `tests/test_custody_convergence.py` (`_tally_rows` now folds `by_source` so the H101 family test pins it per surface, + `test_browse_stats_by_source_converges_with_doctor_for_the_whole_library`); existing exact-shape `stats.custody` assertions across cli/related/works/mcp updated for the new key. Docs: `docs/cli.md` (the `--stats` `stats.custody` paragraph), `docs/architecture.md` (scope-envelope + works subsystem bullets). Full suite green (3024). Precondition: H98–H101 (the `stats.custody` family, shipped); H104 (`custody_counts_by_source`, shipped); pairs with H150 (the `graph` `stats.custody.by_source`). | → cap 7, cap 1 |
| H156 | **Buffer refresh checkpoint** (maintenance rule). The **full** re-derivation is overdue (last full refresh H102, 2026-06-16; H148 was a light top-up): mark all completed, prune slices overtaken by events, and **re-derive the 3-day narrative**, which has accreted into a backward-looking changelog against maintenance-rule §4 (replace the prose `## 3-day plan` Day-1/2/3 walls with a forward-looking horizon) and bump the day/week horizons past 2026-06-19/2026-06-23. Re-confirm the map to `docs/product/mvp.md`, keep ≥6 un-started work slots. Consider the deferred bi-temporal drift framing only if an agent workflow has shown the event record insufficient (else keep deferred). | maintenance |
| H157 | **✓ shipped (2026-06-18). JSON per-source `by_source` cross-surface convergence invariant (cap 1 + cap 7).** H151 pins the *readable rendered* `_By source:_` line byte-identical across the briefing/compiled/`context` surfaces; the **structured JSON** `by_source` map (`{source: {tiers, drift, coverage}}`, the thing those lines render from) rides JSON `status` (H133), the `graph` `stats.custody.by_source` (H150), and the browse-stats `--stats` envelopes (H155, now shipped) — each tied to `doctor`'s `custody.by_source` in a *scattered* per-surface test. Pinned them *once* as the JSON-surface sibling of H151: new `test_every_json_by_source_surface_converges_on_one_map` in `tests/test_custody_convergence.py` collects all four JSON `by_source` maps from one multi-source seed (`status`, `graph --all` `stats.custody.by_source`, `list --stats`, `search --stats`) and asserts each equals `custody_counts_by_source` over the held items **and** `doctor`'s `custody.by_source` on the tiers/drift axes the *lean* browse family carries (the `_lean_by_source` projection — `list`/`search` omit the per-source `coverage` a `(fidelity, drift)` pair can't recover, so coverage is tied separately on the coverage-bearing surfaces `status`/`graph`), and that each surface's per-source entries sum to its own whole `custody`/`stats.custody` block (the H104 sum-to-whole, per surface — `status`'s ledger-vocab whole block normalized through `_posture_from_ledger_counts`, graph/browse already posture-vocab). **Non-vacuous** (the seed is genuinely multi-source and the two sources' mixes differ, asserted) and **mutation-checked** two ways: an inline perturbed-map assertion proving the equality has teeth, and verified by hand that perturbing only the browse fold (`tally_custody_by_source`) makes the test fail on exactly `list --stats` (then reverted byte-identical). Test + docs only — the convergence-module docstring updated and `docs/cli.md` "A sibling invariant" broadened in two places (the browse-stats `--stats` paragraph + the readable-surface analogue cross-ref); **no production change** (`src/scrolls/custody.py` byte-identical to HEAD). Full suite green (3033, +1). Precondition: H133 (shipped), H150 (shipped), H155 (shipped). | → cap 1, cap 7 |
| H158 | **✓ shipped (2026-06-18). Per-source recheck `coverage` in the readable `_By source:_` breakdown (cap 1 + cap 2).** The JSON `by_source` map carried `{tiers, drift, coverage}` per source (H121), but the **readable** `_By source:_` line showed only the fidelity + drift sections — so a human/agent reading a briefing, a compiled `index.md`/group page, or a `context` bundle saw which source is most *drifted* but not which is least *checked*. Each readable per-source bullet now trails a compact **`· coverage V/T`** section (e.g. ``- `web` — 3 scroll(s) · fidelity full 2, reference 1 · drift … · coverage 2/2``), folded through the shared renderer so the one change rides **every** readable surface at once — the bundle briefing **+ its HTML form** (H141), the compiled `index.md` (H145) + group pages (H152), and the `context` bundle (H149) — the readable counterpart of H121's JSON per-source coverage. **Decisions resolved:** (1) the section reads the `coverage` already in the `custody_counts_by_source` map the renderer receives — **no new derivation/ledger read**; (2) **always-show** even when `total` is 0 (an all-reference source renders ``coverage 0/0``) so the section is positionally stable; (3) the **scope headline stays coverage-free** — threaded as an optional `coverage=` arg on the shared `custody.custody_sections` so the per-source path (`custody_source_breakdown`) passes the source's coverage while `render_custody_headline` passes none (the H113/H103 boundary that kept coverage off `status`'s headline). A `tally_custody_by_source` browse-stats entry (no `coverage` member) degrades to no coverage section (`.get("coverage")`). Verified live (a multi-source bundle/context renders `arxiv 0/1`, `web 2/2` with the reference-only item excluded from the denominator; the `_Custody:_` headline carries no coverage). `custody.py` (`custody_sections`/`custody_source_breakdown`/`render_custody_by_source`) + the byte-identical pinned examples regenerated (`docs/library-format.md` `example-category-consolidation`/`example-kb-index`, `tests/test_docs.py`) + tests (`tests/test_custody.py` +7 — the section on/off + always-show + headline-stays-free; `tests/test_bundle.py`/`tests/test_context.py`/`tests/test_kb.py` — coverage pinned on each readable surface incl. HTML; `tests/test_custody_convergence.py` +1 — `test_readable_per_source_coverage_converges_with_the_json_by_source`: the readable `coverage V/T` == `doctor`'s/`custody_counts_by_source`'s `by_source[S].coverage`, sums to whole-library `recheck_coverage`) + docs (`docs/cli.md` export-bundle/context paragraphs + example + the H151 sibling-tie paragraph, `docs/architecture.md` three bullets). Full suite green (3032). Precondition: H121 (the per-source `coverage`, shipped); H141 (the shared readable renderer, shipped); H145/H149/H152 (the readable surfaces it rides, shipped). | → cap 1, cap 2 |
| H159 | **✓ shipped. Readable weakest-source `_Attention:_` line on the bundle/context briefing (cap 1 + cap 9/cap 10).** The weakest-source `attention` flag JSON `status`/`maintain` carry now rides the *readable* surfaces too: one `_Attention: source \`<S>\` carries the most drift (<reason>) — recheck with \`scrolls verify --source <S>\`._` line under the scope custody headline, above the `_By source:_` map, on the `export bundle` briefing (Markdown **and** its HTML form, a red `custody-attention` paragraph) and the model-facing `scrolls context` bundle (`connected`+). The shared `weakest_source` primitive was **lifted from `maintain.py` to `custody.py`** (its natural home beside `custody_counts_by_source`; `maintain` re-exports it so `status`/`maintain` import it unchanged — identity-equal, pinned), and a new `custody.render_custody_attention` renders the line over the bundle scope's own per-source map — so the readable line names the same `{source, reason, command}` as the JSON flag by construction. Honest absence: omitted exactly when the JSON flag is `null` (single-source, clean, or empty scope). The readable-line tie is folded into `tests/test_custody_convergence.py` beside the H139 JSON `attention` tie (`test_readable_attention_line_converges_across_surfaces_and_the_json_flag` + the absent-together sibling): every readable surface's parsed `{source, reason, command}` == `weakest_source(doctor.custody.by_source)` == the `status`/`maintain` flag, all absent together on a clean scope. Tests: `tests/test_bundle.py` (Markdown + HTML line, position, honest absence ×3, round-trip preserved), `tests/test_context.py` (line, convergence, `index`-gate, MCP parity, honest absence), `tests/test_custody_convergence.py` (the cross-surface ↔ JSON tie). Docs: `docs/cli.md` (the `_Attention:_` paragraph), `docs/architecture.md` (export-bundle + maintain bullets). Full suite green (3050). Precondition: H119/H137 (the flag + command, shipped); H141/H149 (the readable surfaces, shipped); H153 (the coverage-on-attention enrichment, shipped). | → cap 1, cap 9, cap 10 |
| H160 | **Attention-flag full-shape cross-surface convergence invariant (cap 1 + cap 7).** Once H153 (per-source `coverage` on the flag) and H159 (the readable `_Attention:_` line) land, the weakest-source `attention` flag rides three surfaces — JSON `status` (H139), JSON `maintain` (H119/H137), and the readable bundle/context line (H159) — each *claimed* to carry the same `{source, drift, coverage, command}` distilled from `doctor`'s `custody.by_source`. Pin that *once*, the H151/H157 pattern: over the multi-source loss seed assert all three surfaces' `attention` agree field-for-field (the readable line parsed back into `{source, drifted, rotted, command}` and, post-H153, `coverage`), all equal `weakest_source(doctor.custody.by_source)`, and all honestly absent together when no source carries loss. Non-vacuous (a genuine multi-source loss seed; mutation-checked — perturbing one surface's distillation breaks the tie). Test + docs only (convergence-module docstring + `docs/cli.md` "A sibling invariant" broadened); no production change. Precondition: H153 + H159 (both must land first); H129/H139 (the JSON `attention` ties already pinned, shipped). | → cap 1, cap 7 |
| H161 | **✓ shipped (2026-06-18). MCP `get_library_health` — the custody audit over MCP (cap 1 + cap 2, MCP parity).** An agent operating purely over MCP could read a scroll's per-item `fidelity`/`drift` and the `graph` `stats.custody.by_source`, but had **no whole-library custody read** — it could not ask "what's the library's custody score, and which source needs attention?" the way the CLI `status`/`doctor` can. New read-only `get_library_health()` MCP tool (`mcp_server.py`, registered in `_TOOLS` + the instructions string) returns **exactly `run_doctor`'s custody block** (`score`/`tiers`/`drift`+`coverage`/`by_source`/`enrichment`/`summaries`/`findings`/`issues`) **plus the two distilled members `status` adds** — the weakest-source `attention` flag (`weakest_source(custody["by_source"])`) and the one-line `headline` (`snapshot_headline(custody_snapshot(report))`) — so the MCP custody picture converges with the CLI `status`/`doctor` by construction (one `run_doctor` read, no new derivation; the shared `weakest_source`/`custody_snapshot`/`snapshot_headline` primitives). **Decisions resolved:** (1) name — `get_library_health` (the doctor-shaped audit framing); (2) **custody posture only** — the tool carries the custody block (whose own `issues`/`findings` feed the *score*, not the exit code), never the repairable structural-findings/`--fix`/exit-code axis (duplicates / missing scrolls / FTS), which stays a CLI act (the H113 read/act boundary — an MCP agent reads the triage signal, the shell repairs); (3) honest absence — an uninitialized library is the present-but-empty block (`score: null`, zeroed counts, `attention: null`, `headline: _Custody: 0 scroll(s)._`), never an error. Network-free (drift read from the verify ledger, no live re-capture). Verified live (before-init empty block; a seeded drift moves the drift posture + flags `attention` while the integrity `score` holds at 100 — the custody-vision invariant). Tests: `tests/test_mcp.py` (+4 — returns doctor's custody block, field-for-field parity with CLI `status`, before-init honest-empty, empty-initialized fully-custodied; tool-surface lock updated), `tests/test_custody_convergence.py` (+2 — the MCP↔`status`↔`doctor` tie as the MCP-surface sibling of the H157 JSON convergence + the honest-null gate; module docstring's surface count bumped to seven). Docs: `docs/cli.md` (MCP-tools table row), `docs/architecture.md` (MCP-server bullet). Full suite green (3056). Precondition: H133 (`status` `by_source`, shipped); H139 (`attention`, shipped); H150 (the MCP `graph` custody precedent, shipped). **Note:** H160 (the attention-flag full-shape cross-surface convergence invariant) was passed over this run — its preconditions (H153 + H159) are now met and it remains the immediate next un-started slice — to ship the more substantive MCP-capability slice as a coherent unit (the H53 "passed over, not overtaken" posture). | → cap 1, cap 2 |
| H162 | **✓ shipped (2026-06-18). `scrolls doctor --source <S>` — scope the whole custody audit to one source (cap 1, maintenance framing).** The per-source `by_source` map (H104/H135) named *which* source is weakest and `verify --source`/`classify --stale --source` (H125/H154) *act* on one source, but there was no way to *read* the **full** custody report scoped to one source — a worker triaging "source `web` is weakest" got the headline counts from `by_source[web]` but had to scan the whole-library `drift.events`/`enrichment.items` to find web's offending ids. `run_doctor` now takes `source: str | None`; a `--source <S>` flag scopes the **whole** audit to that source's held items (`list_items(source=S)`), so every item-derived block — `score`, `tiers`, `drift`, `coverage`, `enrichment`, `summaries`, and the offending-id lists (`drift.events`, `enrichment.items`, `missing_scrolls`, duplicates) — is the one-source view, the audit-side counterpart of the per-source act commands. **Decisions resolved:** (1) **filter the input** item set (not the finished report) so every block is genuinely one-source incl. the narrowed id lists; (2) `by_source` collapses to the **present-and-singleton** `{S: …}` (shape stability); (3) the **exit-code rule is unchanged** (structural `issues > fixed` fails) over the source's attributable findings; (4) the two **non-source-attributable** checks are **skipped** under `--source` — `orphan_scrolls` (an unowned `.md` belongs to no source, and scoping must not flag *other* sources' owned scrolls as orphans) and `fts` (one global index), both reporting their empty/`skipped` defaults and left to a whole-library `--fix`; (5) an **unknown source** is the honest empty audit (`score: 100`, empty `by_source`, exit 0), never an error. The convergence pinned by construction: a `--source S` audit's `tiers`/`drift`/`coverage` equals the whole-library audit's `by_source[S]` slice and its `enrichment.stale` equals `enrichment.by_source[S]` (same held subset, same tally). Verified live (scoped audit == whole `by_source[web]`; `by_source` singleton; drift events narrow to web; orphans/FTS skipped; a drifted source moves the posture without lowering the score). Tests: `tests/test_doctor.py` (+8 — scoped custody view, audit==`by_source[S]` slice, narrowed drift events, narrowed enrichment stale==`by_source[S]`, unknown-source empty audit, orphan/FTS skip + other-source scroll not flagged, only-that-source missing-scrolls + exit code, `--source --fix` repairs only that source), `tests/test_custody_convergence.py` (+2 — the doctor-scope↔`by_source` tie over a multi-source loss seed differing on every axis, mutation-checked + non-vacuous, and the unknown-source honest-empty gate; module docstring's scoped-audit paragraph). Docs: `docs/cli.md` (doctor heading + `--source` paragraph), `docs/architecture.md` (Doctor bullet). Full suite green (3066). **Unblocks H165 (`maintain --source`), H166 (`status --source`), H167 (MCP `get_library_health(source=)`)** — all reuse this `run_doctor(source=)` pre-filter. Precondition: H104/H135 (the per-source maps, shipped); H125/H154 (the per-source act precedents, shipped). **Note:** H160 (the attention-flag full-shape cross-surface invariant) was passed over again — its preconditions (H153 + H159) remain met and the H159 hub-and-spoke already ties all three surfaces to `weakest_source(doctor.by_source)`, so H160's novel content is mostly the mutation-check teeth; the substantive per-source-audit feature unblocking three downstream slices was the higher-leverage unit this run (the H53/H161 "passed over, not overtaken" posture). H160 remains a valid next invariant slice. | → cap 1, maintenance framing |
| H163 | **Per-source `by_source` member on the MCP browse twins' stats (cap 2 + cap 7, MCP parity).** H155 put the per-source `by_source` split on the CLI `search`/`list --stats` envelopes and the always-on `works` stats (which already rides MCP `get_works` via the shared `to_payload`). The MCP `search_scrolls`/`list_scrolls` twins, if they expose a `stats.custody`, should carry the same `by_source` member so an agent reading custody over MCP sees the per-source split the CLI does — or, if those twins emit the bare result array (no `--stats` envelope over MCP), record that asymmetry explicitly and pin it (the MCP browse surface is array-only by design, `get_works` the one stats-bearing twin). Inspect `mcp_server.py`'s search/list tools first to pick which: thread `scope_envelope`+`tally_custody_by_source` through if they envelope, else a one-test parity note. Test (`tests/test_mcp.py`) + docs (`docs/cli.md` MCP parity note). Precondition: H155 (the `by_source` split + helper, shipped); H100 (the `get_works` stats twin, shipped). | → cap 2, cap 7 |
| H164 | **✓ shipped (2026-06-18). Weakest-source `attention` flag on the `graph` stats block (`stats.custody.attention`) (cap 1 + cap 2).** H150 put the per-source `by_source` map on the `graph` `stats.custody` block; H139/H119 distil the *same* per-source map to a single weakest-source `attention` flag on JSON `status`/`maintain`. The graph block carried the *map* but not the distilled *flag* — an agent reading the link graph + custody had to scan `by_source` itself. Now `graph.to_payload` adds `custody["attention"] = weakest_source(custody["by_source"])` (one new line + the import) — the **same shared `custody.weakest_source` primitive** `status`/`maintain` thread, over the graph's own `by_source` (which equals `doctor`'s for the whole-library scope, pinned H150), so the source with the most `drifted + rotted` loss, its `{tiers, drift, coverage}` tally (H153 coverage rides for free), and the exact `scrolls verify --source <S>` recheck command (H137) converge with `status`/`maintain`/`doctor` **by construction** (same primitive, same map — no new derivation, no new ledger read). Honest `null` on the same three gates as the JSON flags — empty / single-source / fully-clean — so a one-source or loss-free graph flags nothing even with drift; ranks the whole `stats.custody.by_source` scope, so independent of `--all` like the map it distils. **CLI + MCP twins share `to_payload`, so the flag rides MCP `get_link_graph` for free** (verified live: the graph flag is byte-identical to `status`'s). Tests: `tests/test_graph.py` (+4 — flags the weakest source incl. coverage+command, honest-null without cross-source loss, single-source-null-even-with-drift, `--all`-independent; the `_all_reference_unverified` helper + the two MCP exact-shape blocks gain `"attention": None`), `tests/test_mcp.py` (+1 — the flag rides `get_link_graph` and agrees with CLI `status`), `tests/test_custody_convergence.py` (+2 — graph `attention` ≡ `status` ≡ `maintain --no-recheck` ≡ `weakest_source(doctor.by_source)` over the loss seed, naming doctor's max-loss source; the honest-null gate; module docstring broadened). Docs: `docs/cli.md` (graph `stats.custody.attention` paragraph + console example + MCP-tools row), `docs/architecture.md` (graph custody bullet). Full suite green (3093). Precondition: H150 (the `graph` `by_source`, shipped); H139/H119 (the `attention` flag + `weakest_source` primitive, shipped). | → cap 1, cap 2 |
| H165 | **✓ shipped (2026-06-18). `scrolls maintain --source <S>` — scope the scheduled custody pass to one source (cap 1, maintenance framing).** The scheduled-maintenance leg of the per-source-scope triad beside H162's `doctor --source` and H166's `status --source`, reusing the **same** shipped `run_doctor(source=)` pre-filter, so a worker triaging "source `web` is weakest" (named by `status`/`maintain`'s `attention` flag) runs the whole maintenance loop against just that source. `_cmd_maintain` takes `source: str \| None`; under `--source <S>` the **recheck** narrows to that source's held, hash-bearing items (`_recheck_held_items`/`_maintain_coverage` thread `source` to `list_items(source=)` — the `verify --source S` set, still stale-bounded/`--all`-able/`--limit`-paced), and the **audit** is scoped (`run_doctor(source=S)`), so `custody`/`headline`/`by_source` (the singleton `{S: …}`)/`enrichment_by_source` are the one-source view and `attention` is naturally `null` (single-source `weakest_source` gate). The scoped `custody`/`by_source`/`headline` **converge by construction** with `doctor --source S` distilled and a `status --source S` payload. **Decisions resolved:** (1) **non-persisting** scoped pass — it records per-item drift events (real work the next whole-library pass folds into the trend) but never writes the single whole-library snapshot/log, so a focused triage pass can't clobber the one baseline with a one-source slice (no per-source storage shape, ADR 0082); since no per-source baseline exists to diff against (the stored snapshot carries only whole-library scalars), the scoped **`delta` is `null`** (honest absence — the per-pass `recheck` movement is the signal); a top-level **`source`** member echoes the scope (`null` whole-library); (2) `--source` composes with `--all`/`--limit`/`--no-recheck`, conflicts with `--history` (a read, not a pass — exit 2, the `--all`/history precedent); (3) view-regeneration (`compile_kb`) stays **whole-library** (deterministic global recompile). Unknown source → honest empty pass (`_Custody: 0 scroll(s)._`, empty `by_source`, `null` `attention`/`delta`, exit 0); exit reflects only `<S>`'s attributable findings (orphan/FTS skipped per `run_doctor`). Verified live (scoped recheck touches only web; `delta: null`; baseline untouched). Tests: `tests/test_maintain.py` (+7 — scoped recheck, the one-source audit == `doctor --source` distilled == whole `by_source[S]`, non-persisting/baseline-untouched, drift events still recorded, unknown-source empty pass, `--source` ∧ `--limit`, `--source` ∧ `--history` conflict), `tests/test_custody_convergence.py` (+2 — the maintain-scope↔`by_source`↔`doctor --source`↔`status --source` tie over the multi-source loss seed, mutation-checked non-vacuous, beside the H162/H166 ties; the unknown-source honest-empty gate). Docs: `docs/cli.md` (maintain `--source` paragraph + console example), `docs/architecture.md` (maintain bullet). Full suite green (3081). **Unblocks H169** (the per-source-scope capstone convergence — the maintain leg now lands; H167 the MCP leg remains). Precondition: H162 (`doctor --source` pre-filter, shipped); H125/H154 (the per-source act precedents, shipped); H34/H123 (the maintain pass + `by_source` member, shipped). | → cap 1, maintenance framing |
| H166 | **✓ shipped (2026-06-18). `scrolls status --source <S>` — scope the status custody read to one source (cap 1 + cap 2).** `status` carried the whole-library custody picture (`custody` block, `headline`, `by_source` map, weakest-source `attention` flag — H38/H117/H133/H139) but only library-wide; a worker that `status`'s `attention` just told "source `web` is weakest" had to re-run `doctor`/`facets --source` to read web's own headline/score. A `--source <S>` flag now scopes the **whole** status read to one source's held items — the status-surface counterpart of H162's `doctor --source` and the read-side sibling of the per-source act commands (`verify --source` H125, `classify --stale --source` H154), reusing the **same** shipped `run_doctor(source=)` pre-filter. **Decision resolved → narrow the `items` block too** (the slice's surfaced decision): a new `source=` param on `items.library_counts` filters every count (`total`/`by_stage`/`by_source`/`unclassified`) to that source, so the whole payload is genuinely one-source and the counts and the custody block agree on scope (a whole-library `items` beside a one-source custody block would be an internal contradiction). Every block is then the one-source view: `items` counts, `custody` snapshot, rendered `headline`, and `by_source` (collapsed to the present-and-singleton `{S: …}`). **`attention` is `null` under `--source` with no special-casing** — `by_source` is a singleton, so the documented `weakest_source` single-source gate (`len < 2`) returns `None` naturally (the flag only discriminates *across* sources). The convergence pinned by construction: a `--source S` status's `custody.tiers`/`drift`/`coverage` equals the whole-library audit's `by_source[S]` slice **and** a `doctor --source S` audit distilled (`custody_snapshot`). Unknown source → the honest empty headline (`_Custody: 0 scroll(s)._`, `score: 100` over an initialized library, empty `by_source`, `null` attention), never an error; before `init` the empty payload (no crash on a missing store). Verified live (`status --source web` narrows every block to web == `by_source[web]`; `attention: null`; `ghost` → empty headline, exit 0). CLI-only (`status` has no MCP twin — H167 is the MCP sibling). Tests: `tests/test_items.py` (+1 — `library_counts(source=)` narrows every count, unknown-source honest-empty), `tests/test_cli.py` (+3 — scoped whole payload == `by_source[S]`/`custody_snapshot(doctor --source)`, items narrowed, attention null; unknown-source empty headline; before-init empty), `tests/test_custody_convergence.py` (+2 — the status-scope↔`by_source` tie over the multi-source loss seed, mutation-checked non-vacuous, beside the H162 doctor-scope tie; the unknown-source honest-empty gate). Docs: `docs/cli.md` (status `--source` paragraph), `docs/architecture.md` (status bullet — the per-source scope). Full suite green (3072). **Unblocks H167 (MCP `get_library_health(source=)`)** — reuses the same `run_doctor(source=)` pre-filter. Precondition: H162 (`doctor --source` pre-filter, shipped); H133/H139 (status `by_source`/`attention`, shipped). | → cap 1, cap 2 |
| H167 | **✓ shipped (2026-06-18). MCP `get_library_health(source=...)` — the scoped custody read over MCP (cap 1 + cap 2, MCP parity).** `get_library_health()` (H161) read only the *whole-library* custody block over MCP; an MCP agent triaging the weakest source (named by the tool's own `attention` flag) had no way to read just that source's health, the way the CLI worker can via `doctor --source`/`status --source` (H162/H166). The tool now takes an optional `source: str \| None = None` that scopes the whole read to one source's held items through the **same** shipped `run_doctor(source=)` pre-filter — so the custody block / `by_source` / `attention` / `headline` are the one-source view: the MCP sibling of the audit-scope (H162) and status-scope (H166) reads, completing the per-source custody triage across CLI **and** MCP. **Decisions resolved:** (1) **reuse `run_doctor(source=)` verbatim** — no new derivation, so the scoped MCP block *is* the scoped CLI audit block plus the two distilled members; (2) `attention` is `null` under a `source` scope with **no special-casing** — `by_source` collapses to the singleton `{S: …}`, so the `weakest_source` cross-source gate (`len < 2`) returns `None` naturally, exactly like `status --source` (H166); (3) **unknown source → the honest empty block** (`score: 100` over an initialized library, empty `by_source`, `null` attention, `_Custody: 0 scroll(s)._` headline), never an error, mirroring `doctor --source ghost`. The convergence pinned by construction: `get_library_health(source=S)`'s block equals a `doctor --source S` audit key-for-key, the whole-library `by_source[S]` slice (tiers/drift/coverage), and a `status --source S` payload's `custody`/`by_source`/`headline`. Verified live (scoped web == its `by_source[web]` slice with `attention: null`; arxiv singleton; unknown source the honest empty block). Tests: `tests/test_mcp.py` (+3 — scoped read == `doctor --source` block, attention null on a single source while the whole read still flags web, unknown-source honest-empty), `tests/test_custody_convergence.py` (+2 — the MCP-scope↔`doctor --source`↔`status --source`↔`by_source[S]` tie over the multi-source loss seed, mutation-checked non-vacuous, beside the H161 whole-library tie; the unknown-source honest-empty gate; the scoped-audit paragraph broadened to name the four-surface per-source read family). Docs: `docs/cli.md` (MCP-tools row), `docs/architecture.md` (MCP bullet). Full suite green. **Unblocks H169** (the per-source-scope capstone — the MCP leg now lands, so the four-surface tie can be pinned once). Precondition: H161 (the whole-library tool, shipped); H162 (the `run_doctor` per-source pre-filter, shipped). **Note:** H160 (the attention-flag full-shape cross-surface invariant) was passed over again — its preconditions remain met and it is a valid next invariant slice; the substantive MCP-capability slice completing the per-source read family across CLI+MCP was the higher-leverage coherent unit this run (the H53/H161 "passed over, not overtaken" posture). | → cap 1, cap 2 |
| H168 | **✓ executed (2026-06-18).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so a full refresh remains **overdue** and is still the next dedicated checkpoint's explicit task, H156: mark all completed, prune slices overtaken by events, **re-derive the 3-day narrative** — it has accreted into a backward-looking changelog against maintenance-rule §4 — and bump the day/week horizons past 2026-06-19/2026-06-23). Marked **H166 shipped** (`scrolls status --source <S>` scopes the whole status read to one source's held items via the shipped `run_doctor(source=)` pre-filter + a new `library_counts(source=)` narrowing — the status-surface counterpart of `doctor --source`; `by_source` collapses to the singleton, `attention` is naturally `null` by the `weakest_source` single-source gate, the scoped custody view == the whole-library `by_source[S]` slice == `doctor --source S` distilled, pinned in the convergence suite). With H166 shipped the un-started **work** slices fell to five (H160, H163, H164, H165, H167) — appended **H169** below to restore the ≥6 floor. **H165** (`scrolls maintain --source <S>`) is the natural next pick: the scheduled-maintenance counterpart of the now-shipped `doctor --source`/`status --source` per-source reads, the third leg of the per-source-scope triad H162 unblocked (preconditions all shipped — H162/H125/H154/H34/H123). Bi-temporal drift framing stays correctly deferred (no agent workflow has shown the event record insufficient). Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H160, H163, H164, H165, H167, H169. | maintenance |
| H169 | **Per-source-scope cross-surface convergence invariant — the scoped reads agree (cap 1 + cap 7).** With `doctor --source` (H162), `status --source` (H166), `maintain --source` (H165, shipped), and MCP `get_library_health(source=)` (H167, once landed) all scoping the *whole* custody read to one source through the **same** `run_doctor(source=)` pre-filter, pin *once* — the scoped-read analogue of H157's whole-library JSON `by_source` convergence — that every `--source S` read reports the *same* one-source custody block. Over the multi-source loss seed (`_seed_two_source_loss`), assert a `doctor --source S` audit's custody view, a `status --source S` payload's `custody`/`by_source`/`headline`, a `maintain --source S` report's `custody`/`by_source`, and MCP `get_library_health(source=S)` all equal each other **and** the whole-library audit's `by_source[S]` slice (same held subset, same tally), with `attention` honestly `null` on each (single-source gate). Non-vacuous (the two sources differ on every axis) and mutation-checked (perturbing one surface's scope breaks the tie). The capstone of the per-source-scope triad, the scoped sibling of the H157 (JSON `by_source`) and H151 (readable line) whole-library convergence invariants. **Note:** the `maintain --source`↔`doctor --source`↔`status --source` three-way tie already landed with H165 (`test_maintain_source_scope_converges_with_the_whole_library_by_source`); H169 is the *capstone* that folds the MCP leg in once H167 ships, so it stays gated on H167. Test + docs only (convergence-module docstring + `docs/cli.md` "A sibling invariant" broadened); no production change. Precondition: H162 (shipped), H166 (shipped), H165 (shipped); H167 (land first — the test grows the MCP surface when it ships, the H160-style "all preconditions met" gate). | → cap 1, cap 7 |
| H170 | **✓ executed (2026-06-18).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so the full refresh — mark all completed, prune slices overtaken by events, re-derive the accreted-into-changelog 3-day narrative per maintenance-rule §4, and bump the day/week horizons past 2026-06-19/2026-06-23 — remains **overdue** and is the next dedicated checkpoint's explicit task, H156). Marked **H165 shipped** (`scrolls maintain --source <S>` scopes the whole pass to one source via the shipped `run_doctor(source=)` pre-filter + a scoped `_recheck_held_items`/`_maintain_coverage`; non-persisting — records per-item drift events but never the whole-library snapshot/log, `delta: null`, top-level `source` echoed; the scoped `custody`/`by_source`/`headline` converge with `doctor --source`/`status --source`, pinned in the convergence suite). With H165 shipped the per-source-scope **triad** (`doctor`/`status`/`maintain --source`) is complete; the only scoped reads left are the MCP leg **H167** and the capstone **H169**. Un-started **work** slices fell to five (H160, H163, H164, H167, H169) — appended **H171** below to restore the ≥6 floor. **H167** (MCP `get_library_health(source=)`) is the natural next pick: the MCP sibling of the now-complete CLI per-source-scope triad, reusing the same `run_doctor(source=)` pre-filter (preconditions all shipped — H161/H162), and it unblocks H169. Bi-temporal drift framing stays correctly deferred. Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H160, H163, H164, H167, H169, H171. | maintenance |
| H171 | **✓ shipped (2026-06-18). Per-source stale-summary debt in `doctor`'s `custody.summaries.by_source` (cap 8 + cap 1).** `doctor`'s `custody` block split the drift axis per source (`custody.by_source`, H104) and the stale-classification axis per source (`custody.enrichment.by_source`, H135) — but the **summary** axis (`custody.summaries`, H29) carried only the whole-library `stale` scalar, so a worker told "summaries are stale" couldn't see *which* source's items drove a cluster stale without scanning `summaries.items`. Added a `by_source` map to `custody.summaries`: a flat `{source: stale_count}` of the *offending* sources only (sorted keys, a clean source omitted — the H135 offenders-only posture). **Decision resolved (the load-bearing wrinkle):** a concept summary spans a *cluster* and the stored fingerprint records only the members digest (not which member moved), so a stale summary is attributed to **every source among its live members** (a summary is "stale for source S" if S participates in the concept) — chose this over the primary-source alternative because it is exactly the offenders set the downstream `kb --stale --source <S>` (H172) must re-synthesize (`kb --stale` acts on concepts, not members). The consequence, documented: one multi-source stale concept counts toward >1 source, so `by_source` **need not sum to `summaries.stale`** (`sum >= stale`, equality iff every stale concept is single-source) — unlike the drift/enrichment maps. This explicitly supersedes the H135-era `_check_summary_provenance` docstring decision to omit the breakdown (that omission was justified by the sum-to-whole convergence this map deliberately does not claim, so cannot break). The read-side foundation mirroring how `enrichment.by_source` (H135) preceded `classify --stale --source` (H154): **unblocks H172** (`kb --stale --source`). `doctor._check_summary_provenance` (the per-source tally, each stale concept attributed to the distinct sources of its live members; docstring superseded) + scaffold `by_source: {}`. Verified live (empty library → `by_source: {}` present; the multi-source attribution and offenders-only/sorted/no-sum-to-whole pinned by tests). Tests: `tests/test_doctor.py` (+4 — empty/no-stale, multi-source double-attribution with sum>stale, offenders-only+sorted, never-feeds-issues), `tests/test_custody_convergence.py` (+1 — `summaries.by_source` ≡ the independent per-source `is_stale_summary` tally over a multi-source seed, the H171 asymmetry non-vacuous, beside the H135 enrichment tie), exact-shape summaries block updated. Docs: `docs/cli.md` (doctor summaries `by_source` paragraph + corrected the enrichment paragraph's stale "no per-source counterpart" claim), `docs/architecture.md` (enrichment + summary-axis bullets). Full suite green (3098). Precondition: H29 (summary provenance + stale tracking, shipped); H135 (the `enrichment.by_source` offenders-only precedent, shipped). **Note:** H160 (the attention-flag full-shape cross-surface invariant) was passed over again — its preconditions remain met and it stays the immediate next *invariant* slice — to ship the substantive per-source-debt feature that unblocks H172 (the H53/H161/H162 "passed over, not overtaken" posture). | → cap 8, cap 1 |
| H172 | **✓ shipped (2026-06-18). `scrolls kb --stale --source <S>` — the summary-axis per-source refresh (cap 8, maintenance framing).** The summary-axis act sibling of `classify --stale --source` (H154, the classification-axis per-source refresh), mirroring how that followed `enrichment.by_source` (H135): `kb --stale` (H31) gains a `--source <S>` flag that narrows the re-synthesis to exactly the stale concepts source `<S>` participates in — the `doctor` `custody.summaries.by_source[S]` offenders (H171) — so a worker triaging "source `web`'s summaries are stale" refreshes just those. The one new primitive is `source` narrowing on the shared `kb_llm._summary_targets` (both generators thread `source=` through to it): on top of the `stale_only` set (the `is_stale_summary` predicate doctor's report also uses), it keeps the concepts with `<S>` among their **live members**. **Decision resolved (the H171 attribution carried through):** a stale summary records only the members digest, not which member moved, so a multi-source cluster is "stale for" *every* member source and is refreshed under **any** of them (`kb --stale --source wikipedia` and `--source web` both regenerate the wikipedia+web BM25 cluster) — exactly the `summaries.by_source` attribution, so the concepts the scoped refresh regenerates equal that offenders set and refreshing **clears that source's entry** (`{arxiv,web,wikipedia}` → refresh `arxiv` → `{web,wikipedia}`; refresh the multi-source cluster → both its sources clear). `--source` is a *narrowing* of `--stale`, not a standalone selection (`--source` without `--stale` is a usage error, the H154 posture); implies the llm engine (the existing `--stale` deterministic reject covers `--source --engine deterministic`); composes with `--batch` (both llm); an unknown/clean source is the network-free no-op. View-regeneration (`compile_kb`) stays whole-library. Verified live (usage-error + deterministic-reject guards, `--help` carries `--source`). Tests: `tests/test_kb_llm.py` (+6 — `_summary_targets` source narrows / unknown-empty / ignored-without-stale_only, per-call + batch scoped refresh, unknown-source no-op), `tests/test_kb.py` (+7 — scoped refresh, per-source signal-clears vs `doctor.summaries.by_source`, multi-source cluster under either source, unknown no-op, `--source` without `--stale` usage error, deterministic reject, `--batch` compose). Docs: `docs/cli.md` (kb `--stale --source` paragraph + example + doctor `summaries.by_source` cross-ref flipped from "a future command"), `docs/architecture.md` (summary-axis enrichment bullet + the per-source §1324 note corrected — the "summary axis has no per-source split" claim, already overtaken by H171, removed). Full suite green (3111, +13). Closes the summary-axis per-source loop H29 (record) → H171 (per-source report) → H172 (per-source refresh), the mirror of H20→H135→H154 on the classification axis. Precondition: H31 (`kb --stale` + `stale_only`, shipped); H171 (the `summaries.by_source` read precursor, shipped). **Note:** H160 (the attention-flag full-shape cross-surface invariant) was passed over again — its preconditions remain met and it stays a valid next *invariant* slice — to ship the substantive feature that closes the summary-axis per-source loop (the H53/H161 "passed over, not overtaken" posture). | → cap 8, maintenance framing |
| H173 | **✓ executed (2026-06-18).** Buffer top-up (maintenance rule, light — **not** a full refresh; the last full re-derivation was H102, 2026-06-16, so the full refresh — mark all completed, prune slices overtaken by events, re-derive the accreted-into-changelog 3-day narrative per maintenance-rule §4, and bump the day/week horizons past 2026-06-19/2026-06-23 — remains **overdue** and is the next dedicated checkpoint's explicit task, H156). Marked **H164 shipped** (the weakest-source `attention` flag now rides the `graph` `stats.custody` block beside its `by_source` map, distilled by the shared `custody.weakest_source` so it converges with JSON `status`/`maintain`/`doctor` by construction and rides MCP `get_link_graph` for free, pinned in the convergence suite). With H164 shipped the un-started **work** slices fell to five (H160, H163, H169, H171, H172) — appended **H174** below to restore the ≥6 floor. **H169** (the per-source-scope capstone convergence invariant) is now **fully unblocked** — its last precondition H167 shipped this run-batch, so the four-surface (`doctor`/`status`/`maintain`/MCP `get_library_health` `--source`) tie can be pinned once; it is the natural next pick (test+docs, the H157/H151 whole-library-convergence sibling on the scoped axis). Bi-temporal drift framing stays correctly deferred. Week plan still maps to `docs/product/mvp.md` (custody-deepening, no new adapters). ≥6 un-started work slots now: H160, H163, H169, H171, H172, H174. | maintenance |
| H175 | **`maintain` report carries `summary_by_source` — the scheduled-surface read of H171's per-source stale-summary debt (cap 8 + cap 1, maintenance framing).** Appended (maintenance rule) to restore the ≥6 un-started floor after H171 shipped. H147 threads `doctor`'s `custody.enrichment.by_source` into the `maintain` report as `enrichment_by_source` (a faithful read via `maintain.report_enrichment_by_source`), so a scheduled worker sees per-source stale-classification debt without a second `doctor` run; H171 just added the summary-axis sibling `custody.summaries.by_source`, but `maintain` does not yet surface it. Add a `summary_by_source` member to the `maintain` report (a new `maintain.report_summary_by_source(report)` faithful read of `report["custody"]["summaries"]["by_source"]`, mirroring `report_enrichment_by_source`), so the scheduled custody pass names which source's summaries need a refresh alongside which source's classifications do. **Decision to carry through (from H171):** this map need *not* sum to the whole (a multi-source stale concept counts toward each contributing source), so the `maintain`↔`doctor` tie is faithful-read equality (`summary_by_source == doctor.custody.summaries.by_source`), **not** a sum-to-whole check — unlike the `enrichment_by_source` tie. Lives on the live pass only (derived from this pass's audit), like `enrichment_by_source`; `--history`/`--trend` replay snapshots and do not carry it (the H40 suggestion posture). `maintain.py` (`report_summary_by_source` + the member) + tests (`tests/test_maintain.py` — present on a stale-summary pass, faithful-read equality with `doctor`, absent/empty on a clean pass, not carried by `--history`; `tests/test_custody_convergence.py` — `maintain summary_by_source ≡ doctor.custody.summaries.by_source` over the multi-source seed, beside the H147 enrichment tie) + docs (`docs/cli.md` maintain paragraph + example envelope, `docs/architecture.md`). Precondition: **H171** (the `summaries.by_source` read precursor, shipped); H147 (the `enrichment_by_source` maintain-member precedent, shipped). | → cap 8, cap 1 |
| H174 | **Weakest-source `attention` flag on the browse `--stats` envelopes' `stats.custody` (`search`/`list --stats`) (cap 2 + cap 7).** H164 added the distilled `attention` flag to the `graph` `stats.custody` block; the browse `--stats` envelopes (`scrolls search`/`list`, roadmap H98/H155) carry the per-source `by_source` *map* over the matched scope but not the distilled *flag*, so an agent paging results still scans `by_source` to find the matched scope's weakest source. Add `custody["attention"] = weakest_source(custody["by_source"])` to the browse-stats assembly (`cli.py` `_cmd_search`/`_cmd_list`, beside the existing `tally_custody_by_source` fold) so the flag names the weakest source *of the matched/filtered scope* — the browse-surface counterpart of H164, over the matched set rather than the whole library. **Decision to surface (the load-bearing wrinkle):** the browse `by_source` is the **lean** projection (`tally_custody_by_source` carries `{tiers, drift}` only, no per-source `coverage` — H155), so `weakest_source` (which `.get("coverage", {0,0})`-degrades) would emit a flag whose `coverage` reads a fabricated `0/0` rather than the source's real recheck fraction. Resolve in the slice: either (a) **drop the `coverage` member from the browse `attention` flag** (a lean flag for a lean map — the honest projection, so a reader never mistakes `0/0` for "nothing checked"), or (b) thread the coverage-bearing `custody_counts_by_source` into the browse fold (heavier, re-reads the ledger per-source). Prefer (a) unless the convergence suite needs the coverage axis — the flag's job here is naming the source, and `status`/`graph` already carry the coverage-bearing flag. Honest `null` on the same empty/single-source/clean gates; over a whole-library (unfiltered, uncapped) scope the source it names equals `status`/`graph`/`doctor`'s. Converges by construction (same primitive). `cli.py` (the member on both envelopes) + tests (`tests/test_cli.py` — present on `search`/`list --stats`, names the matched scope's weakest, honest-null, the lean-coverage decision pinned; `tests/test_custody_convergence.py` — the browse `attention` ≡ `status`/`graph` over a whole-library scope, beside the H164 graph tie) + docs (`docs/cli.md` browse `--stats` paragraph, `docs/architecture.md`). Precondition: H155 (the browse `by_source` map + `tally_custody_by_source`, shipped); H164 (the graph `attention` precedent, shipped); H139/H119 (the `weakest_source` primitive, shipped). | → cap 2, cap 7 |
| H176 | **Summary-axis per-source refresh convergence folded into `tests/test_custody_convergence.py` (cap 8 + cap 1).** Appended (maintenance rule) to restore the ≥6 un-started floor after H172 shipped. H172 pinned the `kb --stale --source S`↔`doctor.custody.summaries.by_source[S]` signal-clears tie in `tests/test_kb.py` (where the slice spec placed it); the convergence suite — the one home for the cross-surface custody ties (H157/H169 whole-library/scoped `by_source`, H171's `summaries.by_source` read tie, H154's *enrichment*-axis per-source refresh tie) — does not yet carry the summary-axis per-source *refresh* tie beside its enrichment sibling. Fold it in: over a multi-source stale-summary seed, assert the concept set `kb --stale --source S` regenerates equals `doctor`'s `custody.summaries.by_source[S]` offenders **and** that refreshing clears exactly that source's entry while leaving the other sources' (the multi-source-cluster case included — refreshing under one of a cluster's sources clears *all* its sources, the H171/H172 attribution). Non-vacuous (the two sources differ; a multi-source cluster spans both) and mutation-checked (perturbing the refresh scope breaks the tie). The summary-axis sibling of H154's enrichment-axis convergence test, completing the per-source refresh convergence on *both* enrichment axes in the one suite. Test + docs only (convergence-module docstring broadened; `docs/cli.md` kb `--stale --source` cross-ref to the suite); no production change. Precondition: H172 (the per-source refresh, shipped); H154 (the enrichment-axis convergence-test precedent, shipped); H171 (`summaries.by_source`, shipped). | → cap 8, cap 1 |

If the queue empties before the day does, deepen tests/fixtures on the slice
just shipped or pick the next-highest PRD capability — never manufacture
cosmetic churn (CLAUDE.md, *Avoid trivial progress*).

---

## 3-day plan (tentative) — through 2026-06-19

- **Day 1 (2026-06-16):** M1 (refresh-safe generated artifacts, ADR 0102)
  **complete** — shipped for compiled `library/` pages (H1+H2) and generated
  `agents/` instruction files (H3), with the architecture/library-format docs
  updated (H4). M2 (completeness invariant) **started**: the contract is written
  and its G1 half (honest absence/failure) is locked by `tests/test_completeness.py`
  (H5); G2 (scope echo + truncation) is enforced on `search` + `list` via the
  opt-in `--stats` envelope (`src/scrolls/scope.py`, H6), extended to
  `related` + `works` + `context` (H7 — `related`/`works` carry the scope
  echo, `context` a `Coverage:` line over `count_matches`), and finished on
  `doctor` (H8 — the drift block's `basis`/`as_of`/`unverified` make it
  verified-now-vs-as-of-last-check honest). **M2 complete: G2 enforced across
  every read surface, ahead of the Day-2 target.** **M3 (progressive context
  budgets) also shipped Day 1** (H9+H10): `scrolls context --budget
  {index,connected,full}` bounds bundle depth through nested tiers, identity/
  index first → deep bodies on demand, with a `_Budget:_` depth-honesty note
  and same-work collapse holding at every tier. Full suite green (2375).
  **M4 (shareable custody bundle) also shipped Day 1** (H12–H15, ADR 0103):
  `scrolls export bundle` / `import bundle` — a self-contained Markdown briefing
  with an embedded lossless custody block, verified round-trip across a fresh
  library. Done as one slice because the round-trip core (ADR 0082) already
  existed; the bundle is an envelope.
- **Day 2 (2026-06-17):** M2, M3, M4, **and M5 all done early** — the whole MVP
  shipped ahead of schedule. M5 (H16–H17) landed as one slice: the dogfood proof
  *hold → prove → detect → take it with me* runs offline against fixtures
  (`tests/test_dogfood.py`), is narrated in `docs/dogfood.md`, and is referenced
  from the vision's §8 success section. **MVP M1–M5 complete.** Next (live
  un-started set, the per-source/attention custody arc): the JSON `by_source`
  cross-surface convergence invariant (H157), the readable weakest-source
  `_Attention:_` line on the bundle/context briefing (H159) and its full-shape
  convergence invariant (H160), the MCP custody read `get_library_health` (H161),
  the scoped `doctor --source <S>` audit (H162), and the MCP browse-twin
  `by_source` stats (H163). The accreted Day-1/2/3 changelog above is itself
  overdue for the **full re-derivation** the H156 checkpoint owns.
- **Day 3 (2026-06-18 → 2026-06-19):** Post-MVP. The cap-8 enrichment
  provenance baseline (H19) and re-derivable classification (H20, the rules
  engine recording `classified_basis` + `classified_ruleset`, surfaced as a
  shared `classification` view) are **shipped**, and that fingerprint is now
  **actionable in `doctor`** — the `custody.enrichment` stale-ruleset block
  (H25, shipped). The classification view now also rides `search`/`search_scrolls`
  hits (H26, shipped) via the shared `search.hit_payload` serializer, so how a
  category was produced reads identically on **every** browse surface — the
  parity the confidence marker assumes. The facets leg of that parity (H28 —
  `facets method`) and the explicit refresh that acts on the stale signal
  (H27 — `classify --stale`, which closes the loop H20 → H25 → H27) are
  **shipped**. The per-item confidence/recency marker (H21 — `confidence`
  `level`/`freshness` on the classification view, convergent with doctor) is
  **shipped**, completing the classification axis of cap 8's "an agent knows
  what to trust". The **summary-axis counterpart is now also shipped**: the
  `summary_provenance` view + doctor `custody.summaries` stale-summary block
  (H29 record/report), the explicit `scrolls kb --stale` refresh that clears it
  (H31, closing the H29→H31 loop the way H20→H25→H27 closes on classifications),
  and the summary axis pinned into the cap-8 cross-engine invariant
  (H33). **The cap-8 enrichment theme — record → report → refresh →
  confidence/view → cross-engine invariant — is complete on both axes.** The
  hourly worker is now framed as scheduled custody maintenance: **`scrolls
  maintain` is shipped** (H22/H23/H34 in one slice) — recheck → regenerate →
  audit → custody delta vs the last run, report-only and idempotent, proven
  offline in `tests/test_maintain.py`. The completeness invariant now also
  covers `maintain` (H37, shipped), and **enrichment provenance travels in the
  shareable bundle's readable briefing** (H35, shipped — the per-scroll
  `classification` view + a `--concept` bundle's `summary_provenance`, derived
  read views outside the lossless JSONL fence). Per-scroll drift posture in the
  bundle briefing (H42) and `export bundle` folded into the completeness
  invariant (H43) are **shipped**, and the maintenance **trend log** is now also
  shipped (H36 — each pass appends `{recorded_at, snapshot, delta}` to an
  append-only `.maintenance/log.jsonl`; `maintain --history [N]` reads the
  score/drift trajectory back). The **custody-headline family is complete** —
  the scope custody picture rides `status` (H38), the bundle briefing (H45), the
  `context` bundle (H47), the `graph` stats block (H52), and `facets
  fidelity`/`drift` (H48), pinned by the H50 cross-surface convergence invariant
  (six surfaces). The **drift-posture axis is now also browsable and complete
  per-item**: `facets drift` (H48) counts it, `verify --unverified` (H51) and
  `list --drift` (H54) act/enumerate on it, the per-item posture rides `related`
  hits + `graph` nodes (H56) alongside the bundle briefing (H42), and **it now
  also rides the *primary* browse rows** — `list` rows + `search` hits (H58) —
  with the per-item cross-surface invariant pinned (H59), extended to the
  *inspect* surface `show`/`get_scroll` (H61) and the `works` representation
  shape (H64). **The per-item drift-posture theme is now complete across all
  seven surfaces: the per-item custody picture (`fidelity` + `drift`) reads
  identically on `list`/`search`/`related`/`graph`/bundle/`show`/`works`.** The
  **per-excerpt provenance theme** is complete too: each `scrolls context`
  excerpt at the `full` budget now carries both a classification-provenance tag
  (H44) and a drift-posture tag (H62), landed in one slice with a shared
  `items.classification_phrase` so the method/confidence reads byte-identical to
  the bundle briefing. The per-item ledger timeline (`scrolls history`, H66),
  **portable custody** (the verify ledger travels in the shareable bundle and as
  `export events`, H67/H72), and the **per-item custody markers on the compiled
  `library/` pages** (H89, the human-readable surface the per-item picture
  skipped) are now all **shipped** — the per-item custody picture reads the same
  on every agent surface *and* the compiled library. The **scope custody
  headlines** then completed across the compiled `library/` group + index pages
  (H95/H96) and were pinned into the convergence invariant (H97), and the
  **`stats.custody` family** completed across `search`/`list`/`related`/`works`/
  `graph` (H98/H99/H100) with its own cross-surface invariant (H101). The
  **per-source custody breakdown** then deepened across `doctor` and the `maintain`
  report — `custody.by_source` carries `{tiers, drift}` (H104), the single weakest
  source `attention` (H119), and per-source recheck `coverage` (H121) — with the
  custody headline now on the `maintain` report (H103) and the rendered headline on
  `status` (H117). The *act* that re-verifies one source (`verify --source <S>`,
  H125) and the `maintain`↔`doctor` per-source convergence invariants (H127/H129)
  are now **shipped** — so the per-source custody picture is read/act/report-complete
  and the scheduled report can never silently desync from the audit. The
  **`maintain --trend` enrichment/summary staleness trajectory** (H131) is now also
  shipped — `--trend` reports a `stale_change` axis beside `coverage_change`, both
  kept out of the integrity-first `posture`. The **per-source breakdown on `status`**
  (H133) is now shipped too — `status` carries the `by_source` map, the status-surface
  counterpart of the `maintain`/`doctor` per-source picture. The per-source breakdown
  in the `export bundle` briefing (H141), the per-source enrichment staleness in the
  `doctor` breakdown (H135), the `verify --source` recheck command in the
  `maintain`/`status` `attention` block (H137/H139), the per-source breakdown on the
  compiled `library/index.md` landing page (H145), and the trend-axis ≡
  telescoped-delta convergence invariant (H143) are now **shipped** too. The
  per-source enrichment staleness in the `maintain` report (H147) and the
  per-source `_By source:_` breakdown on the `scrolls context` model-facing bundle
  (H149 — the readable per-source line now reads byte-identical across
  `status`/`export bundle`/compiled `index.md`/`context`) are now **shipped** too.
  Next (the live un-started set): the per-source breakdown on the `graph` stats
  block (H150), the per-source rendered-line cross-surface invariant (H151), the
  per-source breakdown on the compiled group list pages (H152), the per-source
  `coverage` axis in the `status`/`maintain` weakest-source flag (H153), the
  enrichment-axis per-source refresh `classify --stale --source <S>` (H154), and
  the per-source `by_source` member on the browse-stats `stats.custody` envelopes
  (H155). A **full** re-derivation of the 3-day narrative (it has accreted into a
  changelog, against maintenance-rule §4) is the next dedicated checkpoint's task
  (H156); the day/week plans remain in-horizon and mapped to
  `docs/product/mvp.md`.

Each day ends on a committed, tested, clean stopping point. Slips roll forward;
the 3-day plan is re-derived at each buffer refresh.

---

## Week plan (more tentative) — through 2026-06-23

- Complete M1–M5 (the MVP) with the dogfood flow referenced from the vision's
  success section.
- Deepen provenance-complete enrichment (cap 8): classification + LLM concept
  summaries record inputs/method and regenerate deterministically; surface a
  confidence/recency marker (obsidian "confidence levels" adoption).
- Frame the hourly worker as scheduled **custody maintenance** (audit → drift
  recheck → regenerate views → report) once M1/M2 make regeneration and audit
  trustworthy — the obsidian "scheduled agents" adoption, custody-shaped.
- Consider a bi-temporal framing pass on drift events (captured-at vs
  source-changed-at) *only if* an agent workflow shows the event record is
  insufficient; otherwise keep deferred (MVP "out of scope").
- Explicitly **not** this week: new adapters, productivity surfaces, paid
  research integrations.

---

## Maintenance rule — keeping the 24h buffer fresh

The buffer rots if nobody refreshes it. The rule:

1. **Every run, before stopping**, the worker (or the agent it drives) checks
   whether the 24h buffer's lead slot is done. If the buffer has fewer than ~6
   un-started slots, it **appends** new concrete slices derived from the MVP and
   PRD so the lead always covers the next ~24h.
2. **At least once per 24h** (slot H11 above, or the first run after midnight
   UTC), do a full refresh: mark completed slots, prune slices overtaken by
   events, re-derive the 3-day plan, and re-confirm the week plan still maps to
   `docs/product/mvp.md`.
3. **Date discipline:** always write absolute dates (`YYYY-MM-DD`), never
   "today" — this doc is read later (obsidian anti-pattern: `date: today`).
4. **Provenance:** when a slice ships, the commit subject and any ADR are the
   record; this doc points forward, not backward. Don't turn it into a
   changelog — git is the changelog.
5. **No invented work:** a slice goes on the buffer only if it has a concrete
   implementation path and maps to a PRD capability. If none qualifies, the
   correct buffer entry is "report blocker," not filler.

---

## How this maps to the PRD/MVP and current priorities

| Roadmap horizon | PRD capability | MVP slice |
| --- | --- | --- |
| 24h H1–H4 | cap 6 (refresh-safe artifacts) | M1 + ADR 0102 |
| 24h H5–H8 | cap 7 (completeness invariant) | M2 |
| 24h H9–H10 | cap 10 (context budgets) | M3 |
| 24h H12+ | cap 9 (custody bundle), cap 11 (dogfood) | M4, M5 |
| 24h H19–H21 / Week | cap 8 (re-derivable enrichment, confidence markers) | post-MVP |
| 24h H22–H23, H34 / Week | scheduled custody maintenance — `scrolls maintain` (cap 1 + maintenance framing) | post-MVP |
| 24h H36–H39 / Week | maintenance trend log, completeness over maintain, custody on `status`, HTML briefing (cap 1/7/9) | post-MVP |
| 24h H42–H64 / Week | per-item drift posture everywhere (bundle, facets, related/graph, browse rows, inspect, works) + per-excerpt `context` provenance/drift tags + cross-surface invariants (cap 1/7/8/10) | post-MVP |
| 24h H65–H67 / Week | per-item custody on compiled `library/` pages, the per-item ledger timeline (`scrolls history`), portable custody — ledger travels in the bundle (cap 1/9) | post-MVP |
| 24h H72–H73 / Week | whole-library portable custody (`export`/`import events`) + the portable-custody round-trip invariant (cap 7/9) | post-MVP |

The two obsidian-second-brain adoptions (M1 refresh-safe regeneration, M2
completeness invariant) are intentionally first in the queue: they are the
decision-grade, custody-deepening slices, and everything later reads cleaner
once regeneration is non-destructive and results are scope-honest.
