# Architecture Decision Records

One short record per consequential decision, written when the decision is
made (see `docs/agents/domain.md`). All records below are accepted and
implemented on this branch; `docs/architecture.md` describes the system
they add up to.

| ADR | Decision | Consequence in the code |
| --- | --- | --- |
| [0001](0001-implementation-stack.md) | Python ≥3.11 + uv, src layout, stdlib-first dependencies | `pyproject.toml`; per-adapter deps must pay their way |
| [0002](0002-first-fetch-adapter-wikipedia.md) | First fetch adapter is Wikipedia via the MediaWiki action API | fixed the adapter contract: `ScrollItem -> ScrollItem`, `FetchError`, stage `detected → fetched` |
| [0003](0003-youtube-adapter-oembed-transcripts.md) | YouTube via keyless oEmbed + optional transcripts, not yt-dlp | caption-less videos degrade to metadata-only scrolls |
| [0004](0004-rules-classification-engine.md) | Classification starts with a deterministic rules engine | `rules-v1`; unmatched items stay unclassified for a future LLM engine |
| [0005](0005-deterministic-kb-compiler.md) | KB compiler is a deterministic frontmatter rollup | `library/` rebuilt from scratch each run; concept pages merge by slug |
| [0006](0006-agent-install-stays-in-library-root.md) | `agent install` writes only under the library root | never edits `~/.claude` or other tools' config trees |
| [0007](0007-github-adapter-topics-as-concepts.md) | GitHub via keyless REST; repo topics become concepts | first `concepts` producer; `GITHUB_TOKEN` lifts the rate limit |
| [0008](0008-arxiv-adapter-atom-abstracts.md) | arXiv via the Atom export API; abstract becomes the summary | taxonomy codes go to `tags`; name mapping deferred |
| [0009](0009-fieldtheory-import.md) | X bookmarks arrive via Field Theory import, not native sync | `import fieldtheory`; JSONL lines preserved in `raw_text`; ids dedupe against `scrolls add` |
| [0010](0010-arxiv-pdf-full-text-pypdf.md) | arXiv PDF full text extracted inline with pypdf | PDF failure degrades to the abstract-only scroll |
| [0011](0011-media-capture-command.md) | Media capture is an explicit `scrolls media` command, not part of fetch | media refs gain a root-relative `path`; `media/` tree is cache, not canon |
| [0012](0012-arxiv-taxonomy-names-as-concepts.md) | arXiv taxonomy display names become concepts via a bundled table | papers join the KB concept graph; codes stay in `tags`; supersedes 0008's deferral |
| [0013](0013-generic-pdf-adapter.md) | Generic PDF URLs get a fetch adapter via pypdf | every detected source except `x` now fetches; the binary is the raw record (media ref + content hash), not `raw_text` |
| [0014](0014-mcp-server.md) | `scrolls mcp` serves the library over stdio via the official MCP SDK | tools wrap the same engines as the CLI; ingest chain extracted to `src/scrolls/pipeline.py`; SDK imported lazily |
| [0015](0015-llm-classification-engine.md) | LLM classification is an explicit opt-in: `classify --engine llm` via the Anthropic SDK | fills `domain` + merges `concepts`; structured outputs pin the category vocabulary; missing credentials abort the batch |
| [0016](0016-config-toml-classify-section.md) | `config.toml` is read, starting with the `[classify]` section | `default_engine` + `llm_model`; flag/env overrides always win; malformed config errors honestly; only the CLI loads config |
| [0017](0017-feed-subscriptions-sync.md) | Sync is feed subscriptions, not per-platform sync commands | `follow`/`unfollow`/`sync` over RSS/Atom via stdlib; `subscriptions` table (schema v4); sync registers at `detected`, adapters still fetch |
| [0018](0018-user-overrides-scrolls-set.md) | User overrides are `scrolls set`, free-form and clearable | IDEAS.md §8 layer three; engines' fields only; empty value clears; all-or-nothing parsing; no override bookkeeping |
| [0019](0019-feed-http-caching.md) | Feed polling uses HTTP conditional GETs | `etag`/`last_modified` on subscriptions (schema v5); 304 → status `unchanged`; follow never seeds the cache — only a full sync stores validators |
| [0020](0020-mcp-feed-subscriptions.md) | The MCP server exposes feed subscriptions | `follow_feed`/`unfollow_feed`/`list_feed_subscriptions`/`sync_feeds`; batch semantics shared with the CLI via `feeds.sync_many` |
| [0021](0021-feed-entry-published-dates.md) | Feed entry dates seed `published_at`, normalized to UTC ISO 8601 | RSS `pubDate`/Atom `published`-else-`updated`; adapters' own date wins, seed is the fallback; unparseable dates become None |
| [0022](0022-batched-llm-classification.md) | Batched LLM classification is `classify --engine llm --batch` via the Message Batches API | same engine, half the per-token price; block-and-poll, no persisted batch state; per-request failures fail their item, whole-batch failures abort |
| [0023](0023-url-normalization-item-identity.md) | URLs are normalized at registration; the normalized form is identity and storage | `normalize_url` strips tracking params/fragments/default ports before hashing; `related` matches raw + normalized; feed subscription URLs untouched; closes ADR 0017's identity quirk |
| [0024](0024-uniform-published-at.md) | `published_at` is UTC ISO 8601 from every writer | shared `dates.to_utc_iso`; adapters normalize source dates, unparseable ones degrade to the seed; date-only becomes midnight UTC; closes ADR 0021's dialect note |
| [0025](0025-llm-concept-summaries.md) | LLM concept summaries are stored data the deterministic compiler includes | `kb --engine llm` fills a `concept_summaries` store (schema v6); members fingerprint makes reruns free; 2+ scroll concepts only; shared LLM transport extracted to `scrolls/llm.py`; closes ADR 0005's open slot |
| [0026](0026-doctor-command.md) | `scrolls doctor` diagnoses index/file-tree drift; `--fix` repairs only what is safe offline | merges pre-normalization duplicates into the canonical id (atomic `replace_items`), rewrites missing scrolls, rebuilds FTS; missing media and orphan scrolls are report-only; exit 0 = fully consistent; closes ADR 0023's open migration debt |
| [0027](0027-rm-command.md) | `scrolls rm` removes items — row, scroll file, captured media — files first, row last | id-or-URL refs resolved like `add` mints ids; root-escape guard before any deletion; no tombstone (a still-followed feed re-registers); not exposed over MCP |
| [0028](0028-item-refs-accept-urls.md) | Every item-id argument accepts the item's URL | `pipeline.resolve_item_id` shared by all id-taking commands; misses report the resolved id `(from <url>)`; subscription ids separate; MCP deferred; generalizes ADR 0027's rm handles |
| [0029](0029-google-takeout-import.md) | YouTube history arrives via Google Takeout import, at stage `detected` | spine-only archive: watch time → `saved_at`, never `published_at`; repeat watches collapse to earliest; ads/deleted/non-video entries counted, never fatal; zip, directory, or the JSON file itself |
| [0030](0030-browser-bookmarks-import.md) | Browser bookmarks arrive via Netscape-HTML import, folders become tags | heterogeneous spine: every http(s) URL detects like `add`; folder ancestry → `tags` (root containers excluded), never `category`; earliest `ADD_DATE` wins, tags union; stdlib HTMLParser token stream; bookmarklets/`place:` counted, never fatal |
| [0031](0031-hacker-news-adapter.md) | Hacker News via the keyless Firebase API; link posts degrade to metadata-only | host→`hackernews`, only `/item?id=` fetches; text posts → body + lead summary, link posts → "N points, M comments"; article URL bare in `links`; Show HN → project, Ask HN unclassified; `kids` kept in `raw_text` |
| [0032](0032-batched-concept-summaries.md) | Batched concept summaries are `kb --engine llm --batch`; the Message Batches transport moves to the shared `llm.py` | `anthropic_complete_batch(…, *, schema, max_tokens)` shared by both LLM engines; positional `concept-<i>` ids; per-concept failures still compile, whole-batch failures abort; classify's wrapper re-tags errors; closes the architecture doc's open next step |
| [0033](0033-stack-exchange-adapter.md) | Stack Exchange via the keyless API; one adapter for the whole network, question + top answers, tags as concepts | host→`stackexchange` with `<site>:<id>` source_id; questions only fetch; answers optional second GET, accepted answer leads; tags → `concepts`; weak default `reference` (title rules win); eighth keyless adapter |
| [0034](0034-pypi-adapter.md) | PyPI via the keyless JSON API; identity is the PEP 503 name, keywords as concepts, classifiers as tags | host→`pypi` with normalized-name source_id (version-page dedupes); fetch resolves latest release; description → searchable text; keywords → `concepts`, classifiers → `tags`, project URLs → `links` (package↔repo edge); curated `pypi → tool`, first rule to produce `tool`; ninth keyless adapter |
| [0035](0035-npm-adapter.md) | npm via the keyless registry; README from the packument or, when empty, its tarball | host→`npm` with verbatim (case-sensitive) name source_id, scoped names and version-page dedupe; fetch resolves `dist-tags.latest`; README two-tier (packument `readme`, else capped `dist.tarball` extract — `http.get_bytes` gains `max_bytes`); keywords → `concepts`, no classifier analog so `tags` empty; homepage + normalized repository → `links` (package↔repo edge); `npm → tool`; tenth keyless adapter |
| [0036](0036-crates-io-adapter.md) | crates.io via the keyless JSON API; README from the `.crate` tarball | host→`crates` with folded (case-insensitive, `-`≡`_`) name source_id (version-page dedupes, like PyPI's PEP 503); fetch resolves `default_version`, reads canonical name back; README always from the capped `.crate` tarball (reuses npm's `max_bytes`); keywords → `concepts`, curated category taxonomy → `tags`; homepage/docs/normalized repository → `links` (crate↔repo edge); `crates → tool`; eleventh keyless adapter |
| [0037](0037-crossref-doi-adapter.md) | Crossref via the keyless DOI metadata API; the published-literature sibling of arXiv | host `doi.org`/`dx.doi.org`→`crossref` with the DOI folded lowercase as source_id (case-insensitive, dedupes the legacy resolver); fetch reads `api.crossref.org/works/<doi>`; JATS abstract stripped to a plain `summary`, no `extracted_text` (no full text); `subject` → `concepts`, `type`+venue → `tags`; only `resource.primary.URL` → `links`, `reference` DOIs dropped; date precedence `issued`→…→`created`; `crossref → paper` like arXiv; twelfth keyless adapter |
| [0038](0038-arxiv-published-doi-link.md) | arXiv records its published DOI as a link — the preprint↔published edge | the `arxiv:doi` Atom element becomes a `https://doi.org/<doi>` link (arXiv previously emitted no `links`); `related` folds it to the `crossref:<doi>` id across the DOI case fold, wiring a preprint to its published paper both ways with no new edge type or schema change; no DOI → empty links; `arxiv:journal_ref` stays in `raw_text` |
| [0039](0039-packagist-adapter.md) | Packagist via the keyless JSON API; the PHP/Composer sibling of pypi/npm/crates | host→`packagist` with the `vendor/name` folded lowercase (case-insensitive, two-segment, version-page/`.json`-strip dedupe); fetch ranks `versions` by the numeric core of `version_normalized`, highest stable wins (no `default_version` pointer, no comparator dependency); no README in the API → metadata-only, description → `summary`; keywords → `concepts`, `type`+SPDX licenses → `tags`; repository/homepage/git-source → `links` (package↔repo edge); `packagist → tool`; thirteenth keyless adapter |
| [0040](0040-rubygems-adapter.md) | RubyGems via the keyless JSON API; the Ruby sibling of pypi/npm/crates/packagist | host→`rubygems` with the gem name verbatim (case-sensitive registry, npm's rule; version-page dedupe); `gems/<name>.json` returns the latest version inline (no `versions` collection, no comparator); no keywords → `concepts` structurally empty, no README → metadata-only with `info` → `summary`; SPDX licenses → `tags`; homepage/source/docs URIs → `links` (gem↔repo edge survives a tagged-tree source URI); `rubygems → tool`; fourteenth keyless adapter |
| [0041](0041-huggingface-hub-adapter.md) | Hugging Face Hub via the keyless API; one adapter for models + datasets, the arXiv tag as a cross-source link | host→`huggingface` (also `hf.co`) with the repo kind in the source id (`model:`/`dataset:`, Stack Exchange's one-adapter-many-kinds), id verbatim (case-sensitive), two-segment + subpage dedupe, reserved-path table; fetch routes on kind, card README (frontmatter stripped) → `extracted_text` + lead-paragraph `summary`, degrades to metadata-only; concepts from structured fields (`pipeline_tag`/`task_categories` + `cardData.tags`), not the flat tag soup; `library_name`+license → `tags`; `arxiv:`→arxiv.org link (model↔paper edge, ADR 0038 kin), `dataset:`→Hub link (model↔dataset edge); `model → tool`, `dataset → dataset`; fifteenth keyless adapter |
| [0042](0042-go-modules-adapter.md) | Go modules via the keyless `proxy.golang.org`; the last registry on the JSON-metadata pattern | host `pkg.go.dev`→`go` with the module path verbatim (case-sensitive; module = everything before `@`, so a versioned sub-package dedupes; first segment must be a domain, so stdlib/site routes carry no fetchable module); fetch case-encodes the request (`X`→`!x`), `/@latest` → version+time+`Origin`, `/@v/<v>.mod` → the go.mod as searchable `extracted_text` (the sparsest adapter: no description so `summary` None, no keywords so `concepts=()`, no license/classifier facet so `tags=()`); repo link from `Origin.URL` (the only truth for a vanity path) else derived from the module path for known VCS hosts (package↔repo edge); `go → tool`; degrades to metadata-only; sixteenth keyless adapter, completing the registry family |
| [0043](0043-huggingface-spaces-subadapter.md) | Hugging Face Spaces as a third repo kind on the Hub adapter | `detect_source` adds a `spaces/<org>/<name>`→`space:<id>` branch (the dataset branch's twin); a `_PATH_SEGMENT` kind→segment map drives the `/api/spaces` endpoint, site prefix, and card URL (replacing the two-way dataset ternaries); `sdk` fills the `tags` facet (`library_name`'s slot), `cardData.title` the title (`pretty_name`'s slot), `cardData.tags` the concepts; `cardData.models`/`datasets` → space↔model/dataset links surviving a card-less degrade; `space → tool` like a model; model/dataset behavior byte-unchanged; extends ADR 0041 |
| [0044](0044-link-graph-command.md) | `scrolls graph` — the cross-item link graph the adapters built, materialized as a first-class view | new `graph.py` extracts the link-resolution primitives (`link_tokens`, `identity_tokens`) out of `related.py` (which becomes a byte-identical wrapper, one definition for the per-item and whole-library views); `build_graph` indexes every item's identity tokens once then probes with each link (linear, not the per-pair O(n²)); directed edges `from→to` with the matching link as `via`, parallel links collapse, self-links drop, connected nodes by default (`--all` for isolates); `to_payload` `{nodes, edges, stats}` shared by `scrolls graph` and the MCP `get_link_graph` tool; no network, no model, empty library → empty graph; edges intentionally untyped this slice (relation labels, a KB `graph.md` page, concept/tag edges deferred) |
| [0045](0045-datacite-doi-fallback.md) | DataCite via a fetch-time fallback on the same `doi.org` detection — datasets/software join the published literature | a `doi.py` dispatcher backs `FETCH_ADAPTERS["crossref"]`: try `crossref.fetch_item`, fall back to `datacite.fetch_item` on its `FetchError` (a DataCite-only DOI 404s against Crossref), raise naming both if neither holds it; the registration agency can't be read off the URL and `crossref:<doi>` identity is fixed at `add` time (the ADR 0038/0041 edges depend on it), so the source stays `crossref` and `provenance.adapter="datacite"` tells the truth; `datacite.py` maps the JSON:API `attributes` (titles+subtitle, creators "Given Family", `Abstract` description → `summary` no full text, `subjects` → `concepts`, DataCite date precedence, `resourceTypeGeneral`+`resourceType`+publisher → `tags`, landing + container-DOI `links`); the resource type rides in `provenance.resource_type` so `classify.py` maps it (`Dataset → dataset`, `Software`/`Model` → `tool`, `Text`/article types → `paper`, `Image`/`Sound` → `media`, ambiguous types unclassified) — Hugging Face's repo-kind read (ADR 0041) but a fetch-time fact, not a detection-time one; first two fetch adapters on one detected source; `x` stays the only adapterless source |
| [0046](0046-lobsters-adapter.md) | Lobsters via the keyless JSON API; the whole discussion in one request | host `lobste.rs`→`lobsters` with the `/s/<short_id>` short id verbatim (slug stripped; `/c`,`/t`,`/u`,front pages carry no fetchable story — HN/SE's pattern); a story's `.json` returns submission + tags + the entire comment thread in *one* request (HN defers comments, SE spends a second GET), `description_plain`/`comment_plain` already plain so no HTML grammar; body + comments (deleted/moderated skipped, bylined like SE answers) → searchable `extracted_text`, all comments kept (free, no cap); link submission's article → bare `links` (HN link-story pattern), text submission's body the searchable content; `summary` = body lead else `"N points, M comments"`; tags → `concepts` (github-topics pattern); **no category default — unclassified like Hacker News**, not SE's weak `reference` (a heterogeneous aggregator entry); raw story (comment `depth` included) kept in `raw_text`; honestly keyless where Reddit's `.json` now 403s; seventeenth keyless adapter |
| [0047](0047-context-connected-scrolls.md) | `scrolls context` surfaces connected scrolls from the link graph | a "Connected scrolls" section between Excerpts and Links lists items linked to/from the matches (`graph.build_graph`, the same edges `scrolls graph` reports) but not themselves keyword hits; link-only (concept/tag signal deferred); ranked by centrality then best-match rank then id, capped at the match count; direction names the match ("links to"/"linked from", `+N more`); conditional (no edges → no section); titles from the graph's nodes, no extra `get_item`; flows to the MCP `get_context_bundle` for free; the cross-source edges of ADRs 0034–0046 finally surface in the bundle an agent reads |
| [0048](0048-bluesky-adapter.md) | Bluesky via the keyless AppView; the open social post X could never be | host `bsky.app`→`bluesky` with `<actor>/<rkey>` source_id (actor folded lowercase — handle/DID are case-insensitive — rkey verbatim; profile/feed/list/home routes carry no fetchable post); a handle URL costs one `resolveHandle` GET for the DID the AT-URI needs (the SE/HF two-request shape), a `did:` URL skips it; one `getPostThread` returns the post + its nested reply tree; post text + bylined replies (deleted/blocked/empty skipped) → searchable `extracted_text`, image alt text is the body of a textless post; external card / quoted post / inline `#link` facets → `links` (post↔post and cross-source edges, ADR 0044), images → `photo` media (`@jpeg`→`.jpg` via the existing fallback), `#hashtag` facets → `concepts` (github-topics pattern); synthesized title, `summary` = lead else alt else card title else "N likes, M reposts, K replies"; **no category default — unclassified like Hacker News/Lobsters**; whole thread in `raw_text`; eighteenth keyless adapter, the deferred social-post source (IDEAS.md §6) reached on the open network |
| [0049](0049-mastodon-adapter.md) | Mastodon via the keyless REST API; a federated source matched by URL shape, not host | **first shape-only detection** (no host set — the Fediverse has none): on any host not already claimed, `/@<user>/<digits>` or `/users/<user>/statuses/<digits>` → `mastodon` with `source_id = <host>/<status_id>` (host in identity since ids are instance-local; the two forms dedupe); the all-digits id is the safety that keeps Medium `/@user/<slug>` and Threads/TikTok three-segment URLs out, and a misdetect degrades to a benign failed fetch (API 404), never a wrong scroll; status (`/api/v1/statuses/<id>`) + context (`.../context`) two requests, the second skipped when `replies_count==0` and degrading on failure (HN/Bluesky economy); HTML `content` → text via stdlib `HTMLParser` (no trafilatura); flat `descendants` → bylined replies in one pass; `card`+plain body links → `links` (mention/hashtag anchors excluded), images → `photo`/videos → `thumbnail` media (alt searchable), `tags[].name` → `concepts`; `spoiler_text` content warning leads the body; a boost unwraps to the boosted post; synthesized title, `summary` = lead else alt else card title else "N favourites, M boosts, K replies"; **no category default — unclassified like Bluesky/HN/Lobsters**; whole `{status,context}` in `raw_text`; nineteenth keyless adapter, the second open social network |
| [0050](0050-fediverse-forks-mastodon-api.md) | GoToSocial and Pleroma/Akkoma join the Mastodon adapter by URL shape | the sequel ADR 0049 named: same `mastodon` source + fetch adapter (the forks serve identical Mastodon-shaped `/api/v1/statuses/<id>`+`/context` JSON, the status id opaque to the adapter), only `detect.py` learns their routes — GoToSocial `/@<user>/statuses/<ULID>`, Pleroma/Akkoma `/notice/<FlakeId>`, the shared AP `/users/<user>/statuses/<id>` — and non-numeric ids (ULID, FlakeId); safety generalizes from "id is numeric" to "a distinctive literal anchors the shape, the id constraint as strict as the anchor is weak": `statuses`-bearing forms admit any base62 id, the weak `/notice/` form needs a 16-char length floor a FlakeId clears but `/notice/privacy` does not; `/objects/<uuid>` left to `web` (not a status id); the Mastodon `/@<user>/<digits>` form stays strict-numeric (keeps Medium out); identity still `<host>/<status_id>` (web+AP dedupe), non-numeric ids verbatim; provenance stays `mastodon` (the adapter that served it; the fork unknowable from the URL, unlike DataCite-via-Crossref); no new source/adapter/schema; home-instance-canonical identity deferred like ADR 0048/0049 |
| [0051](0051-misskey-adapter.md) | Misskey via its own keyless API — a Fediverse source that is *not* Mastodon-API-compatible | corrects ADR 0049/0050's assumption that Misskey would be another mastodon shape: it speaks its own API (`POST /api/notes/show` with a JSON body, not `GET /api/v1/statuses/<id>`) with its own note shape (`text` MFM not HTML, `cw`, `user`, `files`+`comment`, bare-string `tags`, `renote`, `reactions` map), so it gets its own `misskey` source + adapter; still detected shape-only like mastodon (`_misskey_id` after `_mastodon_id`, literals never collide) — `/notes/<id>` → `misskey:<host>/<id>`, the weak `notes` literal carrying a base62 + length floor of 10 (the `aid` length; a slug's `-`/`.` or a sub-floor word falls to `web`), a misdetect a benign failed fetch; new reusable `http.post_json`; `notes/show` + `notes/children` two POSTs (second skipped when `repliesCount==0`, degrading on failure), flat children → bylined replies; MFM `text` needs no HTML parser (Lobsters' `*_plain` economy), links scanned from the text + a quote-renote's note (post↔post edge), pure renote unwraps (mastodon's reblog rule), `files` → `photo`/video-`thumbnail` media (`comment` alt searchable), bare `tags` → `concepts`, `cw` leads the body; synthesized title, `summary` = lead else alt else "N reactions, M renotes, K replies"; **no category default — unclassified like Bluesky/Mastodon/HN/Lobsters**; whole `{note,children}` in `raw_text`; twentieth keyless adapter, the third open social network |
| [0052](0052-lemmy-adapter.md) | Lemmy via its own keyless API — the federated link aggregator, a fourth Fediverse API | the third Fediverse split (mastodon/misskey/lemmy by client API, not host): Lemmy is a Reddit-shaped link aggregator (HN/Lobsters' federated cousin) speaking `GET /api/v3/post?id=<id>` + `/comment/list`, not Mastodon's or Misskey's API, so its own source + adapter; detected shape-only (`_lemmy_id` after `_misskey_id`, literals never collide) — `/post/<digits>` → `lemmy:<host>/<id>`, the weak `post` literal carrying an all-digits + exactly-two-segments guard (a `/post/<slug>` or `/post/<id>/<extra>` falls to `web`), a misdetect a benign 404; plain GET so the shared `http.get_json` serves it (no `post_json`); post + comments two GETs (second skipped when `counts.comments==0`, degrading on failure, bounded `max_depth`/`limit`); flat comments sorted into thread pre-order by integer `path`, bylined like Lobsters/SE (deleted/removed skipped); a *real* `name` title (not synthesized — it's an aggregator, like HN/Lobsters), `ap_id` canonical (origin instance); link post's `url` → article `link`, text post's Markdown `body` the content, image post's `url` → `photo` media (told by `url_content_type`), pict-rs `thumbnail_url` → preview `thumbnail`; body URLs scanned + `cross_posts` `ap_id` → `links` (cross-source + post↔post edges); community → one `concept` (subreddit-like topical home, github-topics pattern); `summary` = body lead else "N points, M comments"; **no category default — unclassified like HN/Lobsters/social**; whole `{post,comments}` in `raw_text`; v3 (near-universal; v4/Lemmy 1.0 keeps v3); twenty-first keyless adapter, the first federated link aggregator |
| [0053](0053-piefed-adapter.md) | PieFed via its own keyless `/api/alpha` — a fetch-time fallback behind Lemmy, not a new detected source | PieFed's post URL is byte-identical to Lemmy's `/post/<digits>` (autoincrement int ids), so it can't be its own *detected* source (no GoToSocial-style URL tell); but its API is its own (`/api/alpha` not `/api/v3`, `post.title`/`creator.user_name`/`comment.body`/`post_type` enum not Lemmy's `name`/`name`/`content`/`url_content_type`), so it can't ride Lemmy's *adapter* either — a hybrid needing its own adapter on Lemmy's source; resolved at fetch time by a new `threadiverse.py` dispatcher backing `FETCH_ADAPTERS["lemmy"]` (the `doi.py` pattern, ADR 0045): try `lemmy.fetch_item`, fall back to `piefed.fetch_item` on `FetchError` (a PieFed instance 404s `/api/v3`), raise naming both if neither serves it; Lemmy first (more deployed) so only PieFed pays the one-extra-request tax; identity stays `lemmy:<host>/<id>` (minted before the backend is knowable), `provenance.adapter="piefed"` is honest (the DataCite-vs-Crossref split); `detect.py` unchanged (pinned shared-shape test); the adapter mirrors Lemmy's behavior (post+comments two GETs, `path` pre-order, deleted/removed skip, community→concept, body+cross-post edges, no category default) mapping PieFed's field names — image by `post_type=="Image"`, cross-posts (no `ap_id`) → same-instance `/post/<id>` links, engagement summary "PieFed discussion: N points, M comments"; `sort` omitted (PieFed's vocab differs, `path` re-sort makes it moot); `Poll`/`Event` payloads kept in `raw_text`; twenty-second keyless adapter; corrects ADR 0052's "likely a fourth detected split" prediction |
| [0054](0054-discourse-adapter.md) | Discourse via the keyless `.json` view — a host-less *forum* source, and why Mbin is deferred | the centralized-forum sibling of the aggregators (HN/Lobsters/Lemmy/PieFed): host-less like the Fediverse but **the first non-Fediverse shape-only source** — `/t/<slug>/<topic_id>` on any unclaimed host → `discourse:<host>/<topic_id>` (slug display-only/dropped, id the all-digits third segment, trailing post-number dedupes; the weak `t` literal makes the id carry the weight, a misdetect a benign 404); `GET /t/<id>.json` returns the topic **and** its first page of posts in *one* request (Lobsters' economy, not two), a long thread bounded with the full `stream` in `raw_text`; a *real* `title` (a forum thread, not a synthesized social post), opening post = body, later posts → bylined `### Replies` (mod-action/whisper/deleted skipped), `cooked` HTML → text via stdlib `HTMLParser` (no trafilatura, Mastodon's rule); `tags` → `concepts`, outbound `details.links` (internal/reflection filtered) → `links` (cross-source edges), `image_url` → `thumbnail`; `summary` = OP lead else "N replies, M likes"; **no category default**; validates `post_stream` so a misdetect raises not mis-scrolls; twenty-third keyless adapter. **Mbin deferred**: its read API is OAuth-gated (security.yaml grants no anonymous `/api/entry`; fedia.io 401, kbin.earth 403), so it can't be keyless without a token dance — correcting ADR 0052/0053's "Mbin next" |
| [0055](0055-gitlab-adapter.md) | GitLab via the keyless REST API — the second major code host, the sibling of github | host `gitlab.com`→`gitlab` (host-scoped like github; self-hosted deferred — a bare repo root has no shape tell, unlike the Fediverse literals); identity is the whole `group[/subgroup…]/project` path before any `/-/` sub-resource separator (nested groups, so URL-encoded *whole* `%2F` not split like github's flat `owner/repo`; deep links dedupe; `group/subgroup` ambiguity resolved at fetch, the Go-module pattern), folded lowercase (GitLab forces lowercase slugs, routes case-insensitively — the crates/Packagist fold, not github's verbatim); `GET /api/v4/projects/<encoded>?license=true` keyless (`GITLAB_TOKEN`→`PRIVATE-TOKEN` lifts the limit, github's optional-token posture); README from `readme_url`'s `/-/blob/`→`/-/raw/` rewrite (no second file-tree call, exact since `/-/` is reserved), optional enrichment degrading to metadata-only; `topics`(→`tag_list` fallback) → `concepts` like github, SPDX `license.key` → one `tag` (the registries' license facet, free under `?license=true`); `gitlab → project` like github; twenty-fourth keyless adapter, the second code host |
| [0056](0056-gitea-forgejo-adapter.md) | Gitea/Forgejo via the keyless `/api/v1` — the third code host, where the API lives on each instance's own host | hosts `codeberg.org`/`gitea.com`→`gitea` (host-scoped like github/gitlab; self-hosted deferred — no shape tell); **first code host to carry the instance host in the identity** (`gitea:<host>/<owner>/<repo>`, the Fediverse `<host>/<id>` shape, ADR 0049) because the API is per-host (`codeberg.org/api/v1`, `gitea.com/api/v1`) not a single service — so reaching a self-hosted instance later is a detection-only change; `www.` folded off, owner/repo verbatim (case-insensitive routing, display case preserved — github's rule); one `gitea` source serves both Gitea and its API-compatible fork Forgejo (codeberg runs Forgejo, gitea.com runs Gitea — the mastodon/forks pattern, ADR 0050), `provenance.adapter` stays `gitea`; keyless `GET /api/v1/repos/<o>/<r>` (`GITEA_TOKEN`/`FORGEJO_TOKEN`→`Authorization: token` lifts the limit), inline `topics` → `concepts` like github (no second call); README is README.md-first via the **API** raw route (one fast GET — the web `download_url` 303-redirects anonymous gitea.com clients to login, and listing forgejo/forgejo's root times out) with a root-listing fallback for a non-`.md` README, degrading to metadata-only; no inline license so `tags` empty (github-parallel, not gitlab's license tag); `gitea → project` like github/gitlab; twenty-fifth keyless adapter, the third code host |
| [0057](0057-bitbucket-adapter.md) | Bitbucket Cloud via the keyless `/2.0` API — the fourth code host, single-service like github | host `bitbucket.org`→`bitbucket` (host-scoped, single fixed API host `api.bitbucket.org/2.0` — *not* host-in-id like gitea, because Bitbucket Cloud is one service; Bitbucket Server/DC is a different API, deferred like self-hosted GitLab); identity is the flat `<workspace>/<repo>` folded lowercase (Bitbucket auto-lowercases slugs and routes case-insensitively — the gitlab/crates fold, ADR 0055/0036 — a mixed-case request resolves 200; deep links dedupe; reserved site routes carry no repo); keyless `GET /2.0/repositories/<ws>/<repo>` (`BITBUCKET_TOKEN`→`Authorization: Bearer` lifts the limit, github's posture); **no topics so `concepts` empty by design** (the Go/RubyGems posture, ADR 0040/0042), `language` → the one `tag` (the registries' license-as-tag slot; github ignores language because topics fill that role); README via the `/src/<mainbranch>/<path>` route (no `/readme` endpoint like github, no `/raw/` route like gitea) — README.md-first (one fast GET reading `mainbranch.name`) then a root-listing fallback (`commit_file`/`commit_directory` entries) for a non-`.md` README, degrading to metadata-only (no main branch / 404 / blank / failed listing); title=`full_name`, author=`owner.display_name`(→handle), `created_on`→`published_at`, `links.html.href`→canonical; `bitbucket → project` like the other code hosts; twenty-sixth keyless adapter, the fourth code host |
| [0058](0058-faceted-search.md) | `scrolls search` gains `--source`/`--category`/`--stage` facets — the consumption side catches up to 30+ heterogeneous sources | `search_items` takes optional `source`/`category`/`stage`, AND-ed with the FTS `MATCH` and leaving BM25 order untouched (filters narrow, never reorder); mirrors `list_items` semantics exactly (`source`/`stage` exact, empty-string `category` → `IS NULL` unclassified — the `list`/`set` convention), filtering the joined `items` columns not the text index; wired through the CLI `search` flags (`--stage` with `list`'s `detected`/`fetched`/`rendered` choices) and the MCP `search_scrolls` params (the agent payoff: "papers about X", "this source's take on Y", "unclassified mentions of Z" in one call); the context bundle (`scrolls context`/`get_context_bundle`) inherits the identical facets through `build_context` → `search_items` (scoping the bundle's match also scopes its connected-scrolls graph) and a scoped bundle names its facets in the title; `None` defaults so every existing caller is unchanged; `tags`/`concepts` (JSON multi-value) facets deferred |
| [0059](0059-tag-concept-facets.md) | `search`/`list`/`context` gain `--tag`/`--concept` membership facets — the consumption side reaches the finest-grained structured signal | closes ADR 0058 §5's deferral: `tag`/`concept` filter by *membership* in the JSON list columns, not single-column equality, so they go on all three facet surfaces (`search_items`, `list_items`, `build_context`) to keep ADR 0058's list/search/context symmetry one level deeper; matching is what the rest of the system already uses — `tag` **case-insensitive**, `concept` **by slug** (the `scrolls related` comparisons, the KB/`get_concept_page` slug), so `--concept "full text search"` finds `Full-text search`; **no empty-string overload** (no "untagged" pool — `None` never filters, a value nothing has returns `[]`); single value per facet, AND-ed with the rest (repeated-flag AND within a facet deferred); filtered in SQL via `EXISTS (SELECT 1 FROM json_each(items.tags/concepts) …)` so `search`'s `LIMIT` stays correct, normalized by two functions registered per connection (`items.register_facet_functions`: `scrolls_slug`=`render.slugify`, `scrolls_lower`=`str.lower` because SQLite `lower()` is ASCII-only); the clause builder `items.tag_concept_filters` is shared by `list_items` and `search_items` (lazy `slugify` import dodges the `render`→`items` cycle); wired through the CLI `--tag`/`--concept` flags and the MCP `search_scrolls`/`get_context_bundle` params; context scope note reports them verbatim |
| [0060](0060-mcp-list-scrolls-tool.md) | an MCP `list_scrolls` tool — faceted enumeration for agents | the CLI `scrolls list` had no MCP counterpart (only `search_scrolls` query-ranked + `list_sources` counts), so an agent couldn't ask "what arxiv papers tagged efficient are in the library" — a facet question with no search query; new `list_scrolls` wraps `items.list_items` with the same five facets (ADRs 0058/0059, AND-ed, `""` category → unclassified, `tag` case-insensitive, `concept` by slug), returns the same `_cmd_list` summary shape oldest-first, mirroring the CLI's search/list pair (ranked-by-query vs enumerated-by-facet); the one divergence from the unbounded CLI list is a `limit` (default 50) — an agent pastes results into a context window, so a documented cap keeps it context-friendly and the agent raises it to see more; read-only, empty library → `[]`, no network/model/schema; joins the documented-tool set and the cli.md MCP table |
| [0061](0061-devto-adapter.md) | DEV (dev.to / Forem) via the keyless `/api/articles/<user>/<slug>` — a `web` island becomes a concept-graph citizen | a saved `dev.to` article fell through to `web`, which extracts the text but produces no `concepts`, so the post was an island invisible to `related`/concept facets/the KB; host `dev.to`→`devto` with the `<user>/<slug>` id folded lowercase (Forem mints lowercase handles/slugs and the case-sensitive API only resolves the lowercase form — the gitlab/bitbucket fold, ADR 0055/0057 — a mixed-case request 404s; deep links dedupe; reserved `t`/site routes carry no article); one keyless GET returns the whole article so `tags` → `concepts` finally joins the concept graph (github-topics pattern, ADR 0007), `tags` field empty (no license/classifier facet); the URL handle is the author *or organization* (the byline `user` is the person — `author` reads `user.name`), `body_markdown` → `extracted_text` (already Markdown, Lobsters' economy ADR 0046), `description` → `summary` (→ lead → engagement → None), cross-post `canonical_url` → `links` cross-source edge while the scroll's canonical stays the dev.to permalink, `cover_image`/`social_image` → `thumbnail` media (ADR 0011); **no category default — unclassified like HN/Lobsters/Bluesky** (title rules + LLM decide); self-hosted Forem deferred (no shape tell); twenty-seventh keyless adapter |
| [0062](0062-kb-link-graph-page.md) | `library/graph.md` — the link graph as a compiled, browsable KB page | closes ADR 0044's deferred "rendered KB `library/graph.md` page": the compiled library (`kb.py`, ADR 0005) rolled up every structured field *except* the links the adapters most deliberately built, so the connective tissue (model↔paper ADR 0041, preprint↔published ADR 0038, package↔repo ADRs 0034–0042) was queryable (`scrolls graph`, `related`) and in the context bundle (ADR 0047) but had no browsable page; new `graph.connected_components(graph)` partitions the graph into clusters (edges undirected for the partition, the directed edges kept on each component; ordered by node count then smallest id; union-find, linear), and `graph.graph_over(items)` — factored out of `build_graph`, which now delegates — builds the graph over a *given* item set so the compiler passes its **rendered** items only and every link on the page resolves to a scroll file (a link to an unrendered target drops, the rest of the KB's rule); `compile_kb` writes `library/graph.md` as an adjacency list — each cluster a `## Cluster N` of member bullets (the shared `_item_line`, relative to `library/`) with outbound edges nested as `→ target` — always written like the index (empty → `No linked scrolls yet.`), the index gains a one-line link to `graph.md`, and the compile summary gains `clusters` (+ `graph.md` in `pages`) through `scrolls kb`/`compile_library`; deterministic/offline like every KB page (no model, no network); `build_graph`/`scrolls graph`/`related`/the context bundle byte-unchanged; stays link-only and untyped, inheriting ADR 0044's concept/tag-edge and relation-label deferrals |
| [0063](0063-kb-related-concepts.md) | Related Concepts on KB concept pages — the deterministic concept-graph complement to the link graph | each concept page gains a `## Related Concepts` section listing the concepts that co-occur on its member scrolls, ranked by shared-scroll count; `kb.related_concepts` is a pure co-occurrence map over `group_concepts`, rendered via a generic `_write_page` `trailer`; deterministic/offline like every plain `scrolls kb` page, flowing to `get_concept_page` for free; lives on the concept pages (where neighbours are a short ranked list) *not* as link-graph edges (where concept cliques would swamp the sparse link edges, ADR 0044/0047/0062's deferral), resolving the concept half of that deferral; `build_graph`/`scrolls graph` byte-unchanged |
| [0064](0064-kb-tag-pages.md) | Tag pages in the compiled library — the first-class facet the KB could query but not browse | `tags` was a query facet (`--tag`, ADR 0059) and a field on every item, but the only grouping the KB compiled no page for — sources/categories/concepts each had one; `compile_kb` now writes `tags/<name>.md` per tag plus a `## Tags` index section and a `KbResult.tags` count (free to `scrolls kb`/`compile_library` via `asdict`); `group_tags` keys **case-insensitively** like the `--tag` facet (not concepts' slug merge), so `MIT`/`mit` are one page while `C++`/`C#` stay two despite a shared slug; `_tag_filenames` disambiguates slug collisions with a numeric suffix (`c-2.md`) in sorted-key order — fine because KB pages rebuild each run and link relatively; each page ends with a `## Related Tags` co-occurrence section (the tag analog of ADR 0063, sharing the extracted `_co_occurring` core with `related_concepts`), on the page not as `scrolls graph` edges for the same density reason (ADR 0044/0047/0062); deterministic/offline, closing the *tag* half of ADR 0063's deferral; `build_graph`/`scrolls graph` byte-unchanged |
| [0065](0065-pubmed-adapter.md) | PubMed via the keyless E-utilities efetch XML — the biomedical literature, MeSH as concepts, a DOI edge to Crossref | the largest scholarly corpus had no first-class home (a `pubmed.ncbi.nlm.nih.gov/<pmid>` URL fell through to `web`, losing the structured record — most of all its MeSH terms); host `pubmed.ncbi.nlm.nih.gov`→`pubmed` claimed wholesale (PMID = first path segment, subpages dedupe) while the legacy `ncbi.nlm.nih.gov/pubmed/<pmid>` form is *shape-matched* (that host also serves PMC/Gene/…, which fall through to `web`); one keyless `efetch.fcgi?db=pubmed&id=<pmid>&retmode=xml` GET parsed with stdlib ElementTree (arXiv's discipline, ADR 0008); `MeshHeadingList` descriptor names → `concepts` (the curated controlled vocabulary, the github-topics/arXiv-taxonomy role ADR 0007/0012; qualifiers dropped), author `Keyword`s the fallback for not-yet-MEDLINE-indexed records; the abstract (structured `AbstractText` sections, labels kept) → `summary` with **no `extracted_text`** (metadata-only like Crossref, ADR 0037); the article DOI → a `doi.org` `link` resolving to its `crossref:<doi>` scroll — the PubMed↔Crossref paper edge, ADR 0038's biomedical analog; publication types + journal venue → `tags`, date precedence electronic `ArticleDate`→journal `PubDate` (month-name/year-only/`MedlineDate` parsed)→history; `pubmed → paper` (ADR 0004); degrades to metadata-only, books (`PubmedBookArticle`) and PMC full text deferred |
| [0066](0066-rfc-adapter.md) | RFC adapter — IETF technical standards, keywords as concepts, DOI and obsoletes/updates as edges | technical *standards* (HTTP/TLS/JSON/OAuth RFCs) had no first-class home — a saved RFC link fell through to `web`, a concept-poor unlinked island; new `rfc` source, host-restricted shape match (RFC Editor + `datatracker`/`tools`/`ietf` hosts, only the `rfc<digits>` segment claimed so drafts/WG/org pages fall through to `web` — the shared-NCBI-host posture, ADR 0065); identity is the integer number, leading zeros stripped (`rfc0020`==`rfc:20`); one keyless `rfc-editor.org/rfc/rfc<N>.json` GET parsed with stdlib `json`; curated `keywords` → `concepts` (whitespace placeholder/dupes dropped — github-topics/MeSH role, ADR 0007/0065), maturity `status` title-cased → the one `tag` (Crossref's `type` slot, ADR 0037); the abstract → `summary` with **no `extracted_text`** (body published separately — Crossref/PubMed metadata-only shape); the RFC's DOI (`10.17487/RFC<N>`) → a `doi.org` `link` to its `crossref:<doi>` scroll (RFC↔Crossref, ADR 0038/0065's analog), each `obsoletes`/`updates` target → an `rfc-editor.org/rfc/rfc<M>` `link` (RFC↔RFC standards lineage; inverse `obsoleted_by`/`updated_by` not re-emitted since the graph resolves edges both ways — Crossref's references-economy ADR 0037); the number leads the title (`RFC 9110: HTTP Semantics`) since it is the work's canonical name; `Month Year` dates padded to first-of-month (ADR 0024); `rfc → reference` (ADR 0004), the normative-spec sibling of Wikipedia; degrades to metadata-only; STD/BCP, drafts, and spec full text deferred; twenty-eighth keyless adapter |
| [0067](0067-rfc-full-text.md) | RFC full text — the spec body becomes searchable, with plaintext de-pagination | the abstract-first/full-text-second split ADR 0066 foreshadowed, the arXiv ADR 0008→0010 pattern (abstract for `summary`, body for `extracted_text`): a second keyless `rfc-editor.org/rfc/rfc<N>.txt` GET (injectable, no `pypdf` — RFC bodies are plain text) normalized into `extracted_text` so an agent searches the *normative content*, not only the abstract; degrades to abstract-only on any `.txt` failure (the arXiv PDF-degrade contract ADR 0010 — only the JSON metadata fetch raises), `provenance.extraction_method` records `json+txt` vs `json` and the hash covers the body; one normalizer handles both formats — classic paginated RFCs (form-feed `\f` page breaks, `[Page N]` footers, running `RFC <N> … <date>` headers atop continuation pages) are de-paginated while the modern unpaginated format passes through with only blank-line collapsing, the page-1 header block kept as content, the header-drop guarded by `^RFC <digits>` so a malformed page loses boilerplate not spec; `raw_text` stays the small JSON (body re-fetched on rebuild, no second copy); validated against live RFC 9110 (modern) and RFC 2616 (classic, 27 KB boilerplate removed, no content lost); `.html`/`.xml`/`.pdf` and section-structured extraction deferred |
| [0068](0068-biorxiv-medrxiv-adapter.md) | bioRxiv / medRxiv adapter — preprint servers, two sources on one adapter, with a DOI edge to Crossref | arXiv was the only preprint server, so biology/medicine's largest preprint feeds fell through to `web` — concept-poor, edgeless islands losing subject, version, and the published-journal DOI; bioRxiv and medRxiv share one operator (Cold Spring Harbor Lab) and one keyless API (`api.biorxiv.org/details/<server>/<doi>`, stdlib `json`, arXiv's discipline ADR 0008) differing only by a `<server>` path segment, so **one fetch adapter serves both** (reading `item.source`) — but they stay **two distinct sources** (`biorxiv`/`medrxiv`), *rejecting* the huggingface kind-in-id unify, because a medRxiv paper does not live on bioRxiv and labeling it `source=biorxiv` would be dishonest (the `provenance.adapter="datacite"` honesty value, ADR 0045) — the inverse of the doi/threadiverse one-source-many-adapters shape; identity `biorxiv:10.1101/<accession>` / `medrxiv:10.1101/<accession>`, every `vN`/`.full`/`.full.pdf`/early-access view deduping to it (arXiv's `abs`/`pdf` dedupe, 6- vs 8-digit serials handled); the **highest version** in the ascending `collection` is the current preprint; the whitespace-collapsed abstract → `summary` with **no `extracted_text`** (Crossref/PubMed metadata-only shape, ADR 0037/0065) and **no PDF media** (the `.full.pdf` 403s anonymous clients — the deliberate divergence from arXiv whose PDF serves freely, ADR 0010); the `published` DOI → a `doi.org` `link` to its `crossref:<doi>` scroll — the preprint↔published edge, arXiv's `arxiv:doi` analog (ADR 0038) and PubMed's biomedical sibling (ADR 0065), unpublished preprints edgeless; subject `category` → the one `concept` (sentence-cased so `HIV/AIDS` survives — github-topics/MeSH role ADR 0007/0012), study `type` (only space-bearing, so medRxiv's `PUBLISHAHEADOFPRINT` sentinel drops) + `server` venue + recognized CC `license` → `tags`; `biorxiv`/`medrxiv → paper` (ADR 0004); degrades to metadata-only; ChemRxiv/Research Square (other APIs) deferred |
| [0069](0069-works-by-doi.md) | `scrolls works` — scholarly works clustered by shared DOI | one work now sits in the library as several near-duplicate `paper` entries (an arXiv preprint, its published Crossref article, a PubMed record, a bioRxiv/medRxiv preprint) — the "concept-level merge" the four-paper-source DOI graph (ADR 0037/0038/0065/0068) made the increasingly-motivated open step; new `works.py` engine + `scrolls works` CLI + `get_works` MCP tool cluster items by the **DOI that names the work** (the FRBR/Crossref/OpenAlex sense), not by realized link edges; a representation contributes a work's DOI when its `source_id` *is* a DOI (`^10\.\d{4,}/.+$` — `crossref` folded-lowercase, *and* `biorxiv`/`medrxiv` whose `10.1101/<accession>` is a real DOI, naturally excluding arXiv/PubMed/RFC integer ids) or it carries a `doi.org` `link` (every paper adapter emits the published work's DOI as one, resolved through the same `detect_source`+`normalize_url` path the graph uses — ADR 0023/0037); **catches clusters `scrolls graph` structurally cannot** — the graph connects two items only when a link resolves to an item *already present*, so an arXiv preprint and a PubMed record both naming `doi.org/D` with no `crossref:D` item share no edge yet are one work here (`test_clusters_without_the_crossref_hub_present`); only 2+-representation works reported by default (`--min N`, `--min 1` lists all DOI-bearing items — a single-representation work is just a paper); payload `{works:[{doi,url,representations}],stats:{items,works}}`, representations the `id`/`source`/`title`/`url`/`stage` node shape graph/related use, `stats.items` the library total; `works_over(items)` mirrors `graph_over(items)` so a future KB `library/works.md` page / merged `paper` entries (ADR 0062's analog) can pass rendered items — that KB merge and title-fuzzy matching for DOI-less duplicates deferred; pure/deterministic/offline, no schema change |
